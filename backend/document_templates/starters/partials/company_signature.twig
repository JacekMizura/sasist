<div class="signature-box">
  <strong>Podpis firmy</strong>
  <div style="margin-top:28px;">{{ company.name }}</div>
  {% if operator.name %}<div class="text-muted">{{ operator.name }}</div>{% endif %}
</div>
