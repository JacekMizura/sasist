# Barcode PDF generation (Code128) and label templates
reportlab>=4.0.0
qrcode[pil]>=7.0.0

# PDF barcode import (requires poppler: apt install poppler-utils / brew install poppler)
pdf2image>=1.16.0
# Barcode detection (Linux may need: apt install libzbar0)
pyzbar>=0.1.9
Pillow>=9.0.0
