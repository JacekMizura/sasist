- 2026-06-04 — **WMS lookup routing (prod 404):** `GET /api/wms/returns/orders/lookup` early-mount w `main.py`; usunięty alias `/lookup` (kolidował z `/{return_id}`); handler zawsze `200` + `[]`; logi `[returns.lookup]`.

- 2026-06-04 — **WMS zwroty frontend lookup:** skaner wywołuje `lookupOrdersForWms` (wcześniej tylko toast); logi debug; pusty `[]` → komunikat bez blokady idle; Enter + skan.

- 2026-06-04 — **WMS returns router mount:** `wms_returns` preload przed `order`; `_ensure_wms_returns_router_mounted()` + logi `IMPORTING WMS RETURNS ROUTER`; lazy import w `order`/`complaint`; startup dump tylko `/api/wms/returns/*`.
- 2026-06-04 — **Railway backend Python build:** `nixpacks.toml` (`providers = ["python"]`), `.python-version`, `Dockerfile`, `docs/RAILWAY_BACKEND.md` — backend service Root Directory musi być repo root, nie `frontend` (Node build → `python: command not found`).
- 2026-06-04 — **WMS returns order lookup (backend, produkcja):** `lookup_orders` + `@router.get("/orders/lookup")` + `router.add_api_route`; `main.py` mount + startup `[routes]` dump. Wymaga poprawnego deployu Python na Railway.

- 2026-06-04 — **Frontend API + WMS lookup (prod):** `vite.config` przy buildzie podmienia `http://`→`https://` w `VITE_API_URL` (Railway); `resolveAxiosBaseURL()` z `import.meta.env.VITE_API_URL`; axios bez http w PROD. Backend: `@router.get("/orders/lookup")` + alias `/lookup`, startup log `[routes] GET /api/wms/returns/orders/lookup`. Wymaga redeploy obu serwisów.

- 2026-06-04 — **WMS zwroty — lookup zamówienia (404):** `GET /api/wms/returns/orders/lookup` — przywrócony brakujący `@router.get` na `lookup_orders` oraz wydzielona `_normalize_wms_returns_lookup_query` (wcześniej kod był martwy w `_rmz_line_has_damage_photos`). Frontend `wms/returns/orders/lookup` bez zmian; `tenant_id` + `warehouse_id` w query.

- 2026-05-25 — **WMS — format czasu operacyjnego:** dodano `frontend/src/utils/formatOperationalDuration.ts` z polskim, kompaktowym formatem minut/godzin/dni/tygodni (`45 min`, `3h 25 min`, `1 dzień 3h 31 min`, `1 tydz. 1 dzień 3h`). `Centrum operacyjne` używa go dla aktywności operatorów, idle, alertów, SLA, zadań kierownika, uzupełnień, dostaw, rankingów i wąskich gardeł. WMS top bar oraz kolejki operacyjne używają tego samego formatu dla wieku zadań i czasu od przydzielenia. Backendowe etykiety `warehouse_operations_service` / `warehouse_operations_domains` również nie emitują już surowych `199h 51m`.

- 2026-05-25 — **WMS — tryb wykonania zadania kierownika:** zadania priorytetowe pakowania/kompletacji dostały aktywny kontekst pracy (`wms.activePriorityTask`) zamiast zachowywać się jak powiadomienia. Backend uzupełnia `order_ids` dla priorytetowego pakowania/zbierania i zwraca powód odrzucenia oraz historię. WMS top bar zapisuje aktywne zadanie po akceptacji/starcie, odrzucenie wymaga modala z powodem. Pakowanie filtruje listę do przypisanych zamówień, blokuje skany/nawigację poza zakresem, pokazuje sticky header i pozwala zakończyć/odrzucić zadanie; zbieranie przekazuje zakres `order_ids_csv` do listy produktów. `Centrum operacyjne` ma nową zakładkę `Zadania kierownika` z sekcjami aktywne/w realizacji/odrzucone/escalowane/zakończone oraz kartami odrzuceń z powodem i reassignment CTA.

- 2026-05-25 — **Centrum operacyjne — dispatch zadań kierownika:** alert CTAs can now create real manager priority tasks instead of dead buttons. Added priority task schemas and endpoints under `/wms/warehouse-operations/priority-tasks` (list/create/action), backed by existing `wms_operational_tasks` with manager lifecycle metadata in `payload_json`: `NOWE`, `PRZYJĘTE`, `W_TRAKCIE`, `WYKONANE`, `ODRZUCONE`, `ESKALOWANE`, assignment, deadlines, history, escalation timestamps. Alert actions for replenishment, priority packing, priority picking, and putaway use `action_type=create_task`; frontend opens an assignment modal (operator, priority, deadline, comment). `WmsTopBar` now polls assigned priority tasks and displays compact operator cards with `Akceptuj`, `Przejdź`, and `Odrzuć`.

- 2026-05-25 — **Centrum operacyjne — alerty jako incydenty:** alert model expanded from flat `message/level` to structured incidents with category, priority group (`critical_now` / `requires_action` / `informational`), impact metrics, context, owner, recommended action, CTA buttons, related entities, prediction label, and manager-focus flag. Alert generation now interprets existing domain data into actionable incidents: critical SKU shortages, packing overload/no packers, picking throughput risk, delayed putaway, delayed inbound, carrier failures, and critical idle operators. Frontend `Alerty` renders a manager focus panel and collapsed domain accordions instead of a flat alert feed.

- 2026-05-25 — **Centrum operacyjne — operator card accuracy:** operator snapshot rows now include quantity-based product/order counters, active reference labels, progress tones, persistent last-known location, order row status, last activity, and WMS navigation paths. Picking progress uses `SUM(Pick.quantity) / SUM(OrderItem.quantity)`, packing uses `SUM(OrderItem.packing_quantity_packed) / SUM(OrderItem.quantity)`. Frontend cards show Produkty/Zamówienia counters under progress, use green bars for completed work, and modal rows are clickable with active/completed/blocked/inactive states.

- 2026-05-25 — **Centrum operacyjne — mission control extension:** added modular domain aggregation service `warehouse_operations_domains.py` for replenishment/pick-face alerts, inbound receiving overview, putaway load, carrier issue projection, employee efficiency/quality ranking, bottleneck detection, and smart alert enrichment. Snapshot schema/API now exposes those domains plus expanded manager KPI header fields and export supports `zone`. Frontend `WarehouseOperationsPage` preserves the ERP `PageContainer` shell and current tabs while adding Uzupełnienia, Dostawy, Obciążenie magazynu, Problemy przewoźników, Ranking, and Wąskie gardła surfaces with live polling data. Replenishment quick action now creates/reuses a WMS relocation task through `/wms/warehouse-operations/replenishments/create-relocation`.

- 2026-05-25 — **Centrum operacyjne — live activity + ERP shell fix:** `warehouse_operations_service` now projects open/recent WMS packing sessions, operation sessions, pick rows, receiving scan logs, product warehouse operation audit rows, user activity, order events, and inventory movements into one operator-state stream. Active mode sections use `<= long_break_minutes` and the frontend groups from snapshot live arrays (`picking_operators`, `packing_operators`, etc.) so live packing/picking cards appear immediately. `WarehouseOperationsPage` is wrapped in the standard `PageContainer` white module shell.

- 2026-05-25 — **Centrum operacyjne — session-first monitor:** added explicit live session state (`last_activity_at`, `completed_reason`) to `wms_packing_sessions` and `wms_operation_sessions` with schema upgrade support. Packing order detail now starts/touches a packing session immediately on screen entry; picking product list/detail starts/touches an active picking session before first pick; quick-pick/shortage updates refresh session progress; cart finalization completes the active picking session. Shortage detail, operational tasks, receiving PZ documents, putaway documents, and MM documents also touch operation sessions. Control-center progress now prefers session `progress_percent` instead of averaging isolated event/order counts.

- 2026-05-05 — **Ustawienia WMS — przebudowa layoutu:** nowy `WmsSettingsStickyAside.tsx` (flex → aside `md:w-64` → `sticky top-20` → nav + `flex-1` treść); `WmsSettingsPage` — `PageLayout omitCard`, osobna biała karta tylko na nagłówek + zakładki, panel zakładek `mt-4 overflow-visible`; `StickySaveBar` jako **sibling** (`<>…</>`) poza kartą, `fixed z-50`. `PageContainer`: domyślna karta ma `overflow-visible` gdy nie `fillHeight`. Dirty: `isDirty = packingDirty || pickingDirty` (fingerprints w panelach).

- 2026-05-05 — **Ustawienia WMS — sticky nav + pasek zapisu:** `WmsPackingSettingsPanel` i `WmsPickingSettingsSections` — `aside` `lg:w-64` + wrapper `lg:sticky lg:top-24`, `main` na treść; `WmsSettingsSectionNav` bez własnego `sticky` (jedna warstwa sticky). `StickySaveBar` — wewnętrzny rząd `max-w-[1400px] mx-auto px-6 py-3`, komunikat „Masz niezapisane zmiany”.

- 2026-05-05 — **React Router data router:** `BrowserRouter` zastąpiony przez `createBrowserRouter` + `createRoutesFromElements` (`App.tsx` eksportuje `router`), `main.tsx` renderuje `RouterProvider` — naprawia runtime `useBlocker must be used within a data router` na `/settings/wms` (`WmsSettingsPage`).

- 2026-05-05 — **Panel list (Sellasist chrome):** `OrderList` — jeden rząd nagłówka (`Zamówienia (N wyników)`, +, Spakuj, Pokaż filtry, sort/kolumny/więcej/ustawienia), jeden rząd paska zbiorczego bez zawijania (`overflow-x-auto`, `h-9`, ikony + Spakuj + paginacja `ml-auto`); tabela — `priorityStripeBarClass` przy checkboxie, węższe komórki (`py-1.5`), akcje wiersza w jednym rzędzie (dokument, truck→multi, mail, pin, edycja). Analogiczny szkielet nagłówka + paska na `ReturnsListPanel` i `ComplaintsPanelPage` (WMS link, ustawienia modułu).

- 2026-05-05 — **Ustawienia WMS — nagłówek:** usunięto górny pasek akcji (Zapisz / Przywróć / Odśwież) i opisy localStorage; nagłówek to tytuł + krótki opis biznesowy; zapis wyłącznie przez sticky bar; usunięto duplikaty „Zapisz” w obsłudze braków i przy szkicu reguły zbierania.

- 2026-05-05 — **Ustawienia WMS — sticky „Zapisz” + dirty globalnie:** `WmsSettingsPage.tsx` utrzymuje zakładki Pakowanie/Zbieranie zamontowane (ukryte), zbiera `packingDirty`/`pickingDirty`, pokazuje `StickySaveBar`, `beforeunload` + `useBlocker` przy niezapisanych zmianach. Panele: `WmsPackingSettingsPanel` (`forwardRef`), `WmsPickingSettingsSections` (`registerActions`), rozszerzony fingerprint konfiguracji zbierania i szkicu reguły + obsługa braków przez ref.

- 2026-05-05 — **Warehouse Designer — struktura strony jak Orders:** `WarehouseDesigner.tsx` w jednym `PageContainer`/`PageLayout`: nagłówek `flex justify-between` (tytuł + `DesignerToolbar`), pod spodem jeden rząd `flex gap-4` z pulpitem/sidebar 280px i canvas `flex-1`; `RackSidebar.tsx` zsynchronizowany na 280px + separator `border-r`.

- 2026-05-05 — **TabsNav (global underline tabs):** dodano `frontend/src/components/layout/TabsNav.tsx` (`flex gap-6 border-b border-slate-200`, aktywny `border-slate-900` / `text-slate-900`). `TopTabsNavigation` renderuje wyłącznie `TabsNav` (usunięto wariant „pill” — Produkty, Dostawcy, Zakupy, BDO, Wózki, Materiały magazynowe mają ten sam układ podkreśleń; `WmsModuleLayout.tabsVariant` zostaje tylko dla **miejsca** zakładek: wiersz akcji vs slot `tabs`). `AnalyticsLayout` i poziomy pasek `DocumentsLayout` używają `TabsNav`; szczegół materiału pakowego — przyciski sekcji z `tabsNavItemClassName` zamiast pasków „pill”.

- 2026-05-05 — **Unified PageHeader + tabs underline:** dodano `frontend/src/components/layout/PageHeader.tsx` (breadcrumbs + title/actions + subtitle + optional tabs, `space-y-3`) i przepięto `ListPageHeader` na ten komponent, aby ujednolicić strukturę nagłówków bez globalnego redesignu. `TopTabsNavigation` w wariancie `underline` nie wygląda już jak przyciski (muted inactive, active z `border-b-2` i kolorem primary, tabs poniżej nagłówka). `ProductList` dostał nową strukturę nagłówka opartą o `PageHeader`.

- 2026-05-05 — **Hotfix layoutu WMS/list (full-width):** `PageContainer` wrócił do gęstego pełnego widoku (`max-width: none`, brak `mx-auto`, padding `py-3 px-4`). Dzięki temu zniknęły duże boczne marginesy na listach operacyjnych (Zestawy, Producenci, Dostawcy, Zamówienia towaru, Materiały magazynowe, Lista zamówień).

- 2026-05-05 — **Wspólny nagłówek list:** dodano `ListPageHeader` (breadcrumbs + title + actions) i użyto go na wszystkich kluczowych stronach listowych, zastępując lokalne, niespójne warianty nagłówka.

- 2026-05-05 — **Rentowność w Analiza + alias trasy:** pozycja „Rentowność produktów” jest widoczna także pod kategorią `Analiza` w sidebarze; dodano aliasy tras `/analiza` oraz `/analiza/rentownosc` → `/analytics/dashboard` i `/analytics/profitability`.

- 2026-05-05 — **Ujednolicenie layoutu list + przeniesienie rentowności do Analizy:** `PageContainer` dostał docelowe ramy (`max-w-[1600px]`, `mx-auto`, `px-6`, `py-4`). Dodano wspólny komponent `ModuleListFiltersCard` (jedno źródło panelu filtrów z toolbar + przyciski „Filtruj / Wyczyść”) i podpięto go w modułach: Zestawy, Producenci, Dostawcy, Zamówienia towaru, Kartony i Materiały pakowe. `ProductProfitabilityPage` dostał ten sam shell filtrów oraz KPI przeniesione nad filtry. „Rentowność produktów” usunięto z tabów `Products`, dodano do `Analiza` (`/analytics/profitability`), a stare `/products/profitability` działa jako redirect.

- 2026-05-05 — **Order list UX + spójne notatki:** usunięto akcje z każdego wiersza tabeli zamówień i przeniesiono je do górnego paska zaznaczeń (`OrderListBulkToolbar`: komentarz, notatka, pin/priorytet, druk, eksport). Priorytet/flame na liście ma większą czytelność (okrągłe tło + delikatny pulse dla czerwonego/pomarańczowego). Backend dostał nową tabelę `order_notes` (`order_id`, `type`, `content`, `created_at`) + migrację `ensure_order_notes_table`; `internal_note_append` (single i bulk) zapisuje teraz do `order_notes`; dodano endpoint `GET /orders/{id}/notes`. `OrderDetailPage` w sekcji „Notatki i komentarz” pobiera i pokazuje pełną listę notatek z endpointu, więc notatki z bulk są widoczne w szczególe zamówienia.

- 2026-05-05 — **Order list: kolumny Zysk / Marża % + sortowanie:** `GET /orders` zwraca teraz `gross_profit` i `margin_percent` (liczone po stronie backendu, z uwzględnieniem rabatu zamówienia i kosztu landed produktu), oraz pozwala sortować po `gross_profit` i `margin_percent`. Frontend lista zamówień dostała dwa niezależne pola kolumnowe (`gross_profit`, `margin_percent`) w selectorze kolumn, utrwalane jak pozostałe kolumny; `Zysk` ma kolorystykę decyzyjną (zielony/żółty/czerwony), `Marża %` renderowana osobno.

- 2026-05-05 — **OMS order summary — rentowność i zapis rabatu:** dodano `orders.discount_type` / `discount_value`, migrację SQLite i obsługę w `PATCH /orders/{id}`. Przeliczanie zamówienia stosuje rabat przed finalną wartością, a `OrderRead` zwraca `total_products_value`, `total_purchase_cost`, `gross_profit`, `margin` i `discount_amount`. Panel podsumowania w `OrderDetailPage` jest sticky, pokazuje koszt zakupu / zysk brutto / marżę %, zapisuje rabat i nie oznacza go już jako UI-only.

- 2026-05-04 — **Layout modułów asortymentu / magazyn / zakupy:** `PageCard` + `PageContent` w `components/layout/moduleChrome.tsx`; listy (Zestawy, Producenci, Dostawcy, Zamówienia towaru, Kartony, Rentowność) + `PurchasingPoPage` = `PageContainer` → `PageCard` → `PageContent`; `moduleListTableInteriorClass` dla tabeli w jednej białej karcie; `WmsModuleLayout` + `flushHorizontal` na Dostawcy / Materiały magazynowe / Zakupy; `PageContainer` prop `gutter: "inset"` (Produkty → rentowność, gdy rodzic ma padding); `MainLayout` pełna kolumna także dla `/bundles`, `/manufacturers`, `/suppliers/*`, `/goods-orders`, `/warehouse-materials/*`, `/purchasing/*`, `/products/profitability`. Szczegół PO: `PageContainer` zamiast `max-w-[1920px]`.

- 2026-05-04 — **`PageContainer` — szerokość panelu (1400px):** `max-w-[1400px] mx-auto px-6 w-full` w `components/layout/PageContainer.tsx`; usunięto `panelDetailPageInnerClass` — szczegóły Zamówienie / Zwrot RMZ / Reklamacja / Klient oraz listy używają jednego kontenera; `MainLayout.isMainColumnFullWidth` rozszerzone o `/customers` (+ podścieżki).

- 2026-05-04 — **MainLayout — Zwroty / Reklamacje jak Zamówienia:** `isMainColumnFullWidth` obejmuje `pathname.startsWith("/orders/returns")` oraz `/complaints` (+ podścieżki); kolumna główna `px-0` zamiast `p-10`, żeby nie zwężać w stosunku do `/orders/list`; jedyny poziomy gutter z `PageContainer` (`px-6 xl:px-8`).

- 2026-05-04 — **Layout list głównych (pełna szerokość):** `PageContainer` = `w-full px-6 xl:px-8` (bez `max-w` / `mx-auto`); `ProductList` + `CustomersListPage` ten sam wewnętrzny shell co panel (Zamówienia / Zwroty / Reklamacje).

- 2026-05-04 — **Historia produktów OMS (usunięcie / zamiana / panel):** rozszerzone snapshoty `panel_fulfillment_history` (SKU, EAN, `quantity_before`, `quantity_affected`) przy usunięciu linii i korekcie braku; `PanelFulfillmentHistoryEntryCard` + `panelFulfillmentHistoryDisplay.ts`; sekcja „Zamiany w zamówieniu” jako porównanie OLD/NEW z różnicą wartości; karty archiwum `HistoricalRemovedGhostTableRow` / `HistoricalReplacedArchiveTableRow` z pełną identyfikacją i wartościami.

- 2026-05-04 — **Order detail — sekcja braków vs badge ilości:** stopka wiersza pozycji tylko pod kolumnami 1–2 (`col-start-1 col-span-2`), żeby linia/ramka braków nie wchodziła na kolumnę z `szt.`; blok braków: `mt-4` + `rounded-lg border border-red-200 bg-red-50 p-3 pt-4` zamiast `border-t` na pełnej szerokości; kolumna akcji/cen `relative z-10`, kontener braków `relative z-0`; wiersz bez czerwonej obwódki zewn., subtelne `bg-red-50/10`.

- 2026-05-04 — **Panel listy — wspólny kontener szerokości:** `PageContainer` (`max-w-[1600px] mx-auto px-6`) w `components/layout/PageContainer.tsx`; owija breadcrumb + kartę na `OrderList`, `ReturnsListPanel`, `ComplaintsPanelPage` (zamiast `mx-auto max-w-none px-2 sm:px-3 lg:px-4`). Modale/toasty pozostają poza kontenerem.

- 2026-05-04 — **OMS szczegół zamówienia — Historia zamówienia + KPI czasów:** zgodnie z makietą (linia pionowa, znaczniki zielone z „✓”, karta nagłówka z Info + Truck, notatka `bg-yellow-50` / `border-yellow-200`, odznaki muted/dark/blue). Model `buildOrderHistoryTimelineEvents` (`orderHistoryTimelineModel.ts`) scala `created_at` („Pobranie zamówienia”), `panel_fulfillment_history`, WMS `timeline`, `order_activity_logs` (heurystyki notatki / status / automat); sort malejąco po czasie. Kafelki „Czasy operacji”: wartość u góry, etykieta + niebieski HelpCircle, chip „Trwa”. Komponent `OrderHistoryTimeline.tsx`; `OrderDetailPage` podłączony pod nowy builder.

- 2026-05-04 — **OMS wiersz zamówienia — pick/pack UI:** `WmsPackingOrderLine.picked_quantity_final` (po `picking_finished_at`/`picked_at`: pełne zebranie lub max(picked, zamówione−brak)); badge „Zbieranie” w `OrderLineWmsInlinePickPack` + blok braków używa final. Po domknięciu pakowania (`wms_packing_finished_at` / faza PACKED / `is_completed`) i linii spakowanej: `bg-green-50` `border-green-200`, watermark „SPAKOWANE” (`pointer-events-none`, opacity ~0.065).

- 2026-05-04 — **WMS — faza workflow z timestamów (nie z picków):** `orders.picking_finished_at` (migracja w `ensure_orders_wms_timeline_columns` + backfill z `picked_at`); `apply_fulfillment_state` ustawia `picking_finished_at` przy domknięciu (READY_TO_PACK / NEEDS_DECISION / MISSING). `compute_wms_workflow_phase` + pola karty OMS: `wms_workflow_phase`, `wms_cart_id`, `wms_picking_finished_at`, `wms_packing_*`. `deriveWmsOrderHeaderOperational` oparty na `wms_workflow_phase` — usunięto „wszystko zebrane ⇒ gotowe do pakowania” z sum picków. Etykieta wózka: `cart_display_name_for_wms` (nazwa lub id+wymiary); resolve-cart `display_name`; `WmsPackingCartOrdersOut.cart_display_name`; UI zbierania bez „nazwa (kod)” — pierwszeństwo nazwy wyświetlanej.

- 2026-05-04 — **WMS braki — szczegół zamówienia:** Usunięto „Rozwiąż wszystko” i link „Pełna sekcja braków”; badge inline „Brak: X szt.” przy niebieskim badge szt.; `remove_missing` → modal (`ConfirmModal` + szczegóły produktu) zamiast `confirm`. Backend: tabele `order_refund_drafts` / `order_refund_draft_lines` (DDL w `ensure_order_documents_and_activity_logs_tables`), przy `PATCH ... remove_missing` utworzenie szkicu zwrotu (jeśli brak) + linia `WMS_SHORTAGE` + log `order_activity_logs` („Produkt usunięty z powodu braku magazynowego (WMS)”).

- 2026-05-04 — **Order line edit modal — net/brutto + rabat:** `OrderEditProductModal` — Cena netto / brutto / VAT % z przeliczeniami (anchor `priceAnchorRef`); zapis `unit_price` = netto. Tryb „Edytuj” — bez sekcji rabat. Tryb „Rabat” — tylko rabat od `list_price` (jeśli jest) wzgl. `unit_price`, zapis tylko `line_edit.unit_price`. `OrderDetailPage` — symulacja „Rabat zamówienia” (state, bez API).

- 2026-05-04 — **Dokumenty zamówienia (panel) — typ + API + logi:** backend: `order_documents` / `order_activity_logs` (migracja `ensure_order_documents_and_activity_logs_tables`), enum `OrderDocumentType` w `models/order_document_type_enum.py` (unik konfliktu `models/enums.py` vs folder `enums/`), `POST /api/orders/{id}/documents/upload` (multipart `file` + `document_type`), `DELETE /api/orders/{id}/documents/{document_id}`, przy wgrywaniu wpis `DOCUMENT_UPLOADED` („Dodano dokument: …”). `OrderRead`: `order_documents`, `order_activity_logs`. Frontend: `OrderDetailPage` — po wyborze pliku w „Dokumenty” modal wyboru typu (Paragon–Korekta); „Pliki” → `ZALACZNIK`; „Listy przewozowe” → `LIST_PRZEWOZOWY`; badge rodzaju z API; zakładka „Logi” → „Dziennik panelu”; `ordersApi.uploadOrderDocument` / `deleteOrderDocument`.

- 2026-05-04 — **Order detail — Dokumenty i pliki (logika v2):** upload → `status: approved`; wiersz: druk tylko `window.open(fileUrl)` (bez `window.print` strony), toolbar druk → `console.log`; usuwanie: `Trash2` + `confirm` + usunięcie z `extra*` lub `removed*Ids`; `revokeObjectURL` dla blob. Listy przewozowe: `showTypeColumn={false}`, seed bez `typeLabel`.

- 2026-05-04 — **Order detail — Dokumenty i pliki (akcje FE):** `OrderDocTableRow` rozszerzone o `type`, `fileUrl?`, `mimeType?`. Toolbar: `input type=file` hidden + klik Upload; po wyborze pliku `console.log` + wiersz z `URL.createObjectURL` w stanie `extraOrderDocRows` / `extraOrderFileRows` / `extraOrderWaybillRows`. Wiersze: podgląd (PDF/obraz + URL → `window.open`; inaczej modal z nazwą), pobierz (`window.open` lub `console.log`), drukuj `window.print()`, e-mail `console.log`. Pierwszy list przewozowy (gdy są numery) — przykładowe `fileUrl` PDF (W3C dummy) do testu.

- 2026-05-04 — **Order detail — Dokumenty i pliki (ikony):** usunięto blok „Paragon / Faktura” oraz powiązany stan/API serii (`DocumentSeriesQuickCreateModal`, seria dokumentów). Toolbar sekcji: Print → **Upload** (`lucide` Upload, „Dodaj plik”) → Mail; wiersze bez zmian układu: Eye, Print, **Download**, Mail (pobieranie tylko w wierszu).

- 2026-05-04 — **Order detail — zakładka „Dokumenty i pliki”:** `OrderDetailPage` — trzy sekcje w `space-y-6` (karty `rounded-lg border border-gray-200 bg-white p-4 shadow-sm`, `space-y-3`, nagłówek, toolbar `flex gap-2`: checkbox + „wykonaj” + Printer/Download/Mail, tabela: `thead` `bg-gray-50 border-y border-gray-200` uppercase, wiersze `border-b border-gray-100`, akcje wiersza `Eye`+print+download+email `justify-end gap-2`, badge Zatwierdzony/Niezatwierdzony obok nazwy). Sekcje: **Dokumenty (n)** z kolumną Rodzaj, **Pliki (n)** bez Rodzaj, **Listy przewozowe (n)** jak Dokumenty. Zachowany blok edycji Paragon/Faktura; wiersze tabel z danych zamówienia (dokument sprzedaży, pliki — placeholder, listy przewozowe z `panel_tracking_numbers`).

- 2026-05-04 — **Order detail — zakładka Komunikacja (layout):** `OrderDetailPage` — `flex gap-6`: kolumna główna `flex-1 min-w-0` (Nowa wiadomość: rząd kanałów + szablon, textarea z „Sugestia AI”, Dodaj załącznik / Wyślij; Historia korespondencji z nagłówkiem metadanych + Szukaj i typami wiadomości), sidebar `w-[360px] shrink-0` (`space-y-4`, karty `rounded-lg border bg-white p-4`: Notatka AI, Klient, Zamówienia, Notatki). Bez zmian logiki danych (`noteDraft`, te same źródła WMS/order).

- 2026-05-03 — **Order detail — layout flow (flex, not cosmetics):** `OrderDetailPage` — górny rząd pozycji `flex justify-between items-start`: lewo obraz+título+meta, prawo `flex flex-col items-end` (⋮ bez absolutu na przycisku → menu `absolute` pod przyciskiem), „Wartość”, badge szt.; drugi rząd `w-20` spacer + metryki/WMS/braki/historia w jednej kolumnie. Nagłówek: siatka `lg` `[1fr | auto | 1fr]` gdy operacyjny blok WMS — badge „Zbieranie” w tym samym rzędzie co tytuł i toolbar; status „Zakończone…” wyrównany do tego samego kontenera co strzałki/tytuł. `OrdersPanelStatusSidebar`: opcjonalny `titleTrailing` (np. zwijanie w jednym rzędzie ze „Status panelu”). Tekst szczegółów WMS w środkowej kolumnie: `text-left`.

- 2026-05-03 — **Order detail — sidebar flush right:** główny wrapper `w-full px-6`; siatka tabów `lg:grid-cols-1` poza zakładką Podsumowanie (wtedy nadal 2 kolumny dla panelu wysyłki), żeby pusty drugi tor siatki nie zajmował ~12–15rem przy „Produkty i magazyn”; kolumna produktów `flex-1 min-w-0`, aside `w-[320px] shrink-0 ml-auto`.

- 2026-05-03 — **Order detail — layout pass (Tailwind only):** `OrderDetailPage` — sekcja produktów `flex gap-6/8`, lewa `flex-1`, aside `lg:w-[320px] lg:ml-auto`; wiersz pozycji `grid-cols-[minmax(0,1fr)_auto]` — metryki `gap-x-8`, osobny rząd lokalizacja + `OrderLineWmsInlinePickPack` (`picking` + `packing`) z `ml-auto` na grupie statusów; badge ilości (`szt.`) — `bg-blue-700 text-white font-semibold`; bez zmian logiki/API.

- 2026-05-03 — **Order detail — layout szerokość + produkty:** główny wrapper strony `w-full max-w-none px-6` (bez `mx-auto`); siatka produkty + panel `grid w-full … lg:grid-cols-[1fr_360px] gap-6`; wiersz pozycji `grid-cols-[auto_1fr_auto]` (obraz | info | cena/ilość), osobny wiersz `mt-2 flex flex-wrap` dla lokalizacji + WMS; panel boczny `flex flex-col gap-4`; kebab w `h-9 w-9` + `absolute` na przycisku.

- 2026-05-03 — **Order detail — wiersz pozycji „Produkty i magazyn” (siatka + jedna linia):** `OrderDetailPage` — układ `grid-cols-[80px_minmax(0,1fr)_220px]` (obraz | treść | prawa szyna); w środku tylko 2 rzędy: nazwa+meta, potem **jeden** `flex-nowrap gap-6` z Cena→Rabat→VAT→Stan mag.→Rezerwacja→`LocationTypeBadge`→inline pick/pack (`OrderLineWmsInlinePickPack` z timeline WMS, bez użytkownika przy „Gotowe do pakowania”); usunięte osobne `OrderProductLineWmsTracks`. `normalizeStorageType` — aliasy PL: `podstawowa`/`zapasowa`/`sklepowa`/`buforowa`.

- 2026-05-03 — **WMS fulfillment panel — timestamps + timeline:** `Order` ma kolumny `picking_started_at`, `picked_at`, `packing_started_at`, `packed_at` (migracja `ensure_orders_wms_timeline_columns`). Ustawiane bez nadpisywania: pierwszy `touch_picking_in_progress`, `apply_fulfillment_state(..., READY_TO_PACK)` → `picked_at`, mutacje pakowania / `packing_finish_order` → `packing_started_at` / `packed_at`. `build_wms_timeline_and_operation_times` buduje minimalną oś z tych pól + `operation_times` (`picking_time` / `packing_time` / `total_time` w sekundach, aliasy `*_seconds`); API karty duplikuje `timeline` / `operation_times`. Usunięty błąd `order.updated_at` (Order go nie ma).

- 2026-05-03 — **Order detail — Sellasist shell + WMS panel data:** padding jak lista (`max-w-none px-2 sm:px-3 lg:px-4`), karta z cieniem jak `OrderList`; siatka produktów **75% / 25%**; wiersz pozycji — mniejszy obrazek, menu ⋮ `absolute` przy prawym górnym rogu wiersza, prawa szyna: status (nowe etykiety WMS: *Do pakowania* / *W zbieraniu* / *Gotowe do pakowania* / *Zebrano* + brak/archiwum), cena, badge ilości; API `GET .../wms-fulfillment`: `wms_timeline`, `wms_operation_times`, `wms_picking_line_status` na linii — serwis `wms_order_fulfillment_panel_extras` (PICK z `fulfillment_events`, czasy pick/pack/total). Historia zamówienia scala WMS + panel.

- 2026-05-03 — **Order detail — Produkty i magazyn (UX pass 3, rhythm + rail):** większe odstępy w wierszu pozycji (nazwa, meta, siatka Cena/VAT z `gap-x-6`/`lg:gap-x-8`, rząd lokalizacji `mt-2.5`); badge kompletacji przeniesiony na **prawą szynę** wyrównaną pionowo z treścią (osobno od `LocationTypeBadge`); większy obrazek bez ramki/tła; sekcja produktów bez „karty” (`border-b` zamiast obramowania całej sekcji); siatka listy **~72% / ~28%** + szerszy kontener strony `max-w-[min(1920px,96vw)]`; prawa kolumna Czasy/Historia — typografia `text-sm`/`text-xl`, listy `space-y-5`, bez szarych boxów (same separatory); historia zamian/braków — `border-b` zamiast kart z cieniem; główna karta zamówienia `shadow-none`.

- 2026-05-03 — **Order detail — Produkty i magazyn (UX pass 2):** `OrderDetailPage` — strzałki prev/next + przełącznik „biuro” (🔥, `localStorage` `order_office_pin:{id}`) po lewej przy tytule; **Spakuj** w tym samym rzędzie co zakładki (prawy brzeg, przycisk primary); węższy sidebar statusów (`~13.5rem`) i węższy rail czasów w produktach; lista pozycji jako wiersze (`border-b`, bez kart/szarych płytek), większa typografia, VAT z `vat_percent`; lokalizacja przez `LocationTypeBadge` + `location_storage_type` z WMS; menu ⋯: Edytuj (`OrderEditProductModal` + `patchOrderItemLine` `line_edit`), Zamień, Usuń (`deleteOrderItemLine`), Karta produktu (bez „Spakuj” w menu). API: `ordersApi.patchOrderItemLine` typ z `line_edit`; `wmsPackingApi` pole `location_storage_type`. Backend (sesja wcześniejsza): `OrderItemRead.unit` + `vat_percent` w `build_order_read`.

- 2026-05-03 — **Order detail — Produkty i magazyn (layout only):** `OrderDetailPage` — nagłówek jak Sellasist (tytuł bez `#`, wiersz ID zew + data, pasek 8 ikon z tooltipami: zwroty/reklamacje menu, wiadomość, korespondencja, dokumenty, załącznik placeholder, kopiuj, druk, ustawienia statusów); zakładka produktów: siatka ~70/30 z kartami pozycji (cena/rabat/VAT/stan/rezerwacja, lokalizacja, badge kompletacji bez zmian logiki), menu ⋯, zwinięcie listy „Zwiń”; prawa kolumna: siatka czasów (placeholder) + oś czasu z `panel_fulfillment_history` + notatki WMS. `OrderHistoricalProductTableRows` — wiersze archiwum jako karty zamiast `<tr>`.

- 2026-04-30 — **Order detail primary status:** Usunięto etykietę „Status panelu” i `<select>`; dodano `OrderDetailPrimaryStatusDropdown` — chip `panelSidebarSubRowStyleRich` (`primaryChip`, pasek 5px) z pełną etykietą „Grupa: nazwa”, lista jak sidebar (`buildPanelSidebarLayout`). Opcja `primaryChip` w `panelSidebarHierarchy`.

- 2026-04-30 — **Orders list status visuals:** `panelSidebarSubRowStyleRich` — tło z konfiguracji z niską kryciem (~0.11–0.22 zamiast ~0.88–0.94); opcja `inlineLabel` dla lżejszego cienia. Sellasist sidebar: podstatusy przez ten sam Rich + `panelSidebarSubRowClass` (pasek, tło, `text_color`), ikona z API, lżejsza typografia podstatusów; grupy z separatorem `border-t`, `rounded-lg`, `font-semibold`; węższy indent listy podstatusów. `OrderUiStatusConfigRowPresent` — wariant `inline` (`inline-flex w-fit`, mały padding) dla kolumny zamówienia w `OrderListDenseTable`.

- 2026-04-30 — **Order detail UI alignment:** `OrderDetailPage` — same shell as `OrderList` / `ProductEditPage` (`bg-slate-100`, `px-2 sm:px-3 lg:px-4`, white `rounded-xl` panel); breadcrumb; `OrderStatusSidebar` with collapse + mobile drawer like lista; header uses `OrderUiStatusConfigRowPresent` (compact); toolbar `listSellasistToolbarSquareBtn` (prev/next, lista); flat underline tabs; lighter `PanelCard` / sections; logs „Status panelu” uses same status row. `OrdersLayout` numeric `/orders/:id` uses `flushHorizontal` without bleed; `MainLayout` wide gutter for `/orders/\d+`. `OrderPanelActionsDropdown` trigger uses `listSellasistToolbarSquareBtn`.

- 2026-04-29 — **Product edit scroll + rail:** `ProductEditPage` / `ProductNewPage` — bez `fillHeight`; `ProductEditModal` (page) bez zagnieżdżonego scroll-locka; sticky nagłówek + Podmiot/marża; sidebar = pionowy rail z wysuwanym labelkiem; szablon etykiety w zakładce Etykieta.

- 2026-04-29 — **Product edit UX:** `ProductEditModal` — przywrócone **górne zakładki** jako jedyna główna nawigacja; treść zakładek `activeTab === …` (bez sekcji scroll-to-id); usunięta boczna lista tekstowa Skróty; sidebar = karty **Podsumowanie** + **Szybki dostęp** (ikony z `title`/`aria-label`) + etykieta; padding strony `p-4 sm:p-5` jak karta listy produktów; copy „zakładka Podstawowe” zamiast „sekcja”.

- 2026-04-29 — Products list width: `max-w-[1800px]` + modest `px-3–px-5`; `PageLayout` `flushHorizontal` na `/products/list`; `MainLayout` mniejszy gutter (`p-3–p-6`) tylko dla listy; kolumna Lokalizacje `min-w-[26rem]`/`lg:28rem`, `LocationBadgeStack` bez sztucznego `max-w-[18rem]`; tabela `min-w-max` + scroll.

- 2026-04-29 — Products list Sellasist shell: `bg-slate-100` page band, centered `max-w-[88rem]`, breadcrumb above white `rounded-xl` panel (border + shadow, inner `p-6–p-8`); row actions only duplicate (placeholder), edit, delete — removed print label, warehouse, inventory link, PO draft.

- 2026-04-29 — Products list readability pass: removed `PanelPageContentBleed` on lista (restore `PageLayout` padding); tokens + `ProductList` — `text-sm` table body, `text-xs` headers, `py-2.5`/`px-3` cells, 80×80px thumbnails, `h-9 w-9` row actions, `min-w-[64rem]` table scroll; toolbar `h-10`.

- 2026-04-29 — Products list UI (Sellasist-like): `/products/list` uses `PageLayout` without module tabs (`ProductsLayout`); `ProductList` + `listSellasistTokens` — white dense grid table, compact toolbar/actions, plain photo cells; locations badges unchanged; filter panel `tone=white` without shadow.

- 2026-04-25 — Product Profitability Center (new module):
  - Added backend service `backend/services/product_profitability_service.py` with unified formulas:
    - `cost_of_goods = sold_qty * landed_cost_net`
    - `profit = revenue_net - cost_of_goods`
    - `margin_percent = profit / revenue_net * 100`
    - `frozen_capital = stock_qty * landed_cost_net`
    - `days_cover = stock_qty / avg_daily_sales`
  - Added API route `GET /products/profitability` in `backend/api/product.py` and response schema `backend/schemas/product_profitability.py`.
  - Added frontend API client `frontend/src/api/productProfitabilityApi.ts` and new page `frontend/src/pages/Products/ProductProfitabilityPage.tsx` (filters, KPI cards, sortable table, detail drawer with recommendations).
  - Wired navigation and routing: `frontend/src/App.tsx`, `frontend/src/modules/products/productModuleTabs.ts`, `frontend/src/layout/mainNavConfig.tsx`.

- 2026-04-25 — `useLocalStorage` hardening + profitability page: invalid third argument (e.g. options object) no longer crashes; `ProductProfitabilityPage` passes numeric allowed arrays and persists `products.profitability.rangeDays`.

- 2026-04-25 — Purchase price sync after PZ posting:
  - Added product purchase snapshot columns (`previous_purchase_price`, `last_purchase_date`, `last_supplier_id`, `last_purchase_currency`, plus optional `purchase_price_original`/`purchase_currency`) with SQLite schema upgrade `ensure_products_purchase_snapshot_columns`.
  - Extended `accept_stock_document` trigger in `stock_document_service` to update product snapshot tenant-safely from posted PZ lines where `received_quantity > 0` (weighted unit net when multiple lines per product).
  - Updated product API serialization/enrichment to expose last-purchase snapshot fields, including `last_supplier_brief`.
  - Updated Product edit Prices tab to show current/previous purchase price, last purchase date, and last supplier (plus purchase currency/original price).

- 2026-04-25 — Purchasing business-logic fixes (A–E):
  - Added FX infrastructure: new model `CurrencyExchangeRate`, schema upgrade helpers for `currency_exchange_rates`, PO tax/invoice columns, and wiring in startup upgrades.
  - Added `currency_rate_service` with NBP table A fetch, manual override upsert, date-based rate resolution, and EU-country reverse-charge helper logic.
  - Extended PO domain/API (`purchase_orders`, schemas, services, `/purchasing/orders/*`) with `tax_mode`, `invoice_date`, FX metadata and PLN simulation fields.
  - Added FX endpoints in purchasing API: `GET /purchasing/fx/rates`, `POST /purchasing/fx/manual`, `POST /purchasing/fx/nbp/fetch`.
  - Implemented supplier-scoped manufacturer filtering: `GET /supplier-products/linked-manufacturers` + frontend usage in `PurchaseOrderEditModal`.
  - Fixed negative-discount UX in purchasing views: never show negative discount; show “Powyżej katalogu +X%” when purchase price is above catalog.
  - Added pricing card fields in replenishment product inspector: purchase EUR, purchase PLN net, sale PLN gross (23%), margin %.

## 2026-04-25

- Warehouse layout consistency hotfix:
  - Unified visible location format to `{rack}-{column}-{level}` (example `A2-A-1`) in `frontend/src/components/warehouse/warehouseUtils.ts` via `buildVisibleLocationCode`, and in backend fallback label generator `_bin_label` (`backend/services/warehouse_layout_service.py`).
  - Updated display helpers (`getDisplayLocationLabel`, `getDisplayLocationLabelPhysicalOrder`) to always render canonical visible code, while keeping `location_uuid` as internal identity.
  - Added backend validation `_validate_internal_structure_widths` to reject rack levels where `sum(slot width_cm) > rack.width_cm` (HTTP 400).
  - Adjusted bin width generation to deterministic per-level distribution (`distributedWidthsCm`) so total slot widths match rack width exactly.
  - Added volume display guard in `formatVolume`: invalid/non-positive values now render `Brak danych` instead of numeric artifacts (`NaN`, `0.00`) in UI.
- Rack internal layout width badge fix:
  - Backend `WarehouseLayoutService.get_layout()` now includes computed `width_cm`, `depth_cm`, `height_cm` per bin in `racks[].bins[]` payload.
  - Frontend `InternalLayoutModal` now resolves dimensions safely and never renders empty labels (`SZ cm`): badges show `SZ <value>` or `SZ —`.
  - Added width fallback in renderer: if slot `width_cm` is missing, width is derived as `rack_width / slot_count` on that level.

# Change Log

## 2026-04-25

- **Zakupy — PO cleanup (usuwanie/archiwizacja):** nowy `DELETE /purchasing/orders/{id}` z polityką biznesową: `Draft` bez przyjęć PZ usuwa się fizycznie; PO z przyjęciami PZ lub statusami po wysyłce są archiwizowane (`Cancelled`, znacznik `[ARCHIVED]`), bez hard-delete.
- **Zakupy — numeracja PO (czytelna):** widoczny numer zamówienia uproszczono do `PO/{rok}/{id}`; nowo generowane PO zapisują już ten format, a listy/szczegóły używają wspólnej prezentacji numeru.
- **Zakupy — Historia współpracy (real data):** zastąpiono statyczny ekran nowym API `GET /purchasing/cooperation-history` i UI analitycznym: selektor dostawcy, KPI (total orders, total receipts PZ, first order, last delivery, avg delivery time, on-time %, total net spend, price trend) oraz tabela ostatnich dokumentów PO/PZ.
- **Zakupy — UX spójność:** w szczególe PO dodano jednolitą akcję „Usuń / archiwizuj” (w tym bezpieczny fallback do archiwizacji przy powiązaniach PZ), spójne komunikaty zwrotne i redirect na listę z toastem.

- **Zakupy — Supplier analytics PZ-first:** `build_supplier_analytics` przestał opierać się na `InboundDelivery` jako głównym źródle wykonania; metryki dostawców liczone z `stock_documents` (`document_type='PZ'`) + `RECEIPT` (fallback: linie PZ). Dodane pola API: `deliveries_count`, `planned_orders_count`, `total_purchase_value_net`, `total_purchase_value_gross`, `avg_delivery_interval`, `on_time_rate`, `price_trend`; ranking preferuje realne PZ.
- **Zakupy — dashboard recent docs:** `purchasing_dashboard_service` sekcja `recent_orders` pokazuje ostatnie dokumenty `PZ-*` zamiast listy `InboundDelivery`.
- **Zakupy — oszczędności (price opportunities):** wolumeny i historia cen zakupowych przeliczone na bazie PZ (`stock_document_items.received_quantity`, `RECEIPT.unit_price_net`) zamiast PO-first; fallback na linie PZ przy braku RECEIPT ceny.
- **Zakupy — alerty opóźnień:** reguła `delayed_supplier_delivery` sprawdza najpierw przeterminowane PZ (z expected date na dostawie i bez przyjętej ilości), a alerty PO utrzymane jako fallback.
- **Frontend — ocena dostawców:** tabela i copy przestawione na PZ-first (kolumny: dostawy PZ, PO planowane, terminowość PZ, interwał dostaw, trend ceny, wartość netto), typy API rozszerzone o nowe pola.

- **Produkt — Operacje magazynowe (ledger hotfix):** `GET /products/{id}/movements` przestał zwracać puste `quantity_before/after` — teraz wylicza running balance po chronologii zdarzeń i zwraca te pola per wiersz. Feed wzbogacony o `document_id/document_number/document_type` oraz `unit_cost_net/gross` (dla RECEIPT).
- **Putaway vs podwójne przyjęcie:** mapowanie `STOCK_OP_PUTAWAY` ma teraz `delta=0` w historii produktu (rozlokowanie to relokacja/audyt, nie drugi przyrost globalnego stanu).
- **Produkt — Historia dostaw:** nowy endpoint `GET /products/{id}/delivery-history` agreguje przyjęcia `RECEIPT` z PZ (data, dokument, dostawca, qty, unit net/gross, total net/gross).
- **Frontend produktu:** `ProductWarehouseMovementsPanel` ma dwa taby: **Operacje magazynowe** (kolumny: data, akcja, dokument, użytkownik, lokalizacja, przed, zmiana, po) i **Historia dostaw** (zakupowa tabela PZ).

## 2026-04-19

- **Wózki — zdjęcie z dysku:** `POST /api/uploads/image` zapisuje JPEG/PNG/WebP (max 5 MB) w `uploads/carts/`; odpowiedź `{ url: "/uploads/carts/…" }`. Wspólny komponent `CartImageUrlField` (upload, URL, podgląd 120px, usuń) w `CartEditor` i `BulkCartEditor`.

- **Nawigacja + szczegóły zamówienia:** z fly-outu Zamówienia usunięto Import zamówień, Zwroty — statusy panelu, skrót „+” Nowe zamówienie; z Asortymentu — Import produktów. Na stronie zamówienia (`OrderDetailPage`) jeden przycisk toolbar (dropdown chevron) — **Utwórz reklamację** (istniejący `NewComplaintWizard`) i **Utwórz zwrot** → `/wms/returns` z `state.preselectOrderId` + `openReturnCreateForm` (otwiera formularz RMZ na `WmsReturnsEntryPage`). Lista zamówień: usunięty duplikat „Nowa reklamacja”. Nowy komponent `OrderPanelActionsDropdown.tsx`.

- **Panel — lista zamówień (Sellasist-like):** `OrdersLayout` bez duplikatu tytułu na `/orders/list`; `OrderList` — breadcrumbs, licznik wyników, `[+]`, **Spakuj** → WMS packing, narzędzia (filtry / pola filtrów / **wybór kolumn** / ustawienia); filtry w `OrderListFiltersPanel` w trybie **embedded** (`FilterPanel` muted + siatka jak Produkty); **OrderStatusSidebar** (`OrderStatusSidebar.tsx` = alias `OrdersPanelStatusSidebar`) z `chromeVariant="sellasist"` + mobilny drawer „Statusy panelu”; tabela `OrderListDenseTable` (gęste wiersze, miniatury, płatność, przewoźnik, **sticky Akcje**); paginacja u góry karty; preferencje kolumn `localStorage` **`orders_columns_layout`**. Nowy **`ColumnSelectorModal`** (`@dnd-kit` + dual-list) i **`columnLayoutPreferences.ts`**.
- **Panel — lista produktów:** dynamiczne kolumny wg **`products_columns_layout`**, przycisk **Table2** obok wyboru pól filtrów, ten sam modal wyboru kolumn, **sticky** kolumna Akcje; naprawa `normalizeColumnOrder` (można ukryć kolumny).

## 2026-04-17

- **WMS RMZ — „ZAPISZ” na procesie:** po udanym zapisie wszystkich brudnych linii `navigate` na hub `/wms/returns` (`replace`), opcjonalnie `state.preselectOrderId` z zamówienia; odświeżenie `GET` RMZ + event listy w `try` — przy błędzie tylko log, **redirect i tak**; `sessionStorage` `wms_returns_saved_toast` → zielony komunikat na `WmsReturnsEntryPage`. Błąd zapisu: zostajemy na stronie + `Nie udało się zapisać zwrotu` gdy brak treści z API.

- **WMS RMZ — izolacja kart (kolizja `lineId`):** `lineId` było `ln-${order_item_id}` — dwie linie RMZ z tym samym `order_item_id` (lub duplikat wiersza) współdzieliły `unitRowsByLineId` / overrides i nadpisywały stan. Nowe ID: `rmzl-${rmz_lines.id}` gdy `line.id` z API, inaczej `ln-oi-${order_item_id}-r${rowIndex}`. Masowe accept/reject i zapis modala uszkodzenia biorą `orderItemId` z `lineSeedByLineId`, nie z parsowania `lineId`. Panel RMZ: `key` karty jak wyżej zamiast `order_item_id + product_id`.

- **WMS RMZ — odrzucenie w gridzie:** „Potwierdź odrzucenie” tylko ustawiało lokalnie jednostki + `applyLinePatch`, bez zapisu API — karta zostawała „niedokończona”. `confirmGridRejectEditor`: wyrównanie wierszy do ilości linii, `applyFirstNNullUnitsDecisionToRows` + `flushSync` + przy pełnej linii `saveSplitForLine` → `POST …/process` REJECTED z powodem (`fullLineRejectReason`); badge „ODRZUCONY” po zapisie, przycisk **✓ ODRZUCONY**, przycisk potwierdzenia zablokowany bez powodu / przy „Inny powód” bez tekstu. Backend `process_rmz_line`: dla OK/REJECTED ustawiane `accepted_qty` / `rejected_qty` (i `final_disposition`) jak przy split — poprawny odczyt po odświeżeniu.

- **WMS RMZ — TDZ crash:** `setUnitDecision` miał `applyLinePatch` w tablicy zależności **przed** deklaracją `const applyLinePatch` → błąd „can't access lexical declaration before initialization”. `applyLinePatch` przeniesione nad `setUnitDecision`.

- **WMS RMZ — pełny accept + panel „Przyjęte”:** `beginGridDecision` przekazuje do `saveSplitForLine` **wyliczone wiersze** (`applyFirstNNullUnitsDecisionToRows`), żeby zapis nie czytał przestarzałego `unitRowsByLineId` z zamknięcia hooka (wcześniej mogło cicho zwracać `false` i cofać UI). Panel `LineOperationsCard`: optymistyczne `OK` (zielony pasek + **✓ PRZYJĘTY**), toast „Zapisano”, rollback przy błędzie API.

- **WMS RMZ — przyjęcie jednym klikiem:** po pełnym „Przyjęty” na linii (grid / modal ilości / widok szczegółowy ✓) natychmiast `POST …/process` (same OK) lub `split-process`, `setWmsReturn` + wyczyszczenie dirty dla linii, toast „Zapisano”; globalny „Zapisz” z `hydrateReturn: false` nadal dla wsadu. Przyciski blokowane na `savingSplitByLineId`.

- **WMS RMZ grid — przyjęty + zapis + panel zdjęć:** `buildUnitRowsForLineSeed` — gdy `initialDecision === "OK"` a suma accepted/damaged/rejected &lt; ilości linii, uzupełnij przyjęte do pełnej ilości (API bez `accepted_qty`). Karta: zapisany pełny accept — zielona obwódka/tło jak „pre-save”, stała plakietka `PRZYJĘTY (A)`; zapisane DAMAGED/REJECTED/mix — osobne obramowania. Przycisk paska `ZAPISZ ZMIANY` → `ZAPISZ`. Panel `ReturnsReturnDetailPage` — mini galeria z `line.photo_urls` (WMS + panel).

- **WMS Zwroty / Reklamacje — kafle kolejki (skan statusu):** `WmsReturnsEntryPage` — bez nazw produktów na liście: RMZ = „N pozycji • M szt.”, reklamacja = liczba pozycji reklamacyjnych + `Powód:`; API `ComplaintListRead.lines_count`. Zakończone: tło `#f3f4f6`, border `#d1d5db`, treść karty `opacity` ~0.78 (wstążka osobno, nasycona); duża wstążka przekątna (~35°, szer. do 175%, tekst ~20px). Usunięty stopka „Ostatnia aktualizacja…”. Wykrywanie zamknięcia RMZ bez zmian (done\_\* + słowa kluczowe).

- **WMS RMZ — zapis bez auto-refund + status panelu + UX:** `handleSaveDirtyLines` nie wywołuje już `POST …/refund` (magazyn nie blokuje się na 400). Status w nagłówku: lista z `GET office/return-ui/summary` + `PATCH office/return-ui/returns/{id}/ui-status` (etykiety kubełków Nowe/W toku/Zakończone), usunięty dropdown `ReturnStatus` / workflow. Plakietka nagłówka i „Etykieta panelu” z `ui_status`; blok zwrotu kosztów dostawy przy `enable_refund=false` → krótki tekst o rozliczeniu w biurze. `buildUnitRowsForLineSeed` + `flushSync` przy pierwszym kliku decyzji gdy brak wierszy jednostek; `handleGridDecision`; po pełnym ACCEPTED `applyLinePatch` OK; `acceptAllLocal` ustawia OK na liniach. Panel szczegółu zwrotu: bez trybu „advanced” w copy, chip tylko z etykiety panelu, podział wielosztukowy w `<details>`, przyciski Przyjęte/Uszkodzone/Odrzucone, zwrot środków przy wyłączonym WMS — tylko neutralny tekst (bez przycisku API).

- **Panel RMZ — decyzje proste jak WMS:** `ReturnsReturnDetailPage` / `LineOperationsCard`: „Przyjęty” zapisuje od razu; „Uszkodzony” / „Odrzuć” otwierają boczny panel (B/C, typy RMZ, zdjęcia + `createDamageEntry` gdy są URL, notatka); odrzucenie — lista powodów + „Inny powód” + notatka. Typ `WmsReturnLineProcess.note` (front). Backend `POST …/lines/…/process`: REJECTED wymaga `damage_type`; dla `ops_other` wymagana notatka; naprawa zapisu `damage_type` przy REJECTED (wcześniej kasowane w gałęzi `else`); DAMAGED łączy notatkę z `damage_type`. Mass reject WMS: `damage_type: "mass_reject"`.

- **WMS RMZ — pełny pakiet status / split / zwrot / zdjęcia / warstwy statusów:** `PATCH /wms/returns/{id}/status` + `WmsReturnWorkflowStatusPatch`; lista statusów z `GET /wms/return-statuses`. Linie w `WmsReturnRead`: pole `photo_urls` (parsowane z `rmz_lines`); split-process zapisuje `photo_urls` także przy pustej liście (czyści). `POST /refund`: przy `enable_refund=false` dozwolony tylko `NONE` — zamknięcie przyjęcia bez blokady finansowej. Front: dropdown workflow + „Zapisz status”, nagłówek „Etap / Status”, panel „Zwrot środków (informacja)”, „Zapisz zmiany” z `NONE` gdy zwrot wyłączony, formularz szybkiego podziału ilości (×N), overlay „mieszany” po zapisie, deduplikacja URL zdjęć, event `wms-returns-list-refresh` dla listy panelu.

- **WMS RMZ — „Return already finished” vs edytowalny UI:** W trybie `simple` przejście po pełnym podziale linii szło od razu na `success` → status terminalny i blokada `split-process`, mimo że użytkownik oczekuje dalszej edycji do zwrotu. Backend: `_next_transition_key_for_lines` w `simple` (bez all-rejected) zwraca teraz `qc_complete` zamiast `success` (zwrot nadal kończy `success` po refundzie). `WmsReturnRead`: pola `workflow_finished` / `workflow_editable`; front: `wmsReturnWorkflowFinished`, badge nagłówka, log „Saving split” z `status`, `workflow_finished`, `workflow_editable`.

- **Panel zwrotów — liczniki vs lista:** `GET /wms/returns/` zawsze używał magazynu z `Depends` (domyślny tenant), a `office/return-ui/summary` i lista w UI brały `warehouse_id` z kontekstu magazynu → „Bez etykiety (1)” przy pustej liście. Lista: opcjonalny query `warehouse_id` (jak summary); filtr grupy: join `ReturnUiStatus` dodatkowo po `tenant_id` + `warehouse_id` magazynu. Front: `listAllWmsReturns(..., warehouseId)`; ustawienia statusów zwrotów (`ReturnPanelUiStatusesSettingsPage`) — ten sam `warehouseId` przy summary / CRUD.

- **Reklamacje — GET szczegółu / WMS zapis zdjęć:** `GET /complaints/{id}/`, `POST …/photos`, `POST …/wms-update`, `GET …/events` używały magazynu z `Depends(complaint_panel_warehouse_id)` (domyślny tenant), więc reklamacja na magazynie zamówienia dawała 404 mimo poprawnego `id`. `_warehouse_id_for_complaint_by_id` — magazyn z wiersza + opcjonalny query `warehouse_id`. WMS: `WmsComplaintDetailPage` — `parseInt` na parametrze, komunikat + toast przy 404, log DEV przy ładowaniu; `WmsReturnsEntryPage` — log DEV przy otwarciu z kolejki.

- **WMS RMZ zapis (split-process / process / refund):** endpointy POST używały `warehouse_id` z domyślnego magazynu tenanta (`Depends`), a `GET /wms/returns/{id}` ładuje dokument po magazynie wiersza RMZ — stąd 404 „Return not found” przy zapisie na magazynie ≠ domyślny. POST-y: `split-process`, `lines/.../process`, `refund` — `_warehouse_id_for_return_mutation` (jak GET) + opcjonalny query `warehouse_id`. Front `WmsReturnsPage`: `selectedReturnDbId` z `wmsReturn.id`, guard + log przy zapisie split.

- **WMS Zwroty / Reklamacje — kolejka OMS + RMZ:** `listComplaints` z `warehouse_id` z zamówienia (`GET orders/:id/` → `warehouse_id`) lub z kontekstu WMS; scalona lista zwrotów + reklamacji po `created_at` (malejąco), bez duplikatów (`kind` return vs complaint). UI: chipy Wszystko / Zwroty / Reklamacje; kafle ZWROT / REKLAMACJA z podtytułami (zamówienie, klient, produkty/powód/SLA); jeden ref fokusu `firstQueueTileRef`.

- **WMS RMZ — GET pojedynczego zwrotu:** `GET /wms/returns/{id}` nie używa już domyślnego magazynu tenanta — najpierw `WmsOrderReturn` po `id`+`tenant_id`, potem `_load_rmz` z magazynem dokumentu; opcjonalny `warehouse_id` (walidacja zgodności). Front `WmsReturnsPage`: błąd wczytania bez `navigate` na hub; komunikat + „Spróbuj ponownie” + link do listy.

- **WMS Zbieranie — hub statusów jak Pakowanie:** `/wms/picking` bez belki „Zamówień / pastylki” i bez nagłówków; duże kafle `variant="work"` (min. wysokość, ikony cart/basket/picking 30px). Po wyborze statusu: jednorazowy `getWmsPickingProductLines` + `hubOrderCount` / `hubPickStats` w sesji; belka jak lista pakowania (`WmsPickingSessionTopBar`) na typ zamówień, skan wózka i liście produktów; lista produktów liczniki z wierszy lub snapshotu przy ładowaniu.

- **WMS RMZ — tworzenie zwrotu (POST /api/wms/returns):** `create_wms_return` i `GET .../orders/{id}/returns` używają magazynu z wiersza zamówienia (nie domyślnego tenanta), zgodnie z lookupiem „13” po całym tenancie — usuwa fałszywy 404 „Order not found”. Opcjonalne `body.warehouse_id` musi zgadzać się z magazynem zamówienia (400). Front: zamknięcie panelu po sukcesie, komunikat błędu z `detail`, linie z „W zamów.: 0” wyłączone z edycji + focus pierwszej linii z qty > 0.

- **WMS zbieranie — finalize po braku:** `compute_line_missing_qty` / `recompute_order_fulfillment` przy opcjonalnym `session_cart_id` liczą luz jak `finalize-cart` (PICK tylko z wózka), nie `line_picked_sum_for_order` (inne wózki / legacy). `finalize_wms_picking_cart` i recovery: `recompute` + `flush` przed bramką produktową; log `wms finalize-cart line gate` przed `_classify_order_after_picking_session`. Zgłoszenie braku: `FE_MISSING` / `wms_shortage_declared_qty` wg `min(rem_line, missing_qty)` (FIFO budżetem z body).

- **WMS zbieranie — brak (UI):** Po `report-shortage` natychmiastowy stan: optymistyczna aktualizacja `detail`, potem `load()` z ochroną wyścigu (`detailLoadSeqRef`). Powrót na listę tylko gdy na SKU nie ma już `remaining_to_pick` ani `shortage_declarable_total`; inaczej użytkownik zostaje na karcie (czerwony blok postępu). Lista produktów: `productLinesLoadSeqRef` przy `load()`, normalizacja `picked ≤ total`, licznik „Braki: N linii”, karta z brakiem zawsze w stylu rose, kolumna lokacji „— / Brak”. `wmsPickingDisplayPickedQuantity` + poprawione `remaining` / scan eligible / recovery summary.

- **OMS — tabela „Zamówione produkty” / historia zmian:** Przywrócony układ archiwum po zamianie (szary blok, przekreślenie, `ZAMIENIONO NA →`, nowy produkt z ceną/ilością). Linie qty=0 bez REPLACED: „USUNIĘTO…” + snapshot z `panel_fulfillment_history` (ilość, cena, suma). API `OrderItemRead`: `total_price`, `oms_replacement_*` z metadanych. Badge `PRODUKT ZASTĘPCZY` przy `replaced_from_order_item_id`. Rozszerzone `isOrderItemReplacedArchiveRow` (status z zamówienia gdy brak WMS).

- **Panel lista + historia + pakowanie (UI/data):** API listy: `items_display_lines` (aktywne linie), podgląd/tooltip „+N poz.” z pełnej listy (`OrderList.tsx` → `tooltipProducts`). Historia: przy „Pokaż historię zmian” znów widać `REPLACED` i linie qty 0; wpisy usunięcia z snapshotem ceny/ilości (`OrderDetailPage` + `panel_fulfillment_history`). Pakowanie: `CourierBadge` — logo + nazwa w jednym rzędzie, tekst listów przewozowych tylko gdy `>1`; `PackingView` — kolejność boczna: #kolejki, kurier, klient, pobranie/wartość, karton.

- **Braki / pakowanie / historia (krytyczne):** `POST …/items/{id}` `remove_missing` + `DELETE …/items/{id}` dopisują `import_metadata_json.panel_fulfillment_history` (czytane w `OrderRead.panel_fulfillment_history` + sekcja „Historia zmian”). WMS szczegół zadania braków: nagłówek zamówienia/klient/status, lista z `shortage_lines` + fallback z snapshotu `missing_items` (`oms_action_summary`), CTA zbieranie + link `/orders/:id`. Zgłaszanie braku na szczególe SKU: `shortage_declarable_total` z backendu + `cannotReportPickingShortage` (odblokowanie przy `remaining_to_pick=0`), bez `order_ids` w POST (uniknięcie pustej sesji). Pakowanie / karta OMS: z karty wykluczone linie `REPLACED` i `quantity<=0` (`_order_item_active_for_packing`).

- **WMS zbieranie — brak w trakcie sesji vs finalize:** `report_wms_picking_product_shortage` nie zmienia już `order_ui_status_id` ani nie tworzy zadań Braki — tylko zapis linii + audyt `wms_picking_shortage_reports`. Status panelu po finalize: ustawienia **Obsługa braków**, inaczej `target_status_id` (usunięty duplikat „status przy braku” z konfiguratora zbierania). Front: po zgłoszeniu braku powrót na listę + `pickingListRefreshAt` (natychmiastowy refetch), uproszczone kafelki i modal końca zbierania z listą produktów wg zamówień.

- **Panel zamówień — statusy PL + Produkty i magazyn po zamianach:** `orderListSystemStatusLabel` + lista (`OrderList`), legacy `Orders.tsx` / `layout/Orders.tsx` — brak surowych `NEW` / `DONE` w UI. Zakładka Produkty: blok „Historia zmian (N)” + rozwijana lista „Zamiany w zamówieniu”, checkbox „Pokaż historię zmian” (wiersze qty 0), wiersze REPLACED w trybie historii (przekreślony + „Zamieniono na →”), zamiennik: niebieski badge „Produkt zastępczy” + „Zamiast: …”, kompletacja przez `resolveOmsFulfillmentCompletionBadge`.

- **WMS — obsługa braków (ustawienia) + dogrywka recovery:** Ustawienia → WMS → Zbieranie → „Obsługa braków i wyjątków”: status OMS po zgłoszeniu braku, auto kolejka Braki, blokada innych linii po braku, priorytet po rozwiązaniu, auto-reopen (zapis), status po domknięciu dogrywki. API `GET/POST /wms/settings/picking-shortage`. Lista/detail zbierania: `allow_continue_other_lines_after_shortage` z API. Trasa `/wms/picking/recovery/:orderId` (ten sam ekran co produkty) + `POST /wms/picking/recovery/finalize`; nagłówek dogrywki, quick-pick z `recovery_order_id`, poprawka `order_ids` w `build_wms_picking_product_detail` przy `fixed_order_ids`. Braki: `braki_queue_bucket` w UI, sekcje kolejki, „Przejdź do zbierania” na szczególe gdy recovery_ready / pending replacement.

- **WMS — ustawienia braków (UX):** `PickingShortageSettingsPanel` — copy PL bez żargonu OMS/picker, krótkie opisy pod polami; błąd wczytania tylko gdy pierwsze pobranie się nie udało; domyślny status po dogrywce z `start_status_id` pakowania gdy w DB null; domyślne wartości biznesowe: priorytet `high`, auto-reopen włączony (model + schema + API fallback).

- **WMS Braki — architektura kolejki + OMS:** Kanoniczny URL `/wms/braki` (+ `?order_id=` → auto `/wms/issues/task/:id`). Backend: przed listą/skanem `sync_open_issue_tasks_for_warehouse` + pełny snapshot `build_full_issue_payload_for_order` (wszystkie linie z `compute_line_missing_qty` > 0); lista filtruje karty bez operacyjnej pracy; `last_shortage_at` w API. Finalizacja wózka: `cohort_shortage_order_ids` → deep link z modala. OMS: kompaktowy pasek „BRAKI: …” + WMS / Rozwiąż wszystko; `wms:shortages-updated` odświeża zamówienie, fulfillment, kolejkę, listę zbierania. Badge/trace zamiany: „Zamieniono → …”, „Dodano jako zamiennik za produkt …”; pole `replacement_new_product_name` na linii WMS.

- **WMS zbieranie — regresje braków:** `WmsPickingProductDetailPage` — zgłoszenie braku wg `remaining` + `cart_id` (nie od pustej listy `orders` przy braku `cart_id` w detail); `order_ids` tylko gdy lista niepusta. Lista produktów — badge „Braki” / „Brak a/b”, nagłówek `polishSkuWithShortagesLabel`; po `finalize-cart` modal „Zamówienie zawiera braki” gdy API zwraca `cohort_shortage_product_count` / `cohort_shortage_unit_total` > 0 (przyciski: Braki / Zakończ / Zamknij). Backend: `cohort_shortage_stats_from_orders` + pola w `WmsPickingFinalizeCartResponse`. Testy: `wmsPickingUiGates.test.ts` (vitest), `test_wms_picking_shortage_stats.py` (pytest).

- **OMS — kompletacja vs braki WMS:** Jedna funkcja `resolveOmsFulfillmentLineBadge` + `omsFulfillmentRowToneClass` (`components/orders/omsFulfillmentLinePresentation.ts`) — **brak (`missing_quantity`) przed** `TO_PICK` / „oczekuje”. `OrderDetailPage` tabela: czerwone wiersze, podpis `0/1 zebrano · Brak: 1`, akcje inline (`OrderFulfillmentLineShortageInlineActions`) + odświeżenie `GET /orders` + `wms-fulfillment` po PATCH; kotwica `#wms-braki-sekcja`.

- **WMS zbieranie — SKU po pełnym braku w kolejce:** `_scanner_active_by_product_id` nie wyłącza już produktu po `wms_picking_line_status=missing` z pełnym `picked+missing>=qty` (skan EAN znów otwiera kartę). Frontend: `wmsPickingRowScanEligible`, poprawka fallbacku `lineComplete` (brak w sumie `picked+missing`). Szczegół produktu: blok akcji po pełnym braku (Dalej / Edytuj brak → Braki / Zbierz jednak z komunikatem o OMS).

- **OMS/WMS zamiana przy braku:** `GET …/wms-fulfillment` — `oms_line_status`, `oms_line_secondary_trace` (audyt: „Brak zgłoszony w WMS → zamieniono na: …” / „Dodano jako zamiennik za: …”). Po zamianie: `oms_replacement.new_product_name` w metadanych starej linii; nowa linia `metadata_json` z `oms_substitute_*`; przy pełnym przeniesieniu `quantity=0` na linii `REPLACED`. Kolejka: `order_item_needs_substitute_pick_completion` (linia z `replaced_from_order_item_id` lub `TO_PICK`, pick < qty) — nie znika po częściowym picku; `_refresh…` czyści `TO_PICK` dopiero po pełnym zbieraniu; `_classify_order_after_picking_session` domyka linie `REPLACED` z `quantity>0`. `_clear_fulfillment_shortage_state_if_resolved` czeka na koniec zbiórki zamiennika. API zadań: `substitute_product_id` / `substitute_product_name`. UI: badge „Zamieniono po braku” / „Do zebrania (zamiennik)”, karta kolejki „Klient: …”, „Zamiennik po braku: …”, CTA „Otwórz zbieranie” → `/wms/picking/products/:id` gdy znany produkt.

## 2026-04-16

- **Fulfillment ledger (`fulfillment_events`):** typy PICK | MISSING | REPLACED | REMOVED | WAITING; `order_item_id`, `quantity`, `metadata_json`, timestamps. PICK tworzone przy `Pick` WMS + `finalize` ustawia `finalized`; MISSING przy zgłoszeniu braku; REPLACED przy zamianie OMS; WAITING przy „czeka na towar”. Agregaty WMS/OMS z sum zdarzeń (nie z tabeli `picks`); `picked_quantity`/`missing_quantity` w API = wyliczone. Migracja SQLite `ensure_fulfillment_events_table` + backfill z Pick/kolumn.

- **OMS — akcje na braku per ilość:** `PATCH .../items/{id}` — `replace_product_id` przenosi tylko `min(ceil(missing), max_transfer)` (missing z `compute_line_missing_qty`); `remove_missing: true` zmniejsza `quantity` o tę samą logikę (skaluje `total_price`); `waiting_for_stock: true` zapisuje `metadata_json.oms_waiting_missing_qty`; `false` czyści flagę i snapshot. UI: „Usuń brak z zamówienia” zamiast `DELETE` całej linii. `OrderItemRead.oms_waiting_missing_qty`.

- **OMS zamiana produktu (split linii):** `PATCH .../items/{id}` z `replace_product_id` — stara linia `oms_line_status=REPLACED`, `quantity` = zaokrąglone w górę pobrania (Pick zostają), braki wyzerowane; nowa linia z brakującą ilością, `oms_line_status=TO_PICK`, `wms_picking_line_status=to_pick`, `replaced_from_*`; `touch_picking_in_progress`. Agregaty zbierania / routing pomijają `REPLACED`. Migracja: `oms_line_status` (`ensure_order_items_oms_line_status`).

- **OMS/WMS — braki (jedno źródło prawdy):** `wms_shortage_declared_qty` = zgłoszenie z WMS; `wms_picking_line_missing_qty` = wynik `recompute_order_fulfillment` (`compute_line_missing_qty`: brak tylko przy zadeklarowanym shortage + korekty OMS). Po `report-shortage`, `PATCH/DELETE` linii, „czeka na towar”: recompute; zamknięcie OPEN `order_issue_tasks` przy zerowych brakach; `NEEDS_DECISION`/`MISSING` → `PICKING` gdy braki wyzerowane. `GET /orders/{id}/wms-fulfillment` i serializacja kolejki Braków wołają recompute; karta linii: `shortage_display_kind`, ślad zamiany (`replaced_from_*`). Migracja: `ensure_order_items_fulfillment_sync_columns` w `main.py`.

- **OMS — sekcja „Braki z WMS”:** Zakładka Produkty i magazyn — agregacja braków wg `product_id` z `GET /orders/{id}/wms-fulfillment`, podsumowanie, badge „Braki (X)” przy nagłówku listy produktów; akcje na linii: zamiana produktu (`PATCH /orders/{id}/items/{item_id}`), usunięcie (`DELETE`), „czeka na towar” (metadata `oms_waiting_for_stock`). API: `product_id` na linii karty WMS; `OrderItemRead.oms_waiting_for_stock`.

- **WMS Braki — szczegóły:** `/wms/issues/task/:id` — tylko numer zamówienia, badge „Czeka na decyzję”, lista braków; usunięto blok informacyjny i link do OMS.

- **WMS Braki — kolejka operacyjna:** `/wms/issues` — karta = jedno zamówienie: liczba produktów z brakiem + status „Czeka na decyzję”; `/wms/issues/task/:id` — tylko lista braków + link do panelu OMS; usunięto ekran decyzyjny (pakowanie/zbieranie/MM/usuń z listy). `WmsOrderIssueDecisionPage` zastąpione przez `WmsOrderIssueDetailPage`.

- **OMS — wskaźniki braków WMS:** Lista zamówień (`GET /orders/`) zwraca `wms_missing_line_count` (liczba linii z `wms_picking_line_missing_qty` > 0); `OrderList.tsx` / `PanelListRow` — badge „Braki” / „Braki (n)” (czerwony). Szczegóły zamówienia — badge kompletacji dla linii z brakiem: `Brak: {missing}/{ordered}` na czerwono (bez amber dla braku).

## 2026-04-15

- **WMS Braki — ekran decyzyjny:** `/wms/issues` — hub (skan / numer / duże przyciski kolejki, bez tabeli); `/wms/issues/task/:taskId` — jeden ekran wg `ui_decision` (anulowane → Rozlokuj, brak braków → pakowanie, nowe linie → SKU+lokacja + Zbieraj, wszystko brak → usuń z listy, częściowo → Rozlokuj / Czekaj). API: `GET /wms/order-issue-tasks/resolve-scan`, `GET .../{id}`, pola `ui_decision`, `new_product_lines`.

- **WMS Order Issues (Braki) — picking UI:** `WmsPickingProductsPage` / `WmsPickingProductDetailPage` — ukończenie linii i „do zebrania” wg `remaining_to_pick` (fallback: `total − picked`), żeby po zmianie `required_qty` nie resetować postępu ani nie czyścić `picked_qty`.

- **Produkty — UI pełnoekranowe:** Trasy `/products/new`, `/products/:id/edit` bez modala (`ProductEditModal` `variant="page"` + `PageLayout`); po zapisie/anulowanie → `/products`. Lista: `PanelPageContentBleed`, filtry w karcie ze zwijaniem + `localStorage` `products.list.filtersExpanded` (domyślnie otwarte), siatka `md:grid-cols-2` / `lg:grid-cols-4`, większe odstępy wierszy tabeli. Współdzielony mapper `productListMapper.ts`.

- **WMS quick-pick — auto magazyn:** Query `warehouse_id` opcjonalne; brak / nieprawidłowe → `resolve_quick_pick_warehouse_for_tenant` z `tenant_warehouses` (1 magazyn albo `is_default`, wiele bez domyślnego → 400 „Brak domyślnego magazynu”); `logger.info("Auto-selected warehouse_id=%s", ...)`. Frontend `postWmsPickingQuickPick` może pominąć `warehouse_id` w parametrach.

- **WMS quick-pick — tenant / magazyn:** Backend `post_picking_quick_pick`: log `tenant_id` / `warehouse_id`; dostęp przez `WarehouseService.can_tenant_access_warehouse` (`tenant_warehouses` + legacy `warehouses.tenant_id`) zamiast wyłącznie `Warehouse.tenant_id == tenant_id`. Frontend: `resolveWmsPickingTenantId` + `WmsPickingProductDetailPage` — tenant z kontekstu koszyka gdy zgadza się z magazynem.

- **WMS quick-pick (frontend):** `postWmsPickingQuickPick` — walidacja `tenant_id`, `warehouse_id`, `source_status_id`, ciała JSON (`product_id`, `location_id`, `quantity`, `cart_id`); query `order_type`; nagłówek `Content-Type: application/json`; przy 422 `console.error` z `response.data.detail`; `formatFastApiErrorDetail` dla UI. `WmsPickingProductDetailPage.confirm_pick` — sprawdzenie `cart_id`, `product_id`, `location_id` na liście lokalizacji produktu.

- **Backend — uruchomienie:** `backend/main.py` — przed importami: `RuntimeError` przy `python backend/main.py` (`__package__ is None`); na końcu pliku ten sam komunikat przy `__name__ == "__main__"` (np. `python -m backend.main`); uruchamianie wyłącznie `python -m backend` (`backend/__main__.py` → uvicorn). Start: `print("Backend started OK", flush=True)` w `@app.on_event("startup")`.

- **CSV etykiety — floor_sets bez JSON:** `LabelPrintQueue` — zestawy pięter jako chipy (`[ E F G ]` + `+`); Enter dodaje tokeny z pola (lub wiele po spacji/przecinku); pusty Enter / `+` zapisuje bieżący nawias jako zestaw; `sanitizeFloorSetsMatrix` w `csvGroupingPreview.ts`; `parseCsvFloorSets` zostaje dla ewentualnego importu tekstowego.

## 2026-04-14

- **CSV etykiety — szablon vs grupowanie:** `labelCsvImport` — `validateCsvGroupingTemplateMode` / `csvGroupingPdfBlockedByTemplate` (szablon „pojedynczy” z `{floor}` / `{barcode}` / `{loc_name}` / `{loc_barcode}` bez slotów `_1…` przy włączonym grupowaniu → ostrzeżenie „Ten szablon nie obsługuje grupowania etykiet”; pierwotnie `LabelPrintQueue` blokował „Generuj PDF” + wczesny return w `handleCsvGeneratePdf` (**zastąpione 2026-04-15** — patrz niżej).

- **CSV etykiety — UX grupowania vs szablon (2026-04-15):** `LabelPrintQueue` — brak blokady „Generuj PDF”; przy `group_mode` + niekompatybilnym szablonie: komunikat „Szablon nie obsługuje grupowania — etykiety zostaną wygenerowane pojedynczo”, przycisk „Dostosuj szablon” → `/labels/designer/:id`; `POST /labels/render-pdf` bez `group_mode` / `floor_sets` w tym przypadku; podgląd grupowania ukryty gdy merge nie jest wysyłany.

- **CSV etykiety — mapowanie:** `labelCsvImport` — `floor_1…3` / `barcode_1…3` / `loc_name_1…3` poza listą mapowania (`allSuggestedLabelFieldsForCsvMapping`), czyszczenie mapy i localStorage; ostrzeżenie przy szablonie ze zmiennymi grupowymi bez `group_mode`; `validateCsvAgainstTemplate` nie wymaga CSV dla pól z merge.

- **Etykiety — inspektor / paleta (tryb zgrupowany):** `VariableInspectorPanel` — „Podgląd danych” + „Zmienne do etykiety”; paleta „Magazyn” z „Dane wspólne” / „Elementy etykiety” (bez „Zmienne grupowe”); `locationGroupedVariables.ts` — `formatGroupedLocationDataPreview`, `partitionGroupedWarehouseItems`, strzałki slotów; `LabelTemplateDesigner` przekazuje `previewRecord` do inspektora.

- **Etykiety — podgląd grupowania CSV:** `frontend/src/utils/labels/csvGroupingPreview.ts` — ta sama logika co backend (`floor_sets` / merge po `row`); `LabelPrintQueue` — blok „Podgląd:” pod konfiguracją `floor_sets` (max 10 linii + licznik etykiet).

- **Etykiety — floor_sets (CSV → PDF):** `merge_records_by_floor_sets` + `normalize_floor_sets_param` w `label_record_grouping.py` — grupa po `(rack?, row, floor_set_id)`; rekord ma `set`, `items`, `floor_set_id`, `floor_set`, sloty `floor_1..3` wg kolejności zestawu (brakujące piętra = `null` w slotach i częściowe `items`); piętra spoza zestawów → osobna strona (`set` puste). `POST /labels/render-pdf`: pole `floor_sets`; gdy `group_mode` i lista pusta — nadal `merge_records_by_row_multi_slot`. `LabelPrintQueue`: opcjonalny JSON w CSV.

- **Etykiety — grupowanie CSV → PDF:** `label_record_grouping.merge_records_by_row_multi_slot` — po `_normalize_record_for_bindings`; `group_mode` + `group_by_rack` w `POST /labels/render-pdf`; brakujące sloty `floor_2/3`, `barcode_2/3` = `null`; bez zmian importu CSV ani filtrów (grupowanie tylko w `render_label_template` gdy `group_mode`). UI: tylko tryb CSV w `LabelPrintQueue`.

- **Etykiety — ulubione kolory:** `ColorPicker.tsx` — `localStorage` `label.favoriteColors` (max 48), sekcja „Ulubione kolory” nad paletą sesji; próbka = `commit`, `+` = zapis bieżącego koloru, × usuwa z ulubionych (jak przy palecie).

- **Etykiety — UI drukarni:** `LabelPrintQueue.tsx` — checkbox „Do drukarni…” nad przyciskiem PDF w trybach z backendowym PDF (lokalizacje, CSV, rack, strip, import PDF); stan `pdfPrintReady` (osobno od `printMode` zakładki); `params.print_mode` bez zmian w logice requestów.

- **Etykiety — `print_mode` na API:** `POST /labels/render-pdf` ma query `print_mode` (domyślnie `false`). `render_label_template` → `build_label_pdf_multi(..., print_mode=...)`: przy `false` bleed 0, bez crop marks, kolory RGB (`HexColor`); przy `true` bleed `LABEL_PDF_BLEED_MM`, crop marks, CMYK. Zachowanie bez parametru jak wcześniejszy tryb „ekranowy”.

## 2026-04-13

- **Etykiety — PDF pod druk (minimalnie):** `hex_to_cmyk` w `label_engine.py` (hex `#rgb`/`#rrggbb` → `CMYKColor`); rysowanie etykiet przez `setFillColor`/`setStrokeColor` z CMYK zamiast RGB/`HexColor`; kod kreskowy — czysta czerń CMYK (`hex_to_cmyk('#000000')`) przed `drawOn`/`drawImage` bez zmiany geometrii. `build_label_pdf_multi`: opcjonalne znaki cięcia `DRAW_CROP_MARKS` (domyślnie True) + `_draw_crop_marks_for_label_pdf` przed `render_label_to_canvas_engine` na każdej stronie. Legacy `label_render_service`: CMYK w `_apply_element_color`, prostokątach, QR/Code128.

- **Etykiety — debug `build_label_pdf_multi`:** `logger.warning` z serializowanym szablonem (JSON), `WIDTH_MM`/`HEIGHT_MM`, `WIDTH_PT`/`HEIGHT_PT` (`reportlab.lib.units.mm`), `CANVAS_PAGESIZE` po utworzeniu Canvas, po `save()` odczyt pierwszej strony przez **PyPDF2** `PdfReader` → `PDF_MEDIABOX`; zależność `PyPDF2>=3` w `backend/requirements.txt`.

- **Etykiety — PDF canvas (układ współrzędnych):** silnik etykiet (`render_label_to_canvas_engine`) bez globalnego `translate`/`scale` na stronie; mapowanie mm (origin lewy-górny szablonu) → pt (PDF, lewy-dół) przez `_template_top_rect_to_pdf_pts` (`sx`/`sy`, `y_bottom = label_height_pt - y_mm*sy - h_mm*sy` + offsety w pt). Tekst: `_text_baseline_y_in_box` + `drawString` z dodatnim `setFont`. Code128: wyśrodkowanie w poziomie + `clipPath` gdy za szeroki (bez `scale` na pasku). Obrót elementu: jedno `canvas.transform` (bez `translate`/`rotate`). Trójkąt/strzałek/wielokąt w PDF y-up; wielokąt: `y` punktów od góry ramki → `y_pt + h_pt - py`.

- **CSV etykiety — wymiary szablonu:** `sanitizeTemplateJsonDimensionsForCsvExport` w `labelCsvImport.ts` (ostrzeżenie przy >200 mm; zamiana strony typu A4 w mm na 100×60 przed `POST /labels/render-pdf`). `LabelPrintQueue` — ostrzeżenie pod wyborem szablonu + wysyłka poprawionego `template_json`. Backend `render_label_template` — `_warn_and_clamp_a4_like_page_mm` (te same heurystyki).

- **Backend PDF — brak symbolu A4 w kodzie Python:** etykiety (`label_engine`, `label_render_service`, `barcode_pdf_service`, stub etykiety przesyłki w `complaint_shipment`) zawsze `canvas.Canvas(..., pagesize=(w_pt,h_pt))` z mm; `assert` + `print("FINAL PDF SIZE:", …)` na ścieżkach etykiet; `label_pdf_generation_log` — wykrywanie „office default sheet” bez nazwy A4. Dokumenty magazynowe / PDF reklamacji: `SimpleDocTemplate(..., pagesize=(210*mm, 297*mm))` zamiast importu `A4`.

- **PDF — druk z przeglądarki:** `openPdfForBrowserPrint.ts` — nowa karta z HTML (`@page { size: auto; margin: 0 }`, ramka `embed` na PDF) + podpowiedź druku (100%, bez „fit to page”, marginesy None). Użyte przy: etykiety produkt/wózek/zestaw, zwrot `returnLabelPrintApi`, dokumenty reklamacyjne (druk), PDF zamówień do dostawcy, PDF dokumentów magazynowych; `LabelPrintQueue` — krótki opis w UI.

- **Etykiety — CSV PDF:** `POST /labels/render-pdf` przyjmuje opcjonalne `template_json` (nadpisuje DB). `render_label_template` używa `build_label_pdf_multi`; bez `template_json` (ani w body, ani w wierszu) → 400 z komunikatem „Template required for CSV labels…”. `LabelPrintQueue` (import CSV) zawsze wysyła `template_json` z listy `/label-templates`, przycisk PDF wyłączony gdy brak poprawnego JSON, obsługa błędu 400 z treścią `detail`.

- **Etykiety — debug PDF flow:** `label_pdf_generation_log.log_label_pdf_flow` (`[label-pdf-flow]` + `print`) — SERVICE (`label_render_service` / `barcode_pdf_service` / `cart_service`), `template_id`, `template_json` (obcięty), `width_mm`/`height_mm`, `detail`. `POST /labels/render-pdf` loguje wejście do `label_render_service` (CSV nie idzie przez barcode). `print_pdf_canvas_size` → `print("PDF SIZE:", canvas._pagesize)` w `label_engine`, `build_label_pdf_multi`, `build_barcodes_pdf`.

- **Etykiety — backend sanitize rekordów:** `sanitize_label_record_keys` w `render_label_template` przed merge (klucze w stylu szablonu z klamrami → nazwa bez klamer, rekurencja w listach); `print("SANITIZED RECORD:", …)`. `_normalize_record_for_bindings` / `inject_parsed_location_fields` nie dodają już duplikatów z klamrami; końcówka normalizacji usuwa ewentualne klucze zaczynające się od „{”.

- **Etykiety — CSV / render-pdf payload:** `labelCsvImport` nie tworzy duplikatów kluczy w klamrach (np. rack_name vs „{rack_name}”); `sanitizeRecordsForRenderPdf` usuwa takie klucze także w zagnieżdżonych tablicach (repeater). `LabelPrintQueue` — ta sama sanitacja przy wszystkich body `records` do `POST /labels/render-pdf`; przed CSV wysłaniem `console.log("CLEAN RECORD", record)` dla każdego wiersza.

- **Etykiety — debug PDF:** moduł `label_pdf_generation_log` (`[label-pdf]` w logach): `source`, `template_id`, `template_json_present`, `width_mm`/`height_mm`, `canvas._pagesize` (pt) po utworzeniu Canvas; ostrzeżenie przy rozmiarze jak A4. Wywołania w `render_label_template`, `build_label_pdf`, `build_label_pdf_multi`, `build_label_pdf_engine`, `build_barcodes_pdf`, `cart_service` (ścieżki wózek/koszyk), `generate_pack_pdf`, `get_location_labels_pdf`. Frontend DEV: `generatePdfBlob`, `exportLabelsPdf`.

- **Etykiety — PDF legacy wózek/koszyk:** `barcode_pdf_service.build_barcodes_pdf` nie używa już A4: `pagesize=(width_mm*mm, height_mm*mm)` (domyślnie 100×60 mm), jedna strona na kod, skalowanie Code128 od (0,0). `cart_service` przekazuje mm z domyślnego szablonu cart/basket gdy jest w bazie.

- **Etykiety — wiele CSV:** `LabelPrintQueue` — `multiple` na inpucie plików, `mergeParsedCsvSources` / `dedupeLabelRecordsByRackFloorRow` w `labelCsvImport.ts`, statystyki per plik, limit 5000 wierszy po scaleniu; jedno `POST /labels/render-pdf` (merge w przeglądarce).

- **Etykiety — PDF (fallback klient):** `generatePdfBlob` w `LabelPrintQueue` — strona PDF = rozmiar etykiety (`widthMm`/`heightMm` → pt × 2,83465), jedna etykieta na stronę, bez siatki A4 i marginesów; `drawSvgVector` — jednostki zgodne z `jsPDF` (dokumentacja).

- **Etykiety — PDF rozmiar strony:** `labelPdfPageSetup` (`jsPdfOrientationForLabelShape` — jsPDF w portrecie zamienia wymiary gdy szerokość > wysokość); `exportLabelsPdf` + `generatePdfBlob` używają helpera. Backend: `build_label_pdf_engine` / `build_label_pdf_multi` — brak `translate`/`scale` z kalibracji (strona = dokładnie mm szablonu); klient bez `applyCalibration` na SVG przy eksporcie etykiet.

- **Etykiety — SVG na PDF bez letterboxu:** `svgRenderer` — `preserveAspectRatio="none"` + `width`/`height` w mm na roocie SVG, żeby rysunek wypełniał stronę etykiety (wcześniej `meet` dawało pustą przestrzeń / „mała etykieta w rogu” przy eksporcie PDF).

- **Szablony etykiet — zapis:** `LabelTemplateDesigner` buduje `template_json` jako string z kanonicznymi `widthMm`/`heightMm`/`elements`, duplikat `width`/`height` (mm), bez pustego `group_id` przy `POST`; przy `PUT` zawsze `group_id` (w tym `null`). Log `SENDING TEMPLATE`. `LabelToolbar` przy zmianie typu etykiety zeruje `group_id` (unik 400 z walidacji grupy vs `template_type`).

- **Etykiety lokalizacji — parsowanie kodu:** `parseLocation` / `parse_location` (np. `A1-C-6` → regał `A1`, piętro `C`, rząd `6`; ≥4 segmenty: rack = wszystko przed dwoma ostatnimi). Wstrzyknięcie `floor`, `row`, `rack_name` oraz `{floor}`, `{row}`, `{rack_name}` w `labelLayoutEngine` (root + repeater), `label_engine.compute_layout` + sloty repeatera, `_normalize_record_for_bindings`; zmienne w palecie Magazyn + próbka podglądu `A1-C-6`.

- **PDF etykiet — fonty + Code128:** `backend/assets/fonts` — DejaVu Sans/Bold (TTF); `pdf_fonts.register_pdf_fonts()` przy imporcie modułu. Code128: `_draw_code128_fitted` z jawnym `barHeight` = wysokość komórki (pt) i wyszukiwaniem `barWidth`, wyśrodkowanie w poziomie; skala jednolita tylko gdy kod nie mieści się przy minimalnym pasku. `scaleToHeight` w `label_engine` używa `h_mm * POINTS_PER_MM * 0.7` (punkty); frontend `labelLayoutEngine` — ten sam wzór + `measureTextWidth` z rozmiarem czcionki w pt → px (96 dpi).

- **Etykiety lokalizacji — wykluczenie pięter:** `exclude_floors: string[]` w `POST /labels/render-pdf` (`RenderPdfBody`) oraz `GET /warehouse/layout/labels?exclude_floors=…` (powtarzalny query); `record_effective_floor` + filtrowanie przed `render_label_template` (`apply_label_filters`). **LabelPrintQueue:** UI filtra pięter + `exclude_floors` w body, podgląd po stronie klienta (`locationRecordsFiltered` / CSV).

- **ColorPicker — paleta niestandardowa:** usuwanie koloru (× na hover, `removePaletteColor` po indeksie); tooltip / podpowiedź; na urządzeniach bez hover (sm+) przycisk × widoczny zawsze, na desktopie ukryty do hover lub `focus-within` (klawiatura).

- **Filtr pięter — UI:** `FloorExclusionPanel` w `LabelPrintQueue` (lokalizacje z magazynu + import CSV): wpisywanie liter (przecinki / ciąg), chipy z usuwaniem, tryb wyklucz vs tylko wybrane, presety (wszystkie / wyklucz A–F / tylko wybrane), zapis nazwany w `localStorage` (`label_print_saved_floor_filters_v1`, wczytaj/usuń). Stan `floorFilterUi` → `excludeFloors` przez `excludeFloorsFromUiState` (API `exclude_floors` bez zmian).

## 2026-04-12

- **Seed pustej bazy:** `backend/db/seed_basic_data.py` — tenant `id=1`, magazyn „Magazyn główny” + `tenant_warehouses`, trzy statusy panelu zamówień (`OrderUiStatus`: Nowe / W toku / Zakończone); wywołanie na końcu `upgrade_schema` (startup).

- **SQLite:** `database.create_all_tables()` + startup `ensure_sqlite_tables(announce=True)` z logiem `Creating database tables...` i ponownym `from . import models` przed `create_all` (przy usuniętym `test.db` tabele odtwarzają się przy starcie aplikacji).

- **WMS pakowanie — po zakończeniu:** ustawienie `packing_after_finish_action` (`STAY` | `GO_TO_LIST`) w `wms_packing_settings` + panel Ustawienia WMS → Pakowanie; odpowiedź `POST …/finish` zawiera `packing_after_finish_action`; `usePackingOrderController` przy `GO_TO_LIST` nawiguje na `/wms/packing/orders?status={statusId}` bez dalszego `setDetail`.

- **Zamówienie — mapowanie z klienta:** `CreateOrderPage` — firma i NIP do pól zamówienia + API (`company_name`, `nip` → `billing` w `addresses_json`), adres rozliczeniowy z domyślnego adresu karty, dostawa z wybranego adresu przy wielu wpisach; `OrderCreateBody` / `create_order` + `OrderPatchBody` / `_patch_order_billing_identity` (firma/NIP); bez wypełniania `note`/`comment` danymi firmy.

- **WMS finish pakowania:** `POST /wms/packing/orders/{id}/finish` — potok najpierw ustawia status panelu (spakowane), potem `create_sale_document` (twarde `ValueError` przy włączonym „Utwórz dokument” i braku serii / błędzie zapisu); logi `PACKING_SETTINGS`, `ORDER_PANEL_DOCUMENT_TYPE`, `SELECTED_SERIES`, `DOCUMENT_CREATED`.

- **Klienci (moduł):** modele `Customer`, `CustomerAddress`, `CustomerProductDiscount`; API `GET/POST/PATCH/DELETE /customers`, adresy `GET/POST …/addresses`; kolumna `orders.customer_id`; nawigacja **Klienci** (lista +) + **Dostawcy** przeniesione z Asortymentu; UI lista/edycja (`/customers`, `/customers/new`, `/customers/:id`); wybór klienta na **Nowe zamówienie** z potwierdzeniem przy nadpisaniu danych; `country_code` + select z flagami.

- **Serie dokumentów — jeden GET + pakowanie:** `listDocumentSeries` zawsze bez `?type=`; filtry SALE/pakowanie (`filterSaleSeriesForPacking`); `GET /wms/settings/packing` wymaga jawnego `warehouse_id` (wcześniej zależny od magazynu domyślnego); zapis pakowania `PATCH /wms/settings/packing` (POST nadal działa); walidacja istnienia serii INVOICE/RECEIPT przy zapisie; tworzenie dokumentu po pakowaniu tylko z `invoice_series_id` / `receipt_series_id` wg `panel_document_type` (bez `series_id`); UI pakowania bez „domyślnej serii”, po zapisie ponowny fetch listy serii.

- **Ustawienia — fly-out:** usunięto linki „Zwroty — statusy panelu” i „Zamówienia — statusy panelu” z sekcji Ustawienia (`mainNavConfig.tsx`); trasy `/settings/returns/ui-statuses` i `/settings/orders/ui-statuses` pozostają (wejście z modułów Zamówienia/Zwroty).

- **Lista zamówień — szybkie akcje vs multiakcje:** ikony w pasku zaznaczenia otwierają osobne proste modale (status, dokument, etykieta/wiadomość — stub „wkrótce”, notatka); przycisk **Multiakcje** wyłącznie otwiera `OrderBulkMultiActionModal` (bez bootstrap z ikon); ikona dokumentu = `Printer`.

- **Serie dokumentów — trwałość po odświeżeniu:** domyślny kontekst listy `{ type: null }` (nie wymuszać `SALE` w `GET`); lista pobiera wszystkie serie, widoczne wiersze filtrowane po `type`/`subtype` z kontekstu; zapis kontekstu również w `localStorage`; nagłówki `Cache-Control` / `Pragma` na `GET document-series/`; formularz „nowa” używa `ctx.type ?? "SALE"`; logi DEV `CREATED:` / `FETCHED:`.

- **Dokumenty — lewy sidebar:** tylko sekcja zgodna z aktywną zakładką sprzedaż / korekty / magazyn; pozostałe trasy zakładek pokazują wyłącznie powiązane linki; `getCurrentDocumentModule` + `getDocumentsSidebarSections` w `documentsNavModule.ts`.

- **Lista zamówień — filtry i multiakcje:** rozwijane filtry nad listą z zapisem stanu w `localStorage`; zapytanie `GET /orders/` rozszerzone o daty, źródło, wartość, metodę dostawy, wiele statusów panelu, płatność/dokument; pasek zaznaczenia z ikonami i modalem multiakcji sekcje ↑↓/usuń; wykonanie sekwencyjne per zamówienie; `PATCH` zamówienia: `internal_note_append` w metadanych.

- **Serie dokumentów — UI po polsku:** etykiety typu i podtypu bez angielskich enumów i bez nawiasów pomocniczych; kolumny listy po polsku; VAT — pole `vat_rate_percent` zapis i edycja w formularzu; numeracja — nazwy Ciągła / Miesięczna / Roczna z przykładem numeru, szczegóły formatu w rozwinięciu; po zapisie tworzenia/edycji `rememberDocumentsSeriesListContext` wg zapisanej serii; kontekst listy normalizuje podtyp do dozwolonych wartości.

- **Serie dokumentów — UI kontekstowy:** lista bez nagłówka / filtra globalnego; filtr `type` z `GET /document-series?type=` wg ostatniej ścieżki modułu Dokumenty (`sessionStorage` + `parseDocumentsPathForSeriesContext` w `DocumentsLayout`); podtyp (Faktury/Paragony/PZ/WZ…) dopasowany po stronie klienta; formularz edycji bez bloku tytułu i linku „Lista serii”.

- **Serie dokumentów — tylko moduł Dokumenty:** trasy `/documents/series`, `/documents/series/new`, `/documents/series/:id` (komponenty w `pages/documents/`); usunięto z Ustawienia (fly-out) + przekierowania ze starych `/settings/document-series*`; nawigacja boczna Dokumenty: Serie, Szablony dokumentów (placeholder), Pola własne (placeholder); WMS pakowanie — link do tej samej listy; zamówienie — skrót „Dokumenty → serie”; API bez zmian (`document-series/`).

- **Serie dokumentów — formularz create/edit (Sellasist-like):** kolumny „Ustawienia serii” / „Dane firmy”; `print_template_id` (presety) + ścieżka niestandardowa; `vat_calc_shipping` / `vat_calc_payment`; adres firmy rozbity (`company_street`, `company_house_number`, `company_apartment_number`, `company_regon`); numeracja: preset ciągła / miesięczna / roczna → `numbering_format` + `reset_each_period`; przycisk „Wczytaj z profilu firmy” (`GET /tenants/{id}/` — `TenantRead` rozszerzony o pola profilu); zapis stały na dole po prawej. Migracja SQLite: `ensure_document_series_extended_columns`.

- **Szczegóły zamówienia (panel):** układ 2 kolumn (główna + wysyłka/płatność i podsumowanie); „Adres dostawy” i „Paragon / Faktura” z trybem edycji (ołówek, animacja rozwinięcia, Zapisz/Anuluj); „Wysyłka i płatność” tylko płatność + dostawa; produkty bez zagnieżdżonej wysyłki; modal „Dodaj produkt” (wyszukiwarka `/products/?search=`, dodanie przez `POST /orders/{id}/items/`); `OrderPatchBody` + patch: pola adresu dostawy (`shipping_*` w `addresses_json`).

- **Serie dokumentów (DocumentSeries):** backend `document_series` + CRUD `/document-series` (tenant+magazyn, filtr `type`, bulk-delete); statusy panelu z `order_ui_statuses`; frontend: **Dokumenty → Serie** (`/documents/series`…); zamówienie: `panel_document_series_id` + `PATCH` `document_series_id`; WMS ustawienia pakowania: wybór serii faktury/paragonu z listy `SALE` (podtyp INVOICE / RECEIPT).

- **Panel — szczegół / nowe zamówienie (układ Sellasist-like):** usunięto „Status systemowy”; status panelu przez wspólny `OrderPanelStatusSelect` (jak lista); produkty z akcjami (Dodaj produkt/zestaw, Spakuj → `WMS_ROUTES.packingOrder`), wiersz „Razem (produkty)”; kolumna „Wysyłka i płatność” (typ dokumentu, płatność, status, dostawa, numer FS) + „Zapisz” przez `PATCH /orders/{id}/`; karta „Dopasowane opakowania” z `selected_carton` z API; kupujący edytowalny + zapis (`first_name`… w `addresses_json`); backend: `OrderRead` (`panel_*`, `selected_carton`), `OrderPatchBody` + merge w `import_metadata_json`, `OrderCreateBody` rozszerzone o te pola + `sales_document_number` na zamówieniu. `CreateOrderPage`: ten sam szkielet (sidebar, 4 kafelki, produkty + Wysyłka, dół: etykiety / opakowania / podsumowanie).

- **WMS pakowanie — wybór kartonu:** usunięto „Kartony (słownik)” z lewej kolumny `PackingView`; propozycje z API (`recommended_cartons` + `selected_carton_id` / `selected_carton` na szczególe pakowania); karty klikalne + PATCH `/orders/{id}/select-carton`; mock backend: 2–3 aktywne kartony z magazynu; `AutoActionsView` pokazuje wybrany karton tylko do odczytu; kolumna `orders.selected_carton_id`.

- **Kartony — zapis „nie działał” na `/cartons/new`:** osobna trasa `path="new"` nie ustawiała `useParams().cartonId`, więc `isNew` było fałszywe i zapis nie wywoływał API — jedna trasa `:cartonId` (wartość `new` lub id).
- **Kartony (materiały magazynowe) — zapis:** alias API `POST /api/materials/cartons/` (ten sam router co `/api/cartons/`), frontend `createCarton` → ten URL; payload z liczbami (`weight_kg` 0 gdy puste, walidacja wagi); `console.log` przed create/update; błędy FastAPI (`detail` string / lista) w UI zamiast cichego „Nie udało się zapisać”.

- **Panel zamówień — kolejność podstatusów:** kolumna `order_ui_statuses.is_system`, `POST /api/order-substatuses/reorder` (move up/down lub pełna lista `ordered_ids` tylko dla niestandardowych), reindex `sort_order` po grupie (systemy na początku, potem własne); CRUD: tworzenie z `is_system=false`, usuwanie PATCH sort dla systemowych zablokowane. UI: strzałki góra/dół na `/settings/orders/ui-statuses`, `data-custom-substatus-id` pod przyszły drag.

- **WMS — ustawienia pakowania:** model `wms_packing_settings` (statusy panelu + JSON akcji/dokumentów/etykiety), API `GET/POST /api/wms/settings/packing`, `GET /api/order-statuses`, potok po pełnym spakowaniu w `wms_packing_service` (kolejność: dokument → przesyłka → druk dokumentu → druk etykiety z opóźnieniem → status; kroki warunkowe; wyniki w `post_pack_pipeline` w `WmsPackingScanOut`). UI: zakładka „Pakowanie” w `WmsSettingsPage` (`WmsPackingSettingsPanel`).

- **Panel Zamówienia / Zwroty / Reklamacje — jedna szerokość treści:** wspólny wrapper `PanelPageContentBleed` (anuluje padding poziomy `PageLayout` jak Lista zamówień) w `OrdersLayout` (Zwroty + lista + `/orders/complaints/:id`) i `ComplaintsLayout`; `ReturnStatusesPage` bez `mx-auto max-w-5xl`; sidebar filtrów list ~`17.5rem` (Zamówienia, Zwroty, Reklamacje).

## 2026-04-11

- **Lista zamówień / zwrotów — multi-select i zbiorcza zmiana statusu panelu:** hook `usePanelListBulkSelection` (string `id`, zaznacz wszystkie widoczne, stan `indeterminate`, Shift+zakres); pasek sticky: licznik, „Zmień status”, „Odznacz wszystko”; modal potwierdzenia; toast sukcesu; API `POST /api/orders/bulk-status` i `POST /api/returns/bulk-status` z `{ ids, status }` (`status` pusty = wyczyść etykietę); checkboxy z `checkboxDisabled` przy zapisie. Zwroty: checkboxy w wierszach + `warehouse_id` do summary/patch/bulk.

- **Panel listy (Zamówienia / Zwroty / Reklamacje):** wspólny szkielet `PanelListRowGrid` + `PanelListProductLines` + `ProductThumb` (`frontend/src/components/orders/panelList/`); `PanelListRow` składa zamówienia/zwroty. Zwroty: RMZ, kanał, kwota zwrotu, bez opisu RMZ / czasu względnego / trzeciej ikony. Reklamacje (`ComplaintsPanelPage`): ta sama siatka (`minmax(8rem,12rem)` w kol. 4 zamiast `4.5rem`), hover, typografia; sidebar w karcie ~13.5rem, `py-2.5` na filtrach statusu; checkbox + nagłówek „zaznacz wszystkie”; kolumny: ID → produkty + tagi → status/termin → klient → Eye/Trash; usunięto tekst o filtrze 1:1.

## 2026-04-10

- **Lista zamówień (UI):** pełna szerokość względem `PageLayout` (wrapper w `OrdersLayout`), tło `#f4f7f9`, karty/siatka w stylu WMS (border slate, shadow-sm); sidebar węższy (~13.5rem), bez linku „Metody dostawy…”; wiersz siatką: checkbox+numer+status+panel, produkty z miniaturami 56–64px + nazwa + EAN, klient, **tylko logo kuriera** (`ShippingMethodLogo` `orderList` + `placeholder="package"`), kolumna prawa: kwota pogrubiona, badge płatności, ikony Eye/ExternalLink; usunięto względny czas (`formatRelativeAgo`), trójkąt reklamacji w wierszu, etykietę „Wartość”, tekst „Dostawa —”; `ShippingMethodLogo`: rozmiar `orderList`, opcjonalny `placeholder` truck/package.

- **WMS / Zamówienia — bez osadzonego Dashboard:** usunięto `WmsDashboardPage`, trasę `/wms/dashboard`, zakładkę i `WMS_ROUTES.dashboard`; `/wms` → redirect na `/wms/returns`. Z fly-outu kategorii Zamówienia usunięty link do `/dashboard`; `UI_STRINGS.navigation.dashboard` usunięty (martwy). Globalny `/dashboard` + `wmsDashboardApi` (`/wms/dashboard/summary`) bez zmian dla `Dashboard.tsx`.

- **Dokumenty — nawigacja:** usunięty prawy `<select>` (duplikat zakładek); lewy panel w module (`DocumentsLayout`) + rozbudowany fly-out „Dokumenty” z sekcjami (Sprzedaż: Faktury/Paragony, Korekty, Magazynowe: PZ/WZ/MM, Eksporty/Serie/Szablony/KSeF) z `documentsNavConfig.ts`; typ `NavFlyoutSectionConfig` (`title?` + `items`) w `mainNavConfig` / `NavFlyoutPanel`; trasy zagnieżdżone `/documents/sales/{invoices|receipts}`, `/documents/warehouse/{pz|wz|mm}` (domyślnie faktury / PZ); magazyn: usunięte podwójne zakładki typów w tabeli (filtr z URL).

- **Logo metod dostawy (UI):** wspólny `ShippingMethodLogo` — obraz bez ramki/szarego boxa (max wys. ~40–48px wg wariantu), proporcje zachowane, fallback `Truck` (lucide); ustawienia metod, lista zamówień, szczegół, WMS pakowanie/zbieranie, kartony, `ShippingMethodBadgeRow`.

- **Metody dostawy:** model `ShippingMethod` (UUID, tenant+magazyn, nazwa, logo_url, is_active), `orders.shipping_method_id` FK + migracja z legacy `shipping_method`; API `GET/POST/PUT/DELETE /api/shipping-methods`; zamówienia: lista/szczegóły + `PATCH /orders/{id}/` (metoda), tworzenie z `shipping_method_id`, import zamówień → get_or_create metody. UI: Ustawienia „Metody dostawy”, lista zamówień (logo+nazwa), szczegół/edycja, nowe zamówienie (dropdown), WMS pakowanie/zbieranie (logo+nazwa).

- **Import / metody dostawy:** dopasowanie tylko po **aliasach + kodzie** (nie po pełnej nazwie metody); import zamówień używa **tylko kolumny nazwy przewoźnika** do FK (bez sklejania paczkomatu/punktu); migracja backfill FK bez tworzenia metod z surowego tekstu; czyszczenie „śmieciowych” metod (długie nazwy / Allegro / 24/7 / paczkomat / adres) → zamówienia na OTHER, usunięcie wiersza.

- **Materiały magazynowe (Magazyn):** `cartons` + `carton_shipping_method_links` (M2M → `shipping_methods`) oraz `packaging_materials` (tape/foil/filler, roll/kg/pcs, stock); API `/api/cartons/`, `/api/packaging-materials/` + `PATCH …/stock/`; UI `/warehouse-materials` (zakładki), wpis w fly-out Magazyn; WMS pakowanie: `shipping_method_id` w karcie + podpowiedź kartonów.

- **Kartony — UI (operacyjny, pełna szerokość):** layout modułu bez `max-w-5xl`; trasy zagnieżdżone `cartons` → lista (`CartonsListPage`), `cartons/new` i `cartons/:id` → `CartonDetailPage` z zakładkami (dane / dostawca / magazyn / koszty / metody dostawy); usunięty stary widok modalowy `WarehouseMaterialsCartonsPage`.

- **Karton — szczegóły UI:** układ materiałów magazynowych ukrywa breadcrumb/opis na trasach `/warehouse-materials/cartons/:id` i `.../new`; zakładka dane: zdjęcie (upload), SKU, EAN, aktywny + wymiary; zakładka Magazyn współdzieli `ProductWarehouseStockPanel` + `MagazynInventoryLine` z produktu; backend: `image_url`, `sku`, `ean`, `reserved_qty`, `available_qty` (read).

- **Metody dostawy — słownik przewoźników:** stałe kody `OTHER` + InPost, DPD, DHL, Orlen Paczka, Allegro One, Temu; migracja `ensure_shipping_methods` + `GET /shipping-methods` dopina wiersze i aliasy; `purge_non_canonical_shipping_methods` usuwa pozostałości (przepięcie zamówień + linków kartonów); import nadal **bez** tworzenia metod z tekstu; heurystyka śmieci (paczkomat-kod, adres, długie nazwy, „paczkomat” w nazwie); API: **POST wyłączone**, **DELETE** tylko dla kodów spoza słownika, PUT zablokowana zmiana nazwy przewoźnika; UI ustawień pełna szerokość / wiersze jak WMS, tylko edycja.

- **Metody dostawy — import:** `code` + `aliases_json` (API: `aliases[]`), unikat `(tenant_id, warehouse_id, code)`, migracja + domyślna metoda `OTHER` / „Inne” na magazyn; dopasowanie importu: `normalize` (trim+lower) + `alias in normalized` (najdłuższy wygrywa), fallback OTHER; UI ustawień: kod + aliasy (CSV); nie można usunąć / zmienić kodu OTHER.

- **WMS pakowanie — wybór trybu (modal skanu):** `PackingModeSelectionView` — duży nagłówek statusu (badge), przyciski „Przejdź do listy zamówień” / „Zeskanuj wózek” / „Zeskanuj wózek z koszykami” wg `modes`; skan wózka w modalu (portal), skaner tylko przy otwartym modalu, sukces → zamknięcie + lista z kontekstem wózka. `WmsPackingStatusPage` / `WmsPackingModePage` używają tego widoku (bez „← Statusy”, bez osobnego ekranu skanu z wyboru trybu). Przy sesji bulk/baskets bez `cart_id` przekierowanie na `/wms/packing/mode` zamiast `/scan-cart`.

- **WMS pakowanie — status / tryb / skan wózka:** ekran statusu (`WmsPackingStatusPage`) po wyborze statusu przechodzi do wyboru trybu na tej samej trasie (bez `/mode`), skan `resolve-cart` ustawia sesję i idzie na listę zamówień; produkt/EAN → `showScannerError` (czerwony baner ~2,8 s). `WmsPackingModePage` i `WmsPackingScanCartPage` — pełna szerokość `#f4f7f9`, te same marginesy; tryb: skan wózka jak na statusie. Kontekst skanera: `scannerError` + `showScannerError`.

- **WMS — automatyzacja po pakowaniu:** po pełnym spakowaniu zamówienia `WmsPackingOrderPage` pokazuje `AutoActionsView` (2 kolumny: `StaticOrderPanel` + `StepPanel` z 4 krokami co ~3 s, animacja fade/slide w CSS; przyciski Lista / Wróć do zamówienia / Edytuj w Sellasist → `/orders/:id`; „Wróć” chowa flow i wraca do `PackingView`). Komponenty: `postComplete/AutoActionsView`, `StaticOrderPanel`, `StepPanel`, `StepContent`.
- **WMS pakowanie — domknięcie bez auto-nawigacji:** API karty/szczegółu `is_completed`; front `isPackingOrderCompleted()` (flaga lub linie / sumy). `applyPackingResult` nie przechodzi na `next_order_id` ani na listę. Skan na `AutoActionsView`: `GET …/resolve-ean` → nawigacja tylko do innego `order_id`. Reset stanu po zmianie `orderId`.

- **WMS pakowanie — widok zamówienia (drobne):** tylko pierwsza linia nazwy dostawy (bez adresu w polu), bez logo kuriera; ikona telefonu przy numerze; karta spakowana: usunięty badge „Spakowane x/y”, watermark „SPAKOWANO” po przekątnej (−38°, półprzezroczysty zielony).

- **WMS pakowanie — widok pojedynczego zamówienia (UX):** `PackingView` — badge dokumentu: zielony `Fa [numer]` gdy jest `sales_document_label`, szary „Paragon”/„Faktura” z `document_prefix`; pod klientem kurier (logo + nazwa) i wartość zamówienia; telefon bez ikony; przycisk „Korespondencja” z chatem; notatki magazynowe tylko gdy niepuste; usunięte „Zamów kuriera” / „Wyślij SMS”. Karty: `DefaultCard` pełna nieprzezroczystość; `DoneCard` delikatne tło zielone + overlay „Spakowano”; `ActiveCard` większy stepper, domyślna ilość 0, „Spakuj” wyłączone przy 0; `LineDetailsBlock` bez pustego wiersza Kolor.

- **WMS pakowanie — lista zamówień (Figma dense row):** `/wms/packing/orders` — pionowa lista wierszy (nie siatka kart): lewa kolumna NR/SPAKOWANO, pin, numer, badge „Pa”, kurier; prawy strip produktów inline (`ProductInlineItem`, overflow „+N innych”); nagłówek „Tryb pakowania”, „Zamówień: n”, badge’y Spakowane/Zbierane/Braki; tło `#f4f7f9`; komponenty `OrdersListView`, `OrderRow`, `ProductInlineItem`, `StatusBadges`.

- **WMS pakowanie — widok zamówienia (Sellasist-like):** `/wms/packing/order/:id` — układ 3 stref: lewy sidebar ~300px (klient, telefon z ikoną + przyciski, czerwone NOTATKI, „Spakuj wszystko”, „Przerwij”), nagłówek (numer, kolejka `queue_index`/`queue_total`, wózek/koszyk, pakujący, opakowania, pasek UWAGI KLIENTA, przyciski operacyjne), siatka kart 3 kolumn (`xl`) z ciaśniejszym `gap-3`; trzy osobne komponenty kart (`DefaultCard`, `ActiveCard`, `DoneCard`) + `ScannerHandler` (było `PackingScannerHandler`); klik / skan jak wcześniej; backend `WmsPackingOrderDetailOut` + wzbogacone linie; logo TEMU w `/assets/carriers/temu.svg`.

## 2026-04-09

- **WMS pakowanie — koszyki (MULTI):** `GET /api/carts/by-code/{code}/orders` (lista kolejki po kodzie wózka; unika kolizji z `/{cart_id}/…`), `GET /api/baskets/{code}/order?cart_id=&status=&mode=baskets` (skok do zamówienia koszyka); karty listy z opcjonalnym `basket_code` (tylko `mode=baskets`); skan na liście: najpierw EAN, potem koszyk (pomija listę → order), potem kod wózka (ładuje listę). Komunikaty PL: pusty koszyk, brak w kolejce, nieznany kod.
- **WMS pakowanie — zamówienie:** sortowanie (niespakowane góra), klik → modal [-]/[+] + „SPAKOWANE” (`POST .../line-pack`), „Spakuj wszystko” (`POST .../pack-all`), „Przerwij” → lista; detail: `customer_phone`, `shipping_method_name`, bez adresu w JSON pakowania; wspólne finalizowanie po skanie/linii/pack-all.
- **WMS pakowanie — widok zamówienia (wolny skan):** `/wms/packing/order/:id` — siatka wszystkich pozycji, skan EAN zwiększa pierwszą pasującą linię z niedoborem (bez kolejności); odpowiedź scan: `last_packed_order_item_id` + zielony flash kafelka; UI PL („Spakowane”, „Zły produkt”); kurier tylko nazwa; bez adresu na tym ekranie.
- **WMS pakowanie — flow skanu EAN:** `GET /wms/packing/resolve-ean` (FIFO po `created_at`, kolejka jak lista + linia z `packing_quantity_packed < quantity`, rozpoznanie kodu jak przyjęcie); `GET /wms/packing/orders/{id}/detail`; `POST /wms/packing/orders/{id}/scan` (+1 na `packing_quantity_packed`, przy pełnym zamówieniu status panelu → DONE wg heurystyki nazwy „spakowane/wysłane…” lub pierwszy DONE); front: `/wms/packing/order/:orderId`, lista skanuje EAN → przejście do zamówienia (bulk/baskets: przy braku produktu w kolejce — skan wózka). Kolejka uwzględnia też statusy IN_PROGRESS z nazwą zawierającą „pak”/„pack”.
- **WMS pakowanie — postęp i karty:** `order_items.packing_quantity_packed` (domyślnie 0) + migracja SQLite; `wms_packing_service` liczy `quantity_packed` z tej kolumny (nie z Pick). UI listy pakowania: siatka `repeat(auto-fill, minmax(260px, 1fr))`, karta [obraz | badge ilości, nazwa, EAN, SKU].
- **WMS pakowanie — tryby przed listą:** `GET /wms/packing/modes?status=` (liczniki: bez wózka / BULK / MULTI-koszyki); po wyborze statusu ekran wyboru trybu, potem `no_cart` → od razu zamówienia albo skan wózka dla bulk/baskets; `GET /wms/packing/orders` wymaga `mode`. Sesja `wms_packing_session_v2`. Target-statuses bez `require_cart`/`cart_type`.
- **WMS kafelki statusów (zbieranie + pakowanie):** `GET /wms/picking/configured-statuses` i `GET /wms/packing/target-statuses` zwracają `order_count`, `require_cart`, `cart_type` (`BULK`|`BASKETS`|null); liczniki z agregacji `orders` po `order_ui_status_id`. UI: wspólny `WmsFlowStatusTileButton` — [ikona wózek|koszyk] nazwa (liczba), bez etykiet tekstowych trybu; ikona `basket` w `Icon`.
- **WMS pakowanie — flow status → (skan wózka) → lista:** Backend `GET /wms/packing/target-statuses` (unikalne `target_status_id` z `picking_config` + `requires_cart_scan` jak przy zbieraniu: `scanned`/`baskets`), `GET /wms/packing/orders?status=&cart_id=`; front: `/wms/packing`, `/wms/packing/scan-cart`, `/wms/packing/orders`, `sessionStorage` `wms_packing_session_v1`, skan globalny na ekranach pakowania (resolve jak zbieranie), `WmsPickingResolveCartResponse.cart_type`. Usunięty placeholder `WmsPackingPage`.
- **Wózki — podgląd zamówienia w koszykach:** jeden sposób — modal `OrderProductPreviewModal` (usunięty inline/popover przy badge); nagłówek `Zamówienie #… • Koszyk … • Imię Nazwisko` z `GET /orders/{id}` (`first_name`/`last_name`) + opcjonalny kod koszyka z karty; bulk bez segmentu koszyka.
- **WMS zbieranie — spisanie magazynu przy finalizacji:** `record_wms_quick_pick` zapisuje Pick z `picked_at=NULL` (roboczo na `cart_id`); `finalize_wms_picking_cart` w jednej transakcji: dla pending Pick zdejmuje ilość z `Inventory` (FIFO po partiach w lokalizacji), zapisuje `StockMovement` typu `pick`, ustawia `picked_at` i `status=done`. Lista produktów WMS (`GET .../product-lines`) przy `cart_id` liczy tylko Pick z tego wózka. `clear_cart` usuwa robocze Pick (`picked_at` NULL) przed odpięciem pozostałych.
- **Wózki regałowe — UI operacyjny kart:** `CartCard` uproszczony pod pracę WMS: przycisk **Wyczyść wózek** przeniesiony do akcji w karcie (z modalem), usunięty blok debug `Zbieranie WMS`, status `FULL` pokazuje badge **Gotowe do pakowania** pod linią pojemność/sekcje, etykiety statystyk PL (`Zamówienia`, `Produkty: X szt.`, `Zajęte koszyki`), puste koszyki jako badge `PUSTY`, zamówienia jako badge `[ #... ]`, koszyki z kolorami stanów (empty/partial/ready), oraz klik badge zamówienia w koszyku otwiera popover z `#zamówienie • Koszyk • klient`.
- **Spójność danych koszyka (backend):** `CartService` liczy statystyki koszyka z połączonych źródeł (assigned orders + basket allocations + picks): `total_orders` z unii identyfikatorów, `total_products` także z picks quantity, `used_volume` jako max z przypisań/koszyków/objętości picks; w `GET /carts/{id}` dla koszyków dodane `order_customer_name` z `orders.addresses_json` (shipping/billing).
- **Wózki — Wyczyść wózek:** `CartCard` — widoczny przycisk „Wyczyść wózek”, modal potwierdzenia (Anuluj / Tak, wyczyść), szerszy warunek widoczności (m.in. `total_orders`, WMS picks, `assigned_orders`); po sukcesie nadal `onClearSuccess` (odświeżenie listy). Backend `CartService.clear_cart` dodatkowo zeruje `cart_id` na `PickTask` i `Pick` dla tego wózka (odpięcie kompletacji od sesji wózka).
- **Koszyki / wózki — podgląd zamówienia:** `OrderProductPreviewModal` — miniatury produktów `object-contain`, białe tło, wyśrodkowanie w ramce 128×128, bez przycinania (zamiast `object-cover`).

## 2026-04-07

- **WMS podgląd produktu — globalny skan:** `WmsScannerContext` tryb `product_preview`, `classifyWmsScanCode`, `resolveWmsPreviewScanToProductId` + `GET /api/products/by-ean/{ean}`; hub i `WmsProductPreviewPage` bez lokalnych handlerów; `DevScannerPanel` — podpowiedź dla `product_preview`.

- **WMS zbieranie — przypisanie koszyka (MULTI):** `ensure_order_basket_for_wms_pick` w `picking_assignment_service` — jeśli zamówienie ma już `basket_id`, bez zmian; w przeciwnym razie pierwszy pasujący koszyk (pojemność jak przy `_assign_multi`, objętość zamówienia z `_order_volume_dm3`). Wywołanie przy `GET product-lines/detail` (FIFO + `cart_id`) oraz przed zapisem `quick-pick`. Odpowiedź: `put_to_basket_label`, `put_to_basket_color_index`. UI: blok „ODŁÓŻ DO: …”, kolorowe karty zamówień; usunięte teksty FIFO / sesji.

- **Ustawienia WMS — konfigurator zbierania (UX):** brak auto-otwierania pustego formularza; domyślnie lista zapisanych reguł + przycisk „+ Dodaj kolejny status zbierania”; formularz dopiero po kliknięciu (lub Edytuj na karcie). Akordeon „Zamówienia wieloelementowe” domyślnie zwinięty; „jednoelementowe” rozwinięte. Plik: `WmsSettingsPage.tsx`.

- **WMS zbieranie — kontekst zamówień na szczególe produktu:** `GET .../product-lines/detail?cart_id=` — lista zamówień tylko dla bieżącego wózka (lub bez przypisanego wózka), `picked_quantity` z Picków na tym wózku, `basket_slot` z koszyka MULTI, `active_fifo_order_id` (pierwsze z niedoborem). UI: sekcja „Zamówienia” pod lokalizacjami, podświetlenie FIFO.

- **WMS zbieranie — szczegół produktu / lista:** Dolny pasek akcji zawsze widoczny (`z-[100]`): Zbierz produkt, Podgląd, Zgłoś problem, Brak produktu (`PackageMinus`), Zebrane; komunikaty nad paskiem. Widok produktu do odczytu: zdjęcie, nazwa, EAN, ilości, lokalizacje (bez klikania w wiersze), sekcja zamówień pod lokalizacjami (z `cart_id` w API). Lista produktów: jeden wiersz → szczegół. Pliki: `WmsPickingProductDetailPage.tsx`, `WmsPickingProductsPage.tsx`.

- **Konfigurator zbierania — refaktor:** DB/API `picking_config`: kolumny `pick_unit`, `order_sort`; `POST /wms/picking/config` przyjmuje `pick_unit` + `order_sort` (zamiast samego `strategy`); `strategy` w DB nadal zsynchronizowana. UI: limity BULK globalnie w „Logistyka…”, tryb „Po produktach”, sort zamówień (data / lokalizacje / kurier — placeholder), nowe etykiety kontenerów. Migracja SQLite: `ensure_picking_config_workflow_columns`.

- **Ustawienia WMS — persystencja konfiguratora zbierania:** usunięty `sessionStorage` dla listy reguł; po wejściu w zakładkę Zbieranie — `GET /wms/picking-config`; przycisk **Zapisz konfigurację** — `POST /wms/picking/config` z pełną listą reguł (walidacja min. jedna reguła, blokada przy otwartym szkicu, limity BULK); po sukcesie stan z odpowiedzi serwera + baner „Konfiguracja zapisana.”; szkic: **Dodaj regułę do listy**. API klient: `frontend/src/api/wmsPickingConfigApi.ts`. Ekran `WmsPickingStatusPage` — pusty stan wskazuje konfigurację w ustawieniach.

- **WMS zbieranie — lista i flow tylko z DB:** `GET /wms/picking/configured-statuses` — wyłącznie statusy z `picking_config` (nazwa+kolor z `order_ui_statuses`, bez liczników zamówień); `GET /wms/picking/config?status=<source_status_id>` — `single_mode`/`multi_mode` jako `cart_scan|cart_no_scan|baskets|mobile`, `strategy` `by_date|by_location`, `limits`. Front: brak skanu wózka przy `cart_no_scan` i `mobile`; skan przy `cart_scan` i `baskets`.

- **WMS zbieranie — flow wg konfiguracji:** po wyborze statusu `GET /wms/picking-config/by-source-status`; jeśli `single_mode !== multi_mode` → `/wms/picking/order-type` (jedno / wielo / wszystkie); jeśli ten sam tryb i `mobile` → od razu `/wms/picking/locations` (bez skanu wózka); w przeciwnym razie `/wms/picking/cart`. Brak konfiguracji (404) → jak dotąd tylko skan wózka. Sesja nawigacji: `wmsPickingFlowTypes` + `wmsPickingFlowResolve.ts`.

- **WMS zbieranie — wejście po statusie:** `GET /wms/picking/status-workload` — dla każdego `order_ui_status` liczba zamówień w magazynie oraz `cart_id IS NOT NULL` („w trakcie”); front: `/wms/picking` karty (kolor jak panel), sort malejąco po liczbie zamówień; klik → `/wms/picking/cart` (skan wózka) → `/wms/picking/locations` (lista lokalizacji, stan nawigacji z sesją). `WmsScannerContext` — tryb picking dla podścieżek.

- **WMS `picking_config` (backend):** tabela + model `PickingConfig` (tenant, warehouse, source/target `order_ui_statuses`, strategy `locations|orders`, single/multi mode `bulk|scanned|baskets|mobile`, max\_\* tylko przy bulk); unikat `(tenant_id, warehouse_id, source_status_id)`; API `/api/wms/picking-config` GET/POST/PUT/DELETE; serwis `picking_config_service` + tylko-odczyt `getPickingConfig` / `get_picking_config` w `picking_config_query.py` (bez integracji z pickami).

- **WMS UI zbieranie po lokalizacjach:** `/wms/picking` — ekran scanner-first (lokalizacja → produkt → potwierdzenie), duże fonty / wysoki kontrast, sekcja rozbicia koszyków (B{n} / Wózek), lista demo lub `state.pickList` (`LocationPickListRow[]`); `locationPickingTypes.ts`.

- **WMS PickingRoutingService:** `backend/services/picking_routing_service.py` + `backend/schemas/picking_routing.py` — lista zbiórki po lokalizacjach (strategia „Po lokalizacjach”): zamówienia → pozycje → alokacja z agregatu `Inventory` (greedy wg kolejności lokalizacji, preferencja `type=pick`), `basket_id` z zamówienia (BULK → null), grupowanie po `(location_id, product_id)` z sumą ilości i rozbiciem koszyków, sort po `location.name`; bez zapisu PickTask / stock / MM; `shortfalls` i `warnings` gdy brak stanu lub zamówienia.

- **WMS Picking Orchestrator:** `backend/services/picking_orchestrator.py` + `backend/schemas/picking_orchestration.py` — jeden punkt wejścia: tryby `BULK` / `SCANNED_CART` / `BASKETS` / `MOBILE` per jedno- i wielopozycyjne, podział po `len(items)`, `MOBILE` bez przypisania (kolejka w wyniku), pozostałe przez `PickingAssignmentService` z walidacją typu wózka; pominięcie `cart_id != NULL` (retry); scalaenie przypisanych i odrzuconych. `PickingAssignmentConfig`: opcjonalne `max_orders_in_bulk_single_item` / `max_orders_in_bulk_multi_item` (niezależne limity na BULK; brak zmian schematu DB).

- **Ustawienia WMS — Limity zbioru (logistyka):** sekcja „Limity zbioru” pokazuje pole jednoelementowe i/lub wieloelementowe zależnie od wyboru „Gdzie zbierasz” w danym bloku (tylko „Wózek (bez skanowania)”); ukryte przy skanowaniu, koszykach i wózku mobilnym; walidacja 1–100 po `blur`.

- **Konfigurator zbierania — „Gdzie zbierasz”:** osobny wybór na górze każdego akordeonu (jedno- / wieloelementowe): wózek bez skanowania, ze skanem, koszyki, wózek mobilny (pick & pack). Usunięto duplikat bloku „Kontenery”. Przy wózku mobilnym: tylko prosty tryb (sposób zbierania + opis pick & pack dla wieloelementowych); ukryte typ zbioru, kolejność/strategia i logika przypisań w UI. Limity ręczne w logistyce — osobno dla jedno- i wieloelementowych, gdy dany blok ma „bez skanowania”.

- **Ustawienia WMS — Konfigurator zbierania:** listy „Status do zbierania” i „Status po zebraniu” korzystają z `getOrderUiStatusSummary` (te same podstatusy panelu co Zamówienia); wartości opcji = `id`, etykiety = `name`; usunięto `MOCK_ORDER_STATUSES`.
- **Konfigurator zbierania — logika UI:** „Status do zbierania” wymagany (komunikat po `blur`); blokada wyboru tego samego statusu co „po zebraniu”; ukrycie „Typ zbioru” / liczby zamówień dla jednoelementowych i dla zbioru „po produktach”; liczba w zbiorze ukryta przy koszykach/wózkach (+ krótki opis); strategia bez opcji „Po zamówieniach”; numeracja sekcji zależna od widocznych bloków.
- **Konfigurator zbierania — model operacyjny:** kontenery = wózek bez/z ze skanem + koszyki (bez „brak kontenerów” i bez checkboxa skanowania); jednoelementowe — tylko „Sposób zbierania”; wieloelementowe — zbiór i strategia lokalizacji tylko przy „Po produktach”; przy „Po zamówieniach” wyłącznie kolejność po dacie (tekst, bez opcji po lokalizacjach); usunięto akordeon „Wszystkie zamówienia”; `PickingBlockState` bez `requireContainerScan`.
- **Silnik przypisania zamówień → koszyki:** `backend/services/picking_container_assignment.py` + `backend/schemas/picking_container_assignment.py` — objętość w dm³, first-fit, wiele zamówień na koszyk, `oversized` / `no_capacity_remaining`, wrappery ORM (pojemność koszyka z `usable_volume` cm³ → dm³; objętość zamówienia jak `cart_service`); bez zmian w Pick / API pick.
- **WMS Picking Assignment (transakcyjny):** `PickingAssignmentService.assign_orders_to_cart(..., config: PickingAssignmentConfig)` — `single_item` / `multi_item` z polami `allow_bulk`, `allow_basket`; opcjonalnie `max_orders_in_bulk`; BULK tylko przy `allow_bulk`, MULTI koszyki tylko przy `allow_basket`; bez sztywnego „multi tylko koszyk”; zapis `Order` / `CartBasket` / `Cart.used_volume`; bez PickTask / MM / stock.

- **WMS uzupełnianie braków:** API `buffer_sources` (wszystkie bufory); UI — karty pogrupowane po produkcie, kompakt; karta → ten sam flow MM (skan źródło → skan produkt z walidacją do karty → ilość → draft); po zapisie draft: „Kontynuuj uzupełnianie” (zakładka braków) lub „Kontynuuj przesunięcia” (skan).

- **WMS MM draft — ekran po zapisie:** po udanym `appendMmDraftLine` (`WmsMmTransferPage`) — krok `DECISION` z tytułem „Produkt dodany do przesunięcia”; „Kontynuuj przesunięcia” → `resetSession` (skan lokalizacji, draft na serwerze); „Przejdź do trybu rozlokowania” → `navigate` na `putawayPz(docId)` ze `state.tenantId`. Zakładki Skanowanie/Braki zablokowane na `DECISION`.

- **WMS MM draft (Skanowanie):** `WmsMmTransferPage` — po „Zapisz” bez celu: `POST /wms/mm/draft/lines`, potem ekran decyzji (albo kontynuacja skanu źródła, albo wejście w rozlokowanie dokumentu); brak skanowania celu w `ENTER_QTY`. Ścieżka z celem (`Uzupełnianie braków` → `postWmsMmTransfer`) bez zmian. `WMS_RECEIVING_UPDATED_EVENT` po dopisaniu linii draft — odświeżenie listy rozlokowania.

## 2026-04-06

- **WMS uzupełnianie pick:** lista pozycji gdzie stan na lokalizacji PICK jest poniżej `products.min_pick_quantity` i jest stan w BUFFER (wg `wms_location_badge_kind`). API: `GET /wms/replenishment/lines`, `POST /wms/replenishment/execute` (MM z bufora na pick, walidacja ilości względem stanu w wybranym buforze). UI: zakładka „Uzupełnianie pick”, tabela + modal ilości (`suggested_qty` = min(brak, bufor)).

- **WMS MM (przesunięcie wewnętrzne):** dokument `stock_documents.document_type = MM`, nagłówek `mm_from_location_id` / `mm_to_location_id`, linie per partia (FIFO z źródła), operacje `MOVE_OUT` + `MOVE_IN` na `stock_operations`, korekta `Inventory` (spójnie z putaway). Shell dostawy/dostawcy `__WMS_MM_*` na tenanta (FK bez zmiany NOT NULL na `supplier_id`/`delivery_id`). API: `GET /wms/mm/resolve-location`, `GET /wms/mm/location-inventory`, `POST /wms/mm/transfer`. UI: zakładka „MM / Przesunięcie”, flow skan źródło → produkt (lista/EAN) → modal kartony/sztuki → skan cel → zapis; `wms:inventory-updated` po zapisie.

- **Magazyn — globalny wybór:** `WarehouseContext` ładuje `/warehouses/`, auto-wybór przy jednym magazynie, pamięć localStorage przy wielu; `GlobalWarehouseSelect` w nagłówku `MainLayout` i `WmsLayout` (tylko gdy `warehouses.length > 1`). Usunięto lokalne selecty magazynu z designera, etykiet, kodów, analiz, office/damages, drukarek (tworzenie), inventory itd.

- **Zamówienia do dostawcy — pole `name`:** kolumna `deliveries.name` (nullable), API list/read/create/update + `search` (nazwa lub dostawca); auto-przy tworzeniu `"{Dostawca} {dd.mm.yyyy}"`; lista i modal — nazwa lub `#id`.

- **Zamówienia do dostawcy — akcje jak dokumenty magazynowe:** lista `PurchaseOrdersPage` — ✏️ / 🗑 / 🖨 (menu Drukuj / Pobierz PDF), modal usuwania, toast błędu; `DELETE /deliveries/{id}` tylko dla `draft` (komunikat PL); `supplierOrderPdfUrl` + druk/PDF w przeglądarce.

- **WMS rozlokowanie — zakończenie procesu:** kolumna `stock_documents.relocation_status` (OPEN | DONE), migracja + backfill DONE dla PZ ze `putaway_status = DONE`. Lista `/wms/putaway/pz` wyklucza `relocation_status = DONE`. `PATCH /wms/relocation/pz/{id}/finalize` ustawia DONE; jeśli `receiving_status = DONE`, ustawia `status = zakonczone` (bez zmian inventory). UI: przycisk „Zakończ rozlokowanie”, confirm gdy nie w pełni rozlokowano.

- **Dokumenty magazynowe — usuwanie twarde:** `DELETE /documents/{id}` / `DELETE /stock-documents/{id}` — `hard_delete_stock_document`: bez operacji → kasowanie dokumentu i pozycji; z operacjami → odwrócenie PUTAWAY (stan magazynowy), dla `posted` odwrócenie przyjęcia na lokalizacji i `delivery_items`, potem usunięcie wierszy. UI: ikony ✏️🗑🖨 (menu Drukuj / Pobierz PDF), modal potwierdzenia, toast błędu. `POST .../cancel` nadal soft-cancel (legacy).

- **WMS — operacje magazynowe (SSOT):** tabela `stock_operations` (append-only: `document_id`, `document_line_id`, `product_id`, `location_id`, `qty`, `type`, `batch`, `expiry_date`). Putaway = INSERT `PUTAWAY` (nadpisywanie `stock_item_locations` wyłączone); `processed_qty` = SUM(`qty`) po typie; alokacje w API z agregatu `GROUP BY location_id`. Migracja startowa: kopiowanie `stock_item_locations` → operacje gdy tabela operacji pusta. `build_stock_document_read` preferuje operacje, fallback SIL. Inventory nadal aktualizowane przy putaway.

- **Produkty — Magazyn vs lista:** `GET /products/` i `GET /products/{id}/` zwracają `inventory` (wiersze z tabeli `inventory`: `location_id`, `location_code`, `location_type`, `quantity`, `batch`, `expiry`, plus `warehouse_id`, `location_uuid`). Ładowanie lokalizacji używa `include_inactive` dla `Location`/`Bin`, żeby stany nie znikały przy nieaktywnej lokalizacji. Zakładka Magazyn w `ProductEditModal` pokazuje tabelę z `product.inventory`; lista produktów i badge lokalizacji preferują `inventory` gdy jest w odpowiedzi.

## 2026-04-05

- **WMS rozlokowanie vs przyjęcie:** lista `/wms/putaway/pz` — PZ z `putaway_status != DONE` i **co najmniej jedna linia z `received_quantity > 0`** (nie wymaga już `receiving_status == DONE`). PATCH putaway / suggest-location: bez blokady „najpierw zakończ przyjęcie” (wystarczy `received_quantity` na linii). Frontend: `WmsPutawayPzPage` — karty aktywne dla szkicu; baner informacyjny zamiast blokady; `wms:receiving-updated` po zapisie przyjęcia / zakończeniu — odświeżenie listy rozlokowania i widoku PZ.

- **WMS rozlokowanie — badge lokalizacji jak na mapie:** `PutawayAllocationRead` + lista `/warehouses/{id}/locations` zawierają `storage_type` (z `Bin` po `location_uuid`), `zone` (`rack_name`), `capacity_type` (`Location.type`). `BUFFER` → `reserve` w mapowaniu WMS→storage (wcześniej `neutral` → unknown). `LocationBadge` przyjmuje opcjonalny `storageType`; putaway ładuje PZ przez `GET /wms/putaway/pz/{id}` (jak stock-documents). PATCH putaway — wiersze `locations` z tymi samymi polami.

- **WMS rozlokowanie → stan magazynu:** `patch_wms_putaway_item` zawsze ustawia `Inventory.location_uuid` z `Location` (dopasowanie do layoutu); odpowiedź PATCH opcjonalnie `inventory_snapshot` (wiersz Inventory po zapisie). Po udanym zapisie putaway frontend emituje `wms:inventory-updated`; `WarehouseDesigner` nasłuchuje i wywołuje `fetchProductsForMap()` (GET `/inventory/` + layout + produkty), żeby badge Magazynu = dane z API.

- **WMS rozlokowanie — kartony/sztuki jak przyjęcie:** `WmsPutawayPzPage` — stan `cartonsCount`, `unitsCount`, `unitsPerCarton`, `inputMode` (carton|unit|null), `draft`; skan produktu zwiększa karton lub sztukę; `total` w szt. na PATCH; walidacja `total ≤ received − putaway`; jedno pole + „Łącznie” + linia Kartony|Sztuki; reset po zapisie / nowa lokalizacja.

- **WMS przyjęcie — bez limitu vs zamówione:** usunięte `_assert_receiving_groups_within_ordered` z `patch_wms_receiving_pz_item_quantity` i `split_wms_receiving_pz_item_lines` (`wms_receiving_service.py`). Rozlokowanie nadal: suma putaway ≤ `received_quantity` (`wms_putaway_service.patch_wms_putaway_item`). Frontend: dyskretna informacja „Przyjęto więcej niż zamówiono” w modalu przyjęcia gdy `received + delta > ordered`.

- **WMS modal przyjęcia — jedno pole:** `inputMode` carton|unit (ustawiany ostatnim skanem); pole pokazuje `cartonsCount` lub `unitsCount`; +/- zmienia aktywny licznik; „Łącznie: X szt” + linia „Kartony: … | Sztuki: …”; zapis = `cartons×per+units` (bez draftu na total).

- **WMS liczenie PZ — opakowanie zbiorcze (skany):** `WmsReceivingCountPage` — `cartonsCount` / `unitsCount` / `unitsPerCarton`; skan kartonu (`bulk_ean` lub `product_barcode` z mnożn. >1) → +1 karton i `unitsPerCarton = max(..., dq)`; skan jednostki (`product_ean` / kod x1) → `unitsCount += 1`; pole = suma; pod polem „Kartony: … | Sztuki: …”; badge „Skan: karton (N szt)” / „Skan: sztuka”. Edycja pola: jeśli suma się zmieni → `cartonsCount=0`, `unitsCount`=wpis; bez zmian wartości przy blur — rozbicie ze skanów zostaje.

## 2026-04-04

- **WMS — osobne statusy przyjęcia i rozlokowania:** `stock_documents.receiving_status` = NEW | IN_PROGRESS | DONE (zamknięcie przyciskiem „Zakończ przyjęcie” → `POST /wms/receiving/pz/{id}/finish`); `putaway_status` = NOT_STARTED | IN_PROGRESS | DONE (aktualizacja przy PATCH putaway i z liniami). Lista Przyjęcie: `GET /wms/receiving/pz` — wyłącza DONE. Lista Rozlokowanie: `GET /wms/putaway/pz` — tylko receiving DONE i putaway ≠ DONE. `StockDocumentRead` + listy: `is_fully_received`, `is_fully_putaway` (linie vs zamówienie / putaway). Migracja: pending→NEW, in_progress→IN_PROGRESS, received→DONE; kolumna `putaway_status`.

- **WMS rozlokowanie — wiele lokalizacji na linię PZ:** tabela `stock_item_locations` (`stock_document_item_id`, `location_id`, `quantity`, UNIQUE para); PATCH dokłada do istniejącego wiersza lub tworzy nowy; `quantity_putaway` = SUM(wierszy); odpowiedź PATCH `{ item_id, total_putaway_quantity, locations[{location_id,code,quantity}], document }`; GET PZ czyta alokacje z tabeli (fallback: agregat z inventory gdy brak wierszy). Backfill przy starcie dla linii z `quantity_putaway` > 0 bez SIL (skala z inference lub całość na `location_id` PZ).

- **WMS rozlokowanie — LocationBadge:** `components/warehouse/LocationBadge.tsx` opakowuje istniejący `LocationTypeBadge` (kolory/ikony jak lista produktów / magazyn). Kind API: `PICK|BUFFER|BULK|INBOUND|OUTBOUND` → mapowanie na `storage_type` (`primary`/`neutral`/`buffer`/`pick`/`damaged`). `GET /warehouses/{id}/locations` zwraca `code`, `type`. `PutawayAllocationRead`: `location_code`, `location_type`; pozycja PZ: `putaway_last_location_type`. `location_badge.wms_location_badge_kind` z `location_type` (DOCK/PACKING/…) i `type` (pick/reserve/floor).

- **WMS rozlokowanie — UX:** sortowanie kart: nieukończone na górze, ukończone na końcu, w grupie `putaway_updated_at` malejąco. Po zapisie modal zawsze się zamyka; stan z odpowiedzi PATCH (bez dodatkowego reloadu). Karta ukończona: zielone tło/obramowanie, badge ROZLOKOWANO, lista lokalizacji z ilościami (`putaway_allocations` z agregatu inventory po partii PZ). Częściowe: wiersz „Rozlokowano: X / Y”, „Ostatnio: …” z `putaway_last_location_name`. DB: `putaway_updated_at`, `putaway_last_location_name` na `stock_document_items`.

- **PZ / rozlokowanie — magazyn:** `list_tenant_warehouse_ids` + `ensure_pz_document_warehouse_resolved` / `maybe_auto_assign_single_warehouse_on_pz` w `stock_document_service` — przy braku `warehouse_id` na szkicu PZ: jeden magazyn tenanta → przypisanie (flush); wiele → `Wybierz magazyn dla dokumentu`; zero → `Brak skonfigurowanego magazynu`. `GET /stock-documents/{id}` zapisuje auto-przypisanie gdy jest dokładnie jeden magazyn. Tworzenie PZ z dostawy: `warehouse_id` ustawiane od razu przy jednym magazynie. `set_stock_document_receiving_target`: brak `warehouse_id` w body i na dokumencie → ten sam wybór (nie używa już „domyślnego” przy wielu magazynach). Putaway: `suggest` tylko auto-przypisuje przy jednym WH; `PATCH` wymaga rozwiązanego magazynu. WMS receiving (`apply_wms_receive_deltas`, `patch_wms_receiving_pz_item_quantity`, `split`) wywołuje `ensure` przed zapisem. Frontend putaway: usunięte toasty/blokada „ustaw magazyn przyjęcia”.

- **Jeden magazyn na tenant (WMS / RMZ / panel):** backend `office/return-ui/*` — `warehouse_id` w query opcjonalne, domyślnie `resolve_tenant_default_warehouse_id` (błąd `Brak skonfigurowanego magazynu`). Frontend: `returnUiStatusApi` z opcjonalnym `warehouse_id`; `WmsReturnsPage`, `WmsReturnsEntryPage`, `WmsComplaintDetailPage`, lista/szczegół zwrotów, Setup (tryb zwrotów), `WmsSettingsPage`, statusy panelu zwrotów — bez wyboru magazynu; API zwrotów/reklamacji i panel tylko `tenant_id` tam gdzie wcześniej wymuszano WH z kontekstu.

- **WMS Rozlokowanie (putaway):** osobne trasy `/wms/putaway`, `/wms/putaway/:pzId`, `/wms/putaway/:pzId/item/:itemId` — lista szkiców PZ jak przyjęcie, widok linii tylko z `received_quantity > 0`, karta = jeden wiersz PZ (produkt × partia × data). `quantity_putaway` na `stock_document_items` + migracja; `GET .../suggest-location`, `PATCH /wms/putaway/{item_id}` zwiększa putaway i dopisuje `inventory` w wybranej lokalizacji; `accept` dokłada na lokalizację przyjęcia tylko `(received − quantity_putaway)`. Frontend: odświeżanie co 4 s, skan EAN na liście linii, sugestia lokalizacji (stan ta sama partia → stan produktu → pierwsza lokalizacja).

- **WMS Rozlokowanie — UX jak przyjęcie:** `WmsPutawayPzPage` — karty (zdjęcie, EAN, partia/data, wskaźnik `putaway / received`), klik lub skan EAN otwiera modal; w modalu najpierw skan lokalizacji (badge), potem ilość (domyślnie 0, skan produktu +1, +/−); bez lokalizacji brak edycji ilości i zapisu; ponowny skan tej samej lokalizacji przy qty>0 wywołuje `PATCH` automatycznie; po zapisie qty→0, lokalizacja zostaje. Stary URL `.../item/:itemId` → redirect na PZ ze `state.openPutawayItemId`. `WmsScannerContext`: tryb `putaway` także dla `/wms/putaway/:id`.

- **WMS karta produktu (wspólna):** `components/wms/WmsProductCard.tsx` — wspólna obudowa karty linii (indeks, opcjonalne menu ⋮, zdjęcie 100×100, treść, stopka); opcjonalny `overlay` (watermark, `pointer-events: none`). `WmsReceivingCountPage` — menu ⋮; `WmsPutawayPzPage` — bez menu na listach linii.

- **WMS rozlokowanie — karta ukończona:** przy `put == received`: zachowane zielone obramowanie / tło, badge ROZLOKOWANO, podsumowanie lokalizacji; dodatkowo przyciemnienie (~opacity 80%, lekki grayscale), hover lekko jaśniej; duży watermark „ROZLOKOWANO” (−25°, zielony półprzezroczysty).

- **WMS liczenie PZ — placeholder partii:** dla grupy `product_id` + `delivery_item_id` pokazywana jest co najwyżej jedna pusta karta tylko gdy brak „realnych” linii (`received>0` lub partia lub data ważności); po pierwszej zapisanej partii placeholder znika. Lista i skan używają `getVisibleReceivingItemsForWms`; na placeholderze krótki hint „Pierwsza partia…”. Hint i etykiety partii/dat — tylko gdy `track_batch` lub `track_expiry`; „realność” linii bez śledzenia partii liczy się wyłącznie od `received>0`.

- **WMS PZ — linie „ghost”:** wiersz z `ordered=0`, `received=0`, pustą partią i bez realnej daty (tylko sentinel) — ukrywany w `GET` szkicu PZ, usuwany przy `purge` po PATCH/split WMS i przed `accept`. Frontend też filtruje takie pozycje.

- **WMS liczenie PZ — karty:** jedna karta na wiersz PZ (partia × data); bez grupowania produktów. Sort: ostatnio dotknięta linia (sesja) malejąco, potem kolejność na dokumencie. Skan: wybór linii produktu = najnowszy touch, inaczej najmniejsze `id`. Modal: wstępnie wypełnia partia/data z wybranej linii.

- **WMS przyjęcie PZ — partie (uproszczenie):** modal: jedna ilość + partia/data; `PATCH .../items/{id}` z `quantity_received` (dodaje do partii), `batch_number` / `expiry_date` zawsze w body (`null` gdy produkt nie śledzi); walidacja po `track_*` w DB; komunikaty „Numer partii wymagany” / „Data ważności wymagana”.

- **WMS lista PZ / dokumenty:** statusy liczenia po polsku (`Nowe` / `W trakcie` / `Zakończone`) — wspólny helper `pzReceivingStatusPresentation.ts`; kolumna „Liczby WMS” w Dokumentach magazynowych używa tych samych etykiet. Lista WMS: `receiving_status` liczona z sum linii przy odczycie; `StockDocumentRead` zwraca też `updated_at`; autosave liczenia scala `updated_at` w stanie. Lista przyjęć odświeża się przy każdym wejściu na trasę (`location.key`).

- **WMS liczenie PZ — tylko modal:** `+` / `−` / liczba / cała karta otwierają ten sam modal; brak `bump` na karcie; zmiana `received_quantity` wyłącznie przez **Zapisz** w modalu (`setLineReceivedAbsolute`). Skan otwiera modal lub dokłada do pola „Przyjęto łącznie (szt.)” gdy modal jest dla tej samej pozycji; usunięty `receivingScanAutoMode` z kontekstu/panelu dev. Produkty bez EAN: nagłówek „Brak kodu EAN”, ręczna ilość w modalu.

- **WMS przyjęcie — modal skanu (mieszany):** wewnętrznie kartony + sztuki + mnożnik; **jedno pole** — etykieta i wartość zależą od ostatniego skanu (bulk → „Ilość kartonów”, unit → „Ilość sztuk”); `Łącznie` + dyskretna linia `Kartony | Sztuki`; zapis = suma; bez szarego tła przy zdjęciu / bez osobnych dwóch sekcji i tekstu o mnożniku kartonu.

- **WMS — globalny panel dev skanera:** `WmsScannerProvider` + `DevScannerPanel` w `WmsLayout` (jeden mount, z-index ~10k); `WmsScannerContext`: `handleScan`, `mode` (z trasy), `activeDocument`, rejestracja handlera per strona, historia EAN, toast. Globalny `keydown` w providerze (tylko gdy `DEV` lub `VITE_ENABLE_DEV_SCANNER`). Liczenie PZ: handler w `WmsReceivingCountPage`; picking/putaway/packing/lista PZ/zwroty — stuby z toastem.

- **WMS liczenie PZ:** usunięty blok „Miejsce przyjęcia” (magazyn/lokalizacja/zapis) — tylko liczniki i „Zakończ przyjęcie”; `PATCH` pozycji bez wymogu WH/loc. `PATCH /stock-documents/.../receiving-target` zostaje pod inne ekrany / zatwierdzenie.

- **PZ bez magazynu/lokalizacji przy tworzeniu:** `stock_documents.warehouse_id` / `location_id` nullable (migracja SQLite `migrate_stock_documents_nullable_warehouse_location`). Utworzenie PZ z dostawy ustawia NULL. `PATCH /stock-documents/{id}/receiving-target` + ekran WMS liczenia — wybór magazynu i lokalizacji. `accept` wymaga obu ustawionych.

- **POST /deliveries/{id}/create-pz:** brak body (magazyn/lokalizacja wybierane wewnętrznie: pierwszy magazyn tenanta, pierwsza lokalizacja). Linie PZ tylko z `quantity_ordered` / `purchase_price` pozycji zamówienia. Odpowiedź: `{ id, number, status: "draft" }`. Frontend: uproszczony modal PZ, `createPzFromDelivery(tenantId, id)` + `CreatePzResult`.

- **WMS lista przyjęć (PZ):** karty jak RMZ (`border-2`, `pt-10`, chip „PZ”, `shadow-md`/`hover:shadow-lg`, stopka „Ostatnia aktualizacja”), numer `PZ-RRRR-NNNN`, statusy NEW / IN_PROGRESS / DONE (`receiving_status`), `updated_at` w API i DB (`ensure_stock_documents_updated_at_column`). Nagłówek „Przyjęcie” + podtytuł bez zbędnych hintów; tenant z `localStorage` bez dropdownu. Wspólne `wmsListFormatters.ts` (import w RMZ). Ekran liczenia: usunięte długie podpowiedzi i dropdown tenanta.

- **PZ (przyjęcie z dostawy) — szkic, różnice, zatwierdzenie:** Tabele `stock_documents` / `stock_document_items`; pozycje: `ordered_quantity`, `received_quantity`, pole `quantity` zsynchronizowane z przyjętym (legacy). Migracja `ensure_stock_document_item_ordered_received_columns`. `POST /deliveries/{id}/create-pz` — linie z planowaną ilością na PZ, dokument `draft` (bez zmian inventory ani `quantity_received` na zamówieniu). `GET /stock-documents/{id}`, `PATCH` (tylko `draft`), `POST /stock-documents/{id}/accept` — księgowanie wyłącznie z `received_quantity`, walidacja wobec pozostałej ilości na linii dostawy. Lista `GET /stock-documents/`. UI: zamówienia — modal „Utwórz PZ”; Dokumenty magazynowe — „Otwórz”, tabela (produkt, zamówiono, przyjęto, różnica, cena, wartość), „Przyjmij wszystko”, zapis, zatwierdź przyjęcie.

- **Lista producentów — kolumna Produkty:** tylko `product_count` (bez tooltipu ze stanem magazynowym); tooltip „Otwórz listę produktów…”. Backend: doprecyzowanie w schemacie i docstringu listy, że `product_count` to wyłącznie liczba produktów z `manufacturer_id`. Statystyki magazynowe w modalu → zakładka Statystyki (nagłówki: Liczba produktów, Łączny stan magazynowy, Produkty bez stanu).

- **Zamówienie do dostawcy — PDF:** `GET /supplier-orders/{id}/pdf?tenant_id=` zwraca `application/pdf` (`supplier_order_{id}.pdf`). HTML (`templates/supplier_order.html.j2`) + Puppeteer stdin jak raporty magazynu (`html_document_to_pdf_bytes`). Nabywca: opcjonalne pola na `tenants` (`ensure_tenant_business_profile_columns`: company_name, tax_id, email, phone, adres). Przyciski „PDF” na liście zamówień i w modalu edycji.

- **Zamówienie do dostawcy — wybór produktów:** `GET /supplier-products/top` — do 5 produktów z największą sumą `quantity_ordered` w dostawach (bez `draft` / `cancelled`), potem liczba linii; join z ofertą dostawcy; opcjonalnie `manufacturer_id`. Modal PO: sekcja „Najczęściej kupowane” (zdjęcie, nazwa, cena, badge „Popularny”), bez duplikatu w głównej liście; przełącznik „Ukryj już dodane produkty” (domyślnie włączony) filtruje po `product_id`.

- **Produkt — etykieta detaliczna i galeria:** Zakładki „Etykieta / Dane produktu” i „Zdjęcia”. Dane etykiety w `metadata_json.label_data` (nazwa PL, importer, batch/seria, CE, tekstylia, kraj pochodzenia, cena na etykiecie); producent/adres tylko do odczytu z encji producenta. Galeria w `metadata_json.product_images` (`image_url`, `is_main`, `sort_order`) + URL / `POST /api/uploads`; przy zapisie `image_url` = główne zdjęcie. Komponent `RetailLabel.tsx` (~60×40 mm, PL, warunkowe sekcje i CE).

- **Łańcuch dostaw (producent → produkt → dostawca):** Zakładka „Dostawcy” w edycji produktu (lista `supplier_products`, domyślny dostawca, wskazówka „Najtańszy dostawca”). Producent: zakładki „Produkty” i „Dostawcy” (`GET /manufacturers/{id}/suppliers` — dostawcy z ofertą na produkty producenta). Dostawca: tabela „Produkty” z kolumnami zdjęcie / nazwa / EAN / SKU / cena. Zamówienie do dostawcy: oferta tylko z `supplier_products` + opcjonalny filtr producenta; API `GET /supplier-products/` z `manufacturer_id` i polami `manufacturer_id` / `manufacturer_name` na wierszu.

- **Producenci i dostawcy — dane firmowe:** Kolumny nullable `company_name`, `tax_id`, `city`, `postal_code`, `street` na `manufacturers` i `suppliers` (+ migracja `ensure_manufacturer_supplier_business_entity_columns`). API zwraca i przyjmuje te pola; `name` bez zmian. Walidacja: `country` jak lista dostawców (Polska, Niemcy, …); opcjonalny NIP — długość / znaki (`catalog/business_entity_validators.py`). Lista: pogrubiona krótka nazwa, druga linia: pełna firma · NIP. Modale: zakładki Podstawowe / Adres / Kontakt (dostawca: + Produkty, handlowe w Podstawowych).

- **Asortyment — dostawcy i zamówienia do dostawcy (warstwa biznesowa):** Trasy `/suppliers`, `/suppliers/new`, `/purchase-orders`; wpisy w nawigacji Asortyment (`mainNavConfig` + `navActive`). UI: „Zamówienia do dostawcy” (nie WMS). Przyjęcie magazynowe z wyborem lokalizacji i `POST /deliveries/.../receive` usunięte z modułu; brak aktualizacji inventory przy statusie. `POST /deliveries/quick-from-product` tworzy szkic z produktu. Produkty: `default_supplier_id`, wybór domyślnego dostawcy w `ProductEditModal`, akcja „Zamów u dostawcy” na liście produktów. Dostawca: m.in. `default_currency`, `minimum_order_value`.

- **WMS — dostawcy i dostawy przychodzące (zastąpione powyżej w Asortymencie; historia):** tabele `suppliers`, `deliveries`, `delivery_items` (+ `ensure_suppliers_and_inbound_deliveries_tables`). Statusy dostawy: `draft` | `ordered` | `in_transit` | `received` | `cancelled`. API: `GET/POST /suppliers`, `GET/PUT/DELETE /suppliers/{id}`; `GET/POST /deliveries`, `GET/PUT/DELETE /deliveries/{id}`, `POST /deliveries/{id}/items`, `DELETE .../items/{item_id}`, `GET /deliveries/receive-locations`, `POST /deliveries/{id}/receive` (body: `warehouse_id`, `location_id`, `lines[]` — tylko `Inventory.quantity +=`, bez `assigned_locations`). UI: zakładka WMS „Przyjęcie” — lista dostawców + modal, lista dostaw + edycja (Nagłówek / Produkty), modal przyjęcia z wyborem magazynu i lokalizacji.

- **Producenci — UX (druga iteracja):** `GET /manufacturers/` zwraca też `total_inventory_quantity` i `out_of_stock_product_count` (agregacja inventory po produktach producenta). Lista: logo → klik otwiera edycję; tooltip na liczbie produktów; `?edit=` + `tenant_id=` na `/manufacturers` otwiera modal. `ProductList`: pasek „Producent: {nazwa}”, badge „Filtr aktywny”, link edycji producenta; nazwa z `getManufacturer` gdy jest `tenant_id`. `ManufacturerEditModal`: zakładki Podstawowe / Statystyki / GPSR; klik podglądu logo → fokus pola URL. `ProductEditModal`: wspólny komunikat GPSR „dziedziczone z producenta” nad polami nadpisania.

- **Producenci — UX / modele / API:** relacje ORM doprecyzowane (`foreign_keys` na Tenant↔Manufacturer, Manufacturer↔Product, Product↔Manufacturer). `GET /products/` — filtr `manufacturer_id`. `GET /manufacturers/` — `sort_by` (`name` | `product_count`), `sort_dir`. `GET /manufacturers/{id}` — `total_inventory_quantity`, `out_of_stock_product_count`. UI: lista jak ProductList (tenant + siatka filtrów), sortowanie kolumn, klik w liczbę produktów → `/products/list?manufacturer_id=&tenant_id=`, toast przy dezaktywacji, modal edycji — sekcja statystyk; ProductList — pasek filtru producenta z URL; ProductEditModal — podpowiedź „dziedziczone z producenta: …” gdy brak nadpisania GPSR w metadanych.

- **Producenci (manufacturers):** tabela `manufacturers` (tenant, nazwa, logo*url, kraj, www, email, telefon, active, GPSR: responsible_person*_), FK `products.manufacturer_id` + upgrade SQLite. API `GET/POST /manufacturers`, `GET/PUT/DELETE /manufacturers/{id}` (DELETE: bez produktów = kasowanie; z produktami = dezaktywacja). Produkty: `manufacturer_id` w body, `manufacturer_brief` + `gpsr*responsible*_`w odpowiedzi (override z`product_ui`vs producent). UI:`ManufacturersPage`+`ManufacturerEditModal`; `ProductEditModal` — wybór z katalogu + tekst, e-mail GPSR w metadata.

- **Zestawy — lista / filtry / stan / etykiety:** `bundles.image_url` (upgrade); `GET /bundles/` zwraca `calculated_stock` = min(floor(SUM inventory ÷ ilość)) oraz `product_stock` na składowych; jedno zapytanie `SUM(inventory.quantity)` po zbiorze `product_id`. Filtry: `active_filter`, `name` + `ean_sku` (AND), `price_*`, `stock_*`; `search` zostaje dla `CreateOrderPage`. `POST /labels/bundle` (szablony typu product). UI: `BundlesPage` jak `ProductList` (zdjęcie, kolumny, ikony, podgląd składu); `BundleEditModal` — zdjęcie URL + podgląd, cena w Podstawowych, zakładki Podstawowe / Produkty (bez osobnej Ceny).

- **Zestawy (bundles) — pełna integracja:** tabele `bundles`, `bundle_items`; `order_items.source_bundle_id`, `bundle_instance_id`; eksplozja przy `POST /orders/` (`services/bundle_explosion.py`), scalanie po kluczu `(product_id, source_bundle_id, bundle_instance_id)`, metadane `metadata_json` z `bundle_qty`; opcjonalnie `check_bundle_stock` na zamówieniu. API: `GET/POST /bundles`, `PUT /bundles/{id}`, `GET /bundles/{id}`, `GET /bundles/{id}/expand`. UI: `BundlesPage` (CRUD + składniki po `product_id`), `CreateOrderPage` (wyszukiwarka produktów + zestawów, checkbox weryfikacji stanu), `OrderDetailPage` (grupowanie po `bundle_instance_id`, nagłówek zestawu, badge „Z zestawu”). Upgrade: `ensure_bundles_tables_and_order_item_bundle_columns`.

## 2026-04-03

- **Produkt — orientacja/stos (Logistyka):** dwie karty: „Produkt” vs „Opakowanie zbiorcze”, każda z orientacją, kształtem, `stack_behavior` (stos), kompresją + wysokością po kompresji, max wagą stosu. Backend: kolumny `carton_orientation_type`, `carton_shape_type`, `carton_stack_behavior`, `carton_stack_compressible`, `carton_compressed_height_cm`, `carton_max_stack_weight`; API zwraca też `product_*` (lustro kolumn `orientation_type` / …) oraz przestarzałe aliasy `orientation_type` itd. Create/update: preferencja `product_*` nad starymi kluczami w body.

- **Produkt — zakładka Logistyka:** dwa bloki: „Produkt (pojedynczy)” (L/W/H cm, waga, objętość, jednostka) i „Opakowanie zbiorcze” (`bulk_ean`, `units_per_carton`, wymiary/waga/objętość kartonu). Usunięto „Czas wysyłki” z UI; `product_ui.shipping_time_days` jest usuwane przy czyszczeniu pól UI w metadata. Backend: kolumny kartonu na `products` (już wdrożone). `ProductList` mapuje i synchronizuje pola kartonu po zapisie; `updateCartonDimension` po wszystkich hookach stanu.

- **Lista produktów — lokalizacja na mapie:** API `locations[]` z `location_uuid`; na badge `LocationTypeBadge` opcjonalna pinezka (mapa); `ProductLocationMapModal` lazy + `Suspense`, ten sam pipeline layoutu co Magazyn (`layoutStateFromWarehouseApi` + `WarehouseLayoutRenderer` read-only), podświetlenie binów `highlightedBinUUIDs` + `focusedBinUUID` (pomarańczowy vs niebieski). `RackLayer` / `WarehouseCanvas`: nowe `focusedBinUUID`.

- **Produkt — alarm magazynowy (Magazyn):** kolumny `products.enable_stock_alert`, `products.min_total_stock` (schema `ensure_products_stock_alert_columns`); API `ProductBody` + `_product_to_dict` + create/update (`model_fields_set` dla progu przy PUT). UI: karta „Alarm magazynowy” między „Stan magazynowy” a „Poziomy uzupełniania” (`ProductEditModal.tsx`); lista `ProductList` mapuje pola. Warunek docelowy powiadomień (poza tym zadaniem): `SUM(inventory.quantity) < min_total_stock` gdy alarm włączony.

- **ProductEditModal — układ jak Sellasist:** zakładki Podstawowe / Ceny / Magazyn / Logistyka / Oferty; nagłówek (nazwa, ID, stan fizyczny, cena); układ 2 kolumn + sidebar (zdjęcie, etykieta); usunięty zapis `stock_quantity` z formularza; `sale_price` + `metadata_json` (`product_ui`: VAT, promocja, osoba odpowiedzialna). Backend: `ProductBody.metadata_json`, create/update zapis; create ustawia też `purchase_price`.

- **Sidebar Asortyment (fly-out jak Zamówienia):** `mainNavConfig.tsx` — Produkty (`/products`), Zestawy (`/bundles`), Producenci (`/manufacturers`); pierwszy wiersz z `+` i menu (Dodaj produkt / zestaw / producenta). `NavFlyoutPanel.tsx` — `showAssortmentPlusMenu`, dropdown jak przycisk zamówień. `ProductList.tsx` — `?create=1` otwiera `ProductEditModal` w trybie tworzenia. Strony `BundlesPage`, `ManufacturersPage` + trasy w `App.tsx`; `navActive.ts` dla `/bundles` i `/manufacturers`.

- **Lista produktów (UI magazyn):** `ProductList.tsx` — lokalizacje przez `LocationTypeBadge` (max 3 + „+N więcej”); bez kolumny planów; zdjęcie 64px; akcje ikonami; `ProductLabelPrintModal` — PDF.

- **Badge lokalizacji (typ):** `components/warehouse/LocationTypeBadge.tsx` — jedna linia: ikona + adres + ilość (pogrubiona); bez etykiet typu; kolory z `getStorageTypeStyle`. `LocationPicker` (ProductEditModal): przypisane wiersze + lista dodawania.

- **Typy magazynowania (spójność):** Kanoniczne: `primary`, `pick` (legacy `store` → `pick`), `buffer`, `reserve`, `damaged`; brak/nieznany → `unknown` (szary, nie primary). Frontend: `types/warehouse.ts` (`NormalizedStorageType`), `storageTypeColors.ts`, `storageTypes.ts`, `storageTypeIcons.tsx` (unknown = HelpCircle). Backend: `storage_types.py`, `layout_bin_storage_type` przy zapisie layoutu; `GET /products/` — `storage_type` z `warehouse_bins` po `location_uuid` (`product.py`). Metryki PDF/report: `pick` + `unknown` w capacity; value report 3-kubłowy grupuje pick/buffer/unknown do primary.

- **Product ↔ inventory refactor (flags):** `backend/config/product_refactor_flags.py` — env `DISABLE_ASSIGNED_LOCATIONS_INVENTORY_SYNC`, `DISABLE_STOCK_QUANTITY_INVENTORY_WRITE`, `ENABLE_LEGACY_BRIDGE_APPLY_PLAN` (default false). `product.py`: structured logs on update; conditional sync; 400 on `stock_quantity` when write flag on; `_sync_inventory_from_assigned_locations` guard + `bypass_sync_disable` for bridge; deprecation warning on legacy warehouse 1 / Import path; `POST /products/{id}/apply-assigned-locations-to-inventory/`. `import_service.py`: optional skip of `_move_inventory_from_import_to_assigned` when sync flag on.

- **Szablon etykiety — root → repeater.template:** `frontend/src/labelSystem/moveRootElementsIntoRepeaterTemplate.ts` (pure TS); CLI `scripts/move_root_into_repeater_template.py` (plik JSON lub rekord z `template_json`). Jeden repeater na root; pozostałe elementy root przesuwane o `(-repeater.x,-repeater.y)` i doklejane do `repeater.template.elements`; root zostaje `[repeater]`.

- **Label designer — repeater tylko przez konwersję:** usunięty pusty „Powtarzacz” z lewego panelu (zostaje Rack builder z gotową zawartością). Konwersja od 1 elementu głównego; Ctrl/⌘+klik na płótnie = wielowybór; `idsForConvert` = multi z Warstw lub pojedyncze zaznaczenie.

- **Label designer — „Konwertuj na repeater”:** wielokrotny wybór w panelu Warstwy (Ctrl/⌘+klik); przycisk pod listą warstw owija zaznaczone **elementy główne** w jeden `repeater` (`dataset: "levels"`, bbox → `x/y/width/height/itemWidth/itemHeight`, dzieci przesunięte względem min), bez zmian silnika layout/render.

- **Label designer — dodawanie do repeatera:** `addElement` kieruje nowe elementy do `repeater.template.elements`, gdy zaznaczony jest repeater lub element w jego szablonie (`resolveTargetRepeater` + `findRepeaterOwningElement` dla grup/zagnieżdżeń); `insertIntoRepeaterTemplate` rekurencyjnie (group + zagnieżdżone repeatery).

- **Kolejka druku — regał:** `rackLabelDataset.ts` (`transformLocations` row/column/sequential, `chunkItems`, `pipelineFlatLocationsToRepeaterRecords`, self-test); UI „Lokacji na etykietę” + tryb kolejności + opcjonalna liczba kolumn; `buildRecordsLikeRackLabelModal` stosuje prepare przed `chunkDataset`; PDF regału pobiera szablon i wysyła pocięte rekordy; licznik/podgląd wg `locationPageRecords` / `rackPageRecords`.

- **Druk etykiet / wiele lokacji na jednej kartce:** `utils/labels/chunkDataset.ts` — dzielenie listy na rekordy z `[datasetKey]: slice`; `LabelPrintQueue` `buildRecordsLikeRackLabelModal` używa `chunkDataset`; `getSlotsPerLabelLikeRack` — siatka: kolumny × wiersze wg wys. etykiety; pion: wg wysokości; poziom: wg szerokości (bez zmian silnika layoutu).

- **Repeater — brak duplikacji przy wielu powtarzaczach:** layout (`labelLayoutEngine` + `label_engine._compute_layout_items`) dokleja `dataset_index` (przed filtr/sort) i `repeater_slot` (po filtrze/sorcie) do rekordu slotu; walidacja szablonu ostrzega przed rodzeństwem z tym samym `dataset` bez filtra; `ElementProperties` — pola Filtr / Sortuj. Wzorzec A: jeden repeater + `visibleIf` z `{repeater_slot}`; B: osobne repeatery z `filter` np. `{dataset_index} == 0`.

- **Label presets / rack builder / PNG import:** repeater `template.elements` — jeden root `group` (wymiary = `itemWidth` × `itemHeight`), dzieci bez zmian `x/y`; typ `RepeaterElement.template.elements` → `(LabelElement | GroupElement)[]` w `labelSystem.ts`.

- **Label Template Designer — repeater UX:** `flattenOverlayEntries` — liczba slotów z `record[repeater.dataset].length` (spójnie z podglądem SVG). Zaznaczenie `{ id, slotIndex }` (`pickTopElementAtCanvasPx` + ALT ignoruje top), podświetlenie tylko wybranego slotu, chip „Repeater item #n”, inspector przez `overlayEntries.find` po `id`+`slotIndex`, hint przy przeciąganiu dziecka repeatera, log `[REPEATER CLICK]`.

- **Label Template Designer — panel Warstwy:** `components/label/LayersPanel.tsx` — lista top-level `template.elements` (góra = z przodu), wybór, ↑/↓ z przeliczeniem `zIndex` = indeks, przełącznik `visible` (ukryte: brak w layoutcie/SVG, bez hit-test w designerze). Typy: `visible?` na bazie etykiety / grupa / repeater; `flattenElements` + `_compute_layout_items` pomijają `visible === false`.

- **Label Template Designer — interakcja:** Warstwa `data-draggable-wrapper` ma `pointer-events: none` (nie przechwytuje kliknięć). Wybór i start przeciągania przez `pickTopElementIdAtCanvasPx` na `mousedown` płótna (`overlayEntries` + `zIndex` + kolejność spłaszczenia); puste tło czyści zaznaczenie. `getOverlayHitSizePx` — min. 6×6 px dla cienkich linii; siatka: `pointer-events: none` na liniach. Uchwyty zmiany rozmiaru: `data-resize-handle`, `pointer-events: auto`.

## 2026-04-02

- **Import CSV etykiet:** `labelCsvImport.ts` — `AUTO_MAP` (trim+lowercase), fuzzy (`regał`/`półk`/`ean`), `inferCsvFieldForHeader`; `localStorage` `csv_label_mapping` z `{ headers, mapping }` (przywracanie tylko przy identycznej liście nagłówków); `buildColumnMappingWithPersistence` + log `[CSV AUTO MAP]`; `LabelPrintQueue` — upload, przycisk „Automatyczne mapowanie” (`forceAuto`), zapis przy zmianie selecta.

- **Label engine / PDF:** Tekst — metryki `asc`/`desc` (`_font_vertical_metrics_pt`); middle: `y_metric = y_pt + h_pt/2 - text_height/2 + desc`, potem `y_baseline = y_metric - OPTICAL_CENTER_FACTOR×font_size` (domyślnie 0.08); top/bottom bez offsetu optycznego.

- **Etykiety (UI):** `ColorPicker` — wyłącznie `label_user_colors` (brak presetów); pusty stan: tylko „+”; kolor z szablonu spoza palety → kafelek „orphan” (obramowanie przerywane); popover + „Zastosuj” dopisuje do localStorage.

- **Label engine / PDF:** Tekst — `_text_baseline_y_in_box`: `text_height_pt = font_size * 0.7`, środek pionowy: `y_baseline = y_pt + h_pt/2 - text_height_pt/2`; użyte w `_draw_text`, `_draw_static_text_layout`, legacy `render_elements` (text/statictext). Oś pozioma: `align` lub `horizontalAlign`.

- **Label engine / PDF:** `_draw_rectangle` — kolor z layoutu: `fill` przed `backgroundColor`/`color`; brak ponownej oceny `conditions` w rysowaniu (usunięto `_resolve_color`). Warunkowe `fill` z `compute_layout` jest używane w PDF.

- **Label engine:** `_rect_conditions_from_element` — obsługa `conditionalStyles` oraz `conditions`; normalizacja `when`/`color`/`borderColor`; `_compute_layout_items` + `_draw_rectangle`.

- **Label engine:** `_evaluate_condition` — RHS niecytowany: próba `float`/`int`, inaczej token jako string; `==`/`!=` przez `_as_cmp_string` (strip, spójne int/float); debug `print("COND:", ...)`.

- **CSV etykiety — warunki:** `_normalize_record_for_bindings` używa `key not in record` (nie `not record.get(key)`), żeby nie nadpisywać `loc_name` itd. pustym aliasem z `location_name`; sync tylko braced↔bare gdy drugi klucz brakuje; `render_label_template` loguje `FINAL RECORD` per rekord.

- **Szablony etykiet — preview vs PDF (layout):** `flattenElements`: repeater używa `childRecord = { ...record, ...itemData }` jak backend; `label_engine.compute_layout` sortuje top-level `elements` po `zIndex`; repeater `ih`: `itemHeight or itemWidth or height or 20` (także w legacy `render_elements` / `_render_elements_svg`); `renderText.ts`: baseline/rozmiar jak `_draw_text` (pt + `POINTS_PER_MM`), bez `dominant-baseline="middle"` dla zwykłego tekstu.

- **Szablony etykiet:** `LabelTemplatesList` — podgląd hover: snapshot `currentTarget` przed `setTimeout`, guard `!el / !isConnected` w `showPreview`, cleanup timerów przy unmount; SQLite — `PRAGMA foreign_keys=ON` na połączeniu; `PUT`/`POST` label-templates walidują `group_id` (tenant + `template_type`) i `IntegrityError` → 400 zamiast 500.

- **CMP — dziennik zdarzeń (czytelność):** Krótsze opisy w `complaintAuditHumanize.ts` / `complaintStructuredHumanize.ts` (np. decyzja = sama etykieta Zwrot/Wymiana/… + kontekst po „—”); w tabeli `ComplaintDetailPage` pogrubiona tylko kolumna akcji, data mniejsza i stonowana, obramowanie wierszy + hover, jedna linia z `truncate` + `title`; `dedupeComplaintHistoryRows` (okno 8 s, `dedupeKey` na typach poziomu sprawy / linii / przesyłki) scala szybkie powtórzenia do ostatniego wpisu.

- **CMP — dziennik zdarzeń (gęstość + paginacja):** `ComplaintDetailPage` — tabela `table-fixed` (Data ~180px, Użytkownik ~120px, Działanie elastycznie), `text-xs` / `py-1.5` / `min-h-[36px]`; selektor „Na stronę” 10/25/50 (domyślnie 25), nawigacja stron; legacy `audit_events` z filtrem `type !== "decision_update"` (jeden wpis decyzji — `line_updated` / humanize).

- **CMP — oś czasu: statusy i przesyłki:** Backend: `COMPLAINT_PROCESS_STATUS` (tylko `status` w payload, deduplikacja), `LINE_PROCESS_STATUS` (kody akcji, deduplikacja per linia), `SHIPMENT_STATUS` z `shipment_id` + deduplikacja; zapis przy zmianie przesyłki (`patch_complaint_shipment`), utworzeniu CUSTOMER/SERVICE/OUTBOUND. Front: przy niepustym `complaint_events` — tylko ten log (bez duplikatów z API przesyłek); `complaintStructuredHumanize.ts` + role-aware komunikaty przesyłek; audyt legacy bez surowych `message` i bez „z → do” dla statusów.

- **CMP — `complaint_events` (structured log):** Tabela `complaint_events` (UUID, `complaint_id`, opcjonalnie `line_id`, `event_type`, `payload_json`, `created_at`, `actor` domyślnie System); indeksy `complaint_id`, `created_at DESC`, złożony `(complaint_id, created_at DESC)`. Dual-write z `append_complaint_audit_event` (payload = meta, bez polskiego `message`). API: `complaint_events` w `ComplaintRead`, `GET /complaints/{id}/events` (paginacja). Stałe typów w `complaint_event_types.py`.

- **CMP — oś czasu (audyt czytelny):** `complaintAuditHumanize.ts` — mapowanie typów `audit_events` na polskie opisy (decyzje, operacje, logistyka, dokumenty, zdjęcia, rozliczenie); format `data — System — **akcja**` + kontekst pozycji; sort malejąco po czasie. Backend: przy zapisie rozliczenia linii (bez zmiany decyzji) wpis `line_settlement_saved`. UI: lista w `ComplaintDetailPage`.

- **CMP — auto rozliczenie zwrotów:** Backend `ComplaintOrderSummary.shipping_cost` (z `_shipping_cost_from_order`). Front: tylko decyzja „zwrot” ma formularz kwoty; pełny zwrot — auto `unit_price × quantity`; wymiana/odrzucenie — komunikat bez rozliczenia; `buildLineDecisionPatch` bez `settlement_type` dla wymiany. Podsumowanie globalne: suma zwrotów produktów, zwrot wysyłki (raz gdy jest zapisany zwrot pozycji), łącznie.

- **CMP — unified lightbox zdjęć linii:** Nowy `ComplaintLinePhotoLightbox.tsx` — pełny ekran, nawigacja strzałkami i klawiaturą po **wszystkich** zdjęciach linii (kolejność: klient → magazyn), zoom kółkiem, przeciąganie przy powiększeniu, reset zoom przy zmianie zdjęcia, ESC / klik tła / X, etykieta Customer/Warehouse, licznik, pasek miniaturek (`buildComplaintLinePhotoList` + integracja w `ComplaintLinesDecisionsPanel`).

- **CMP miniatury zdjęć (brak przycinania ramek):** W `ComplaintLinesDecisionsPanel` usunięto `overflow-x-clip` z kolumn przewijanych zdjęć; padding `px-1.5`; kafelki jako `flex` + `box-border`, obramowanie na przycisku, `ring-inset` dla wyboru i `focus-visible`; wewnętrzny biały panel z `overflow-hidden` tylko pod `object-contain`; `LineProductThumb` — ten sam wzorzec ramki + wewnętrzny holder.

- **CMP — duplikaty URL w `complaint_lines.photo_urls_json`:** Backend: `dedupe_complaint_photo_urls_preserve_order` + `complaint_photo_url_dedupe_key` w `schemas/complaint.py`; `complaint_photo_urls_from_db` zwraca już listę unikalną po kluczu (ścieżka `/uploads/…` vs ten sam zasób jako URL absolutny). Merge w `POST /photos` (complaint + line), `POST /wms-update`, `_apply_line_photo_uploads` i `merged_photos` w `from-order` używa deduplikacji. Front: `ComplaintLinesDecisionsPanel.uploadAndAttachToLine` nie wywołuje już `wms-update` po `uploadComplaintPanelPhotos` (serwer zapisuje linię w `/photos`); stan zdjęć zsynchronizowany z odpowiedzią API.

- **`POST /complaints/{id}/photos` — jedna zapisana lista na linię:** `merge_photo_url_strings_idempotent` (kolejność: najpierw istniejące, potem nowe, jeden slot na klucz `complaint_photo_url_dedupe_key`). Po multipart: `saved_raw` → `saved` przez ten sam merge (oszczędza zduplikowane części `photos`). Jedno przypisanie `target_line.photo_urls_json` na żądanie. Logi `logger.debug` (`complaint_panel_photos`): liczba części multipart, unikalne incoming, oraz `line_before` / `line_after` / `merged_urls`.

## 2026-04-01

- **CMP — decyzja steruje rozliczeniem (UI):** `complaintLineSettlement.ts` — mapa typów rozliczenia per `line_decision`, agregacja podsumowania z linii. `ComplaintLinesDecisionsPanel`: przy wymianie PATCH z `settlement_type: REPLACEMENT`, przy odrzuceniu `REJECTION`; sekcja rozliczenia ukryta dla naprawy; dla zwrotu tylko radio pełny/częściowy + kwota/waluta (bez „brak”); dla wymiany/odmowy tekst konsekwencji decyzji. `ComplaintDetailPage`: „Rozliczenie z klientem” jako karta tylko do odczytu (liczby zwrotów/wymian/odmów, suma zwrotów z `settlement_*`); usunięto globalny zapis `patchComplaintResolution` / modal potwierdzenia.

- **CMP — UX spójność line-centric (bez zmian API):** Podsumowanie rozliczenia vs pozycje (nowe etykiety, democja stylu, helper text); lista blokujących pozycji przy zamknięciu + PL komunikat; „Następny krok” w blokach operacji; badge odbioru w pierwszej pozycji; helper przy osi formalnej; ujednolicenie zdjęć (hover, usuń, dodaj).

- **Reklamacje — architektura per-linia (część 2):** Backend: decyzja linii `refund`, łańcuch operacji `pickup → warehouse_in → refund_done`, akcja `REFUND_COMPLETED` w `complaint_line.py`; walidacja przy przejściu statusu na `ZAAKCEPTOWANA` / `ODRZUCONA` — każda linia musi mieć decyzję i ukończone operacje. Odczyt/patch rozliczenia pozycji (`settlement_*` w `ComplaintLinePatch` / `ComplaintLineRead`), `parent_complaint_id` + `parent_complaint` / `child_complaints` w `ComplaintRead`. Migracja SQLite: `complaints.parent_complaint_id`, kolumny rozliczenia na `complaint_lines`. Frontend: przycisk Zwrot, blok operacji zwrotu, rozszerzone `complaintLineOperations`, per-linia sekcja rozliczenia w `ComplaintLinesDecisionsPanel`, karta powiązanych reklamacji w `ComplaintDetailPage`.

- **WMS — brak podwójnego uploadu zdjęć (CMP panel):** W `ComplaintLinesDecisionsPanel` poll sesji telefonu/QR miał w `useEffect` zależności od `linePhotosById` / `notatek` — każdy merge zdjęć odpalał efekt ponownie i `tick()` mógł przetworzyć te same URL-e dwa razy. Zmniejszono deps do `phoneUploadSession` (+ stabilne `save` / `warehouseId`), dodano refy dla aktualnych map zdjęć/notatek, `phoneSessionPollBusyRef` (brak nakładających się ticków), `panelUploadLockRef` w `uploadAndAttachToLine`. Kolejność: najpierw `saveLineWarehouseData`, potem aktualizacja `seenUrls` w sesji (żeby cleanup efektu nie ustawiał `cancelled` przed zapisem). Bez zmian API ani backendu.

## 2026-03-31

- **ComplaintLinesDecisionsPanel (layout):** Usunięto górny blok „Klient i zamówienie”; układ pozycji: kolumna główna `flex flex-col gap-4` (blok produkt+decyzje, potem `grid` 2 kolumny: zdjęcia klient/magazyn z main `h-[260px]` + miniatury `flex gap-2`, operacje + notatka + „Dodaj zdjęcie” `h-12`); prawa kolumna `lg:w-72` — zwięzły grid klient/zamówienie, select statusu, „Zapisz reklamację”. Bez zmian API ani pól.

## 2026-03-30

- **Complaint detail — integracja operacji i zdjęć per pozycja:** `ComplaintLinesDecisionsPanel` ma teraz układ 2 kolumn (`2fr/1fr`) dla aktywnych operacji: lewa kolumna = dotychczasowy blok operacji (bez zmian logiki), prawa = panel „Zdjęcia magazynowe” z ikoną kamery, miniaturami 60x60, pustym stanem, notatką magazynową per item oraz podglądem lightbox (next/prev/close).
- **Flow zdjęć per item (bez nowego UX):** kamera/telefon QR/kolektor zostały osadzone bezpośrednio w panelu pozycji, z użyciem istniejącego WMS photo-upload (`/wms/photo-upload/session`) i zapisem wiązania zdjęć do konkretnej linii przez `wmsUpdateComplaintItems`.
- **Usunięta globalna sekcja zdjęć magazynowych:** z `ComplaintDetailPage` usunięto całkowicie duplikujący blok „Zdjęcia magazynowe”; zdjęcia są widoczne tylko przy właściwej pozycji w sekcji operacji.

- **Rozdział zdjęć klient vs magazyn (CMP):** backend `Complaint` ma osobne pole `warehouse_photo_urls_json`; upload `POST /complaints/{id}/photos` zapisuje teraz wg `photo_kind` (customer → `photo_urls_json`, warehouse → `warehouse_photo_urls_json`). `ComplaintRead` zwraca niezależnie `customer_photo_urls` i `warehouse_photo_urls`.
- **Per-item dane magazynowe:** `ComplaintLine` używa `note_warehouse` + `photo_urls_json` (jako `warehouse_photos` w API); `POST /complaints/{id}/wms-update` zapisuje `note_warehouse` i zdjęcia per pozycja. Frontend CMP WMS wysyła `note_warehouse` w payload.
- **UI separation:** `ComplaintDetailPage` sekcja „Zdjęcia od klienta” korzysta z `customer_photo_urls`; sekcja „Zdjęcia magazynowe” renderuje tylko źródła magazynowe (`warehouse_photo_urls` + `lines[].warehouse_photos`) i nie miesza ich z klientem.

- **CMP WMS — trwały zapis danych inspekcji:** backend dostał endpoint `POST /complaints/{id}/wms-update` (tenant/warehouse scoped) zapisujący per-pozycja `warehouse_note` + przypięte zdjęcia (`photo_urls_json` na `complaint_lines`), wraz z wpisem audytu `wms_update`. Dodano migrację kolumny `complaint_lines.warehouse_note` i ekspozycję pola `warehouse_note` w `ComplaintLineRead`.
- **CMP WMS — realny save button:** `Zapisz reklamację` w `WmsComplaintDetailPage` wywołuje teraz `wmsUpdateComplaintItems(...)` i zapisuje notatki + referencje zdjęć per item; toast `Zapisano` pozostał jako feedback.
- **CMP WMS — wiązanie zdjęć z pozycją:** po uploadie zdjęcia w WMS refs (`/uploads/...`) są przypisywane do konkretnej pozycji (`photoRefsByLine`) i wysyłane w `wms-update`; nie są już tylko lokalnym podglądem.
- **Complaint detail — „Zdjęcia magazynowe” z backendu:** sekcja w `ComplaintDetailPage` pokazuje teraz trwałe zdjęcia zapisane na liniach reklamacji (`data.lines[].photo_urls`, z etykietą pozycji), obok ewentualnych lokalnych preview.

- **WMS CMP — flow zdjęć + zapis:** `WmsComplaintDetailPage` dostał modal „Dodaj zdjęcie” z tym samym operacyjnym schematem jak RMZ (3 kanały: 📷 kamera, 📱 telefon/QR z sesją `wms/photo-upload`, 📦 kolektor/mobile capture), polling sesji QR i natychmiastowe miniatury na kartach produktu. Dodano przycisk `Zapisz reklamację` w nagłówku karty zamówienia (toast „Zapisano”); notatki per produkt pozostają lokalne (auto-persist w `localStorage`), bez decyzji/finansów/logistyki.

- **WMS — szczegóły reklamacji (CMP) w trybie operacyjnym:** dodano nowy widok `frontend/src/pages/wms/WmsComplaintDetailPage.tsx` pod trasą `/wms/returns/complaints/:complaintId` (wpięty w `App.tsx`, helper `WMS_ROUTES.complaintsProcess`). Klik kafelka CMP na wejściu WMS (`WmsReturnsEntryPage`) otwiera teraz ten widok zamiast pełnego panelu biurowego.
- **Zakres widoku CMP (inspection-only, bez zmiany logiki):** top z kartą zamówienia + read-only status reklamacji, środkowa lista pozycji (miniatura, nazwa/EAN/SKU, ilość), tagi powodów (`defect_ids` + `reason`), wyłącznie akcja „Dodaj zdjęcie” (multi-upload przez istniejące `uploadComplaintPanelPhotos(..., "warehouse")`) z miniaturami i statusem wysyłki, oraz lokalna „Notatka magazynowa” (localStorage).
- **Świadomie pominięte sekcje w WMS CMP:** brak decyzji finansowych/operacyjnych, brak logistyki, brak „Przyjmij wszystko”, brak korespondencji i innych akcji klient/backoffice.

- **WMS — wejście zwrotów (`WmsReturnsEntryPage`):** formularz „Nowy zwrot” przeniesiony z trwałej prawej kolumny (`lg:grid-cols-3`) do panelu wsuwiego z prawej (~400px, overlay + blur, zamknięcie: X, klik w tło, Esc); główny widok po wyborze zamówienia: skan, karta zamówienia, kafelki RMZ/CMP — bez formularza do czasu kliknięcia „+ Nowy zwrot”. Logika `createWmsReturn` / pola bez zmian.

- **WMS — jeden wejściowy flow Zwrot / Reklamacja (tylko UI):** zakładka i nagłówek `Zwrot / Reklamacja`, sekcja po wyborze zamówienia „Zwroty / Reklamacje”, brak CTA „Nowy zwrot” przed wyborem zamówienia; karta zamówienia przewija do istniejącego formularza RMZ; lista kafelków: niebieskie RMZ + fioletowe reklamacje (`listComplaints` + filtr `order_id`, klik → `/complaints/:id`); bez tworzenia reklamacji w WMS. Usunięty efekt zamykający formularz przy każdej obecności zwrotów (psuł kartę zamówienia i „Dodaj kolejny zwrot”).

- **Reklamacja — zamówienie wymiany tylko z operacji (modal):** po wyborze trybu „Wymiana + odbiór” / „Tylko dostawa” otwiera się `ComplaintExchangeOrderSection` w modalu (`hideComplaintOrderTypeSwitch`); z rozliczenia usunięto CTA „Przejdź do nowego zamówienia” i formularz; REPLACEMENT w rozliczeniu = tylko decyzja finansowa (tekst w UI). Fallback bez strony szczegółów: `goExchange` → `/orders/new` z prefill.

- **Reklamacja — zamówienie wymiany inline (hist.):** po wyborze „Wymiana” na pozycji API ustawia `operational_decision=exchange` (sync z `ComplaintLinesDecisionsPanel`); prefill, `POST /orders/` z `complaint_order_type`; wcześniej inline pod `isOperationalExchangePath`, potem przeniesione do modalu wyłącznie ze ścieżki produktów.

- **Dokumenty reklamacji (PDF):** model `ComplaintDocument` (typ `DECISION` | `CORRECTION` | `RMA`, `file_url`, `created_at`), tabela + migracja SQLite; PDF (ReportLab) zapisywane pod `/uploads/complaints/{id}/documents/`; **DECISION** przy przejściu na `ZAAKCEPTOWANA` / `ODRZUCONA` (w tym auto-akceptacja terminu); **CORRECTION** przy `resolution_type` REFUND / PARTIAL_REFUND; **RMA** przy pozycji z `line_decision=repair` (grupy producentów, wady, miniatury zdjęć z dysku); `POST /complaints/{id}/documents/regenerate`; pole `documents` w `ComplaintRead`; front: sekcja „Dokumenty” + pobieranie + regeneracja.

- **Rozliczenie z klientem (backend + UI):** `complaints` — pola `resolution_type`, `resolution_status`, `resolution_amount`, `resolution_currency` + migracja SQLite; `PATCH /complaints/{id}/resolution` (walidacja kwot vs zamówienie źródłowe, te same „unlock” co zwrot/odmowa); **tylko przy statusie `ZAAKCEPTOWANA` lub `ODRZUCONA`** (HTTP 400 w przeciwnym razie); audyt `resolution_set`, `refund_created`, oraz przy `POST /orders/` z `origin=COMPLAINT` — `replacement_order_created` + domknięcie `REPLACEMENT`; blokada nowego zamówienia wymiany gdy rozliczenie finansowe zakończone. Front: sekcja w `DECYZJA` + statusach końcowych; w `DECYZJA` wybór/kwota bez zapisu; zapis po zamknięciu; kwota + waluta w jednym rzędzie; przycisk zapisu nieaktywny wizualnie + `title` / klik → komunikat, gdy brak uprawnień.

- **Complaint UX polish (UI-only):** strengthened deadline banners (prominent overdue state), timeline active-step emphasis (larger node + thicker active connector), clearer product/decision styling in line cards (larger name, qty badge, semantic button colors), exchange flow selection switched from radios to step-like action cards, detail view financial/major-defect visual polish, and list-row urgency/hover/quantity-badge/action-disabled improvements. No backend/API logic changes.

- **GET /complaints `sort_by=deadline_urgency`:** Fixed 500 caused by raw SQL `ORDER BY` using non-existent column `complaints.status` (real column: `complaint_process_status`). Replaced with ORM `case()` / `Complaint.status` + julianday day-diff; unknown `sort_by` falls back to `created_at DESC`. Added `backend/tests/test_complaint_deadline_sort.py`.

## 2026-03-28

- **Reklamacje — lista i audyt:** API listy: `accepted_by_law`, `response_deadline_days_remaining`, `is_overdue`; domyślne sortowanie `deadline_urgency` (SQLite); POST ` /complaints/{id}/photos` (customer | warehouse | defect_evidence) + audyt; PATCH decyzji z `defect_ids`; audyt przy auto-akceptacji terminu, operacji linii (`/complaint-lines/.../operation`), zmianach linii (`line_update`), zdjęciach przy tworzeniu; UI: badge „Uznana automatycznie”, termin na liście, podświetlenie wierszy, zapis tagów wad / upload zdjęć z panelu.

- **Reklamacje — produkcyjne domknięcie (UI + przesyłka):** status `OCZEKIWANIE_NA_PRODUKT` w typach/etykietach/filtrach/osie czasu; baner terminu 14 dni nad przebiegiem (`prominent` + pola API `response_deadline_*`, `accepted_by_law`); oś „Oś czasu” scala `audit_events` z API; banery oczekiwania na produkt / przypomnienie ≥7 dni; `ComplaintShipmentTransportSection` z `ref.openPickupModal()` — wywołanie po wyborze „Wymiana + odbiór”; backend: audyt `courier_ordered` przy `POST` przesyłki od klienta (`complaint_shipment.py`).

- **Reklamacja — wymiana (sekwencja UI):** blok „Wymiana — nowe zamówienie” usunięty z kolumny Decyzja; tryb (`EXCHANGE` / `REPLACEMENT`) w **Operacjach** (`ComplaintLineOperationsBlock` + `exchange_kind` w PATCH); typy/API front: `ComplaintLineDetail.exchange_kind`, `ComplaintLinePatchPayload.exchange_kind`.

- **Reklamacja — operacje per pozycja:** `complaint_lines.operation_status`; łańcuchy: naprawa (5 kroków), wymiana (odbiór → zamówienie → wysyłka), odrzuć (odbiór → zwrot); `PATCH /complaints/{id}/lines/{line_id}` z `operation_status`; przy zmianie `decision` kasowanie operacji; UI: oś czasu + przyciski „następny etap”, wymiana: skróty do zamówienia + potwierdzenie; naprawiona kolejność argumentów `patchComplaintLine` w panelu.

- **Reklamacja — odbiór od klienta:** istniejąca przesyłka `CUSTOMER` + `COURIER_PICKUP` = kierunek INBOUND / typ PICKUP w odpowiedzi API; modal „Odbiór od klienta” (dane z zamówienia, przewoźnik, data, notatka); po `DELIVERED` automatycznie `complaints.logistics_status = IN_INSPECTION` (Przyjęto do inspekcji); sekcja widoczna dla każdej reklamacji z liniami; panel — przyciski kolejnych etapów przesyłki (MVP).

- **Reklamacje — wiele produktów (per linia):** `complaint_lines.line_status` / `line_decision`; statusy linii niezależne od siebie (`NOWE` … `ODRZUCONA`); decyzje na linii: `repair` / `exchange` / `reject`; API `PATCH /complaints/{id}/lines/{line_id}`; szczegóły: `ComplaintLinesDecisionsPanel` (grupowanie wizualne po producencie z katalogu), bez globalnego bloku „jedna decyzja operacyjna”; zamówienie z reklamacji z `complaintLineId` w prefill po utworzeniu ustawia `decision: exchange` na tej linii.

- **Reklamacja — dwie ścieżki zamówień:** „Wymiana” (`exchange` + `complaint_order_type=EXCHANGE`) vs „Wysłanie nowego” (`replacement` + `REPLACEMENT`); po `POST /orders/` z `origin=COMPLAINT` wymagany `complaint_order_type`; automatyczna przesyłka `complaint_shipments` z `shipment_role=OUTBOUND`, `shipment_business_type`, `fulfillment_mode` (`DELIVERY_AND_PICKUP` / `DELIVERY_ONLY`); API GET shipment zwraca `outbound_shipment`; UI: osobne przyciski + panel „Nadanie do klienta”.

- **Reklamacja — wymiana (nowe zamówienie):** decyzja operacyjna `exchange` zamiast „zwrot do dostawcy”; przycisk otwiera istniejący formularz `/orders/new` z prefill (`complaintExchangePrefill`): klient, adres, linie z `product_id`; zapis tworzy zamówienie `origin=COMPLAINT` + `complaint_id` + `original_order_id`, potem PATCH reklamacji (`operational_decision: exchange`). UI: banner „Zamówienie z reklamacji” na tworzeniu, badge + linki na `OrderDetailPage`.

- **Reklamacje — lista panelu (UX):** `complaints.deleted_at` + `DELETE /complaints/{id}/` (soft delete); lista z miniaturą pierwszej pozycji (`product_image_url`), `#id` + referencja drugorzędnie, jedna linia zamówienia, badge z dokładnym statusem PL (`complaintStatusBadgeClass`); ikona kosza + modal (EN) + odświeżenie listy i podsumowania.

- **Reklamacje — tylko z zamówienia:** `complaints.order_id`, `photo_urls_json`, tabela `complaint_lines` (FK `order_items`); `POST /complaints/from-order` z walidacją; `GET` zwraca `order`, `lines`, auto `customer_*` z zamówienia; stary `POST /complaints/` → 400. Kreator 3 kroków (`NewComplaintWizard`): lista zamówień, wybór pozycji, notatka / powód / opcjonalne zdjęcia (base64); wejścia: zamówienie (wiersz + nagłówek), lista zamówień, panel reklamacji. Redirect `/orders/complaints/:id` (trasa pod `OrdersLayout`); szczegóły: pozycje + link do zamówienia.

- **Panel — przesyłki reklamacji (MVP):** tabele `complaint_shipments` + `complaint_shipment_events`; API pod `/api/complaints/{id}/shipment` (GET/POST/PATCH), etykieta PDF GET `.../shipment/label`; numer `MOCK-*`; statusy ORDERED → PICKED_UP → IN_TRANSIT → DELIVERED / CANCELLED + wpisy na osi czasu (PL). UI: kolumna prawa „Transport / Odbiór”, modal zamówienia (sposób, przewoźnik, dane, data), przyciski etykieta / śledzenie / anuluj + symulacja postępu.

- **Panel — szczegóły reklamacji (`/complaints/:id`):** siatka 3 kolumn (30% / 40% / 30% na `lg`): lewo klient + zamówienie + galeria zdjęć klienta; środek produkt (obraz ~120px, nazwa, SKU), chipy wad z opcjonalnym uploadem zdjęć, zdjęcia magazynowe (upload + siatka podglądu); prawo duże przyciski decyzji operacyjnej i finansowej (zapis lokalny + wpis na osi czasu); nad siatką pełna szerokość: status panelu jako chipy (PATCH jak dotychczas); na dole oś czasu (utworzenie + zdarzenia z interakcji). Typ `ComplaintDetail` rozszerzony o opcjonalne pola pod przyszłą integrację (klient, źródło zamówienia, produkt, URL zdjęć klienta).

- **Panel — szczegóły RMZ (`/orders/returns/:id`):** układ 2 kolumn (pozycje ~50% / prawa oś: podsumowanie + klient + notatki localStorage + komunikacja localStorage); wiersz produktu poziomo (miniatura, nazwa+SKU, ilość+cena, inline Przyjmij/Uszkodzony/Odrzuć + Zapisz); tryb advanced — kompaktowy wiersz ilości; dziennik + zwięzłe załączniki na dole strony; etykiety PL zamiast OK/DAMAGED/REJECTED w UI; typ zwrotu z `refundTypeLabelPl`.

- **Panel RMZ — densja + kontekst:** miniatury 56–64px; mniejsze paddingi/gapy; blok „Finanse i klient” (zwrot + klient); „Dane do przelewu” z `addresses_json` zamówienia; „Dokument sprzedaży” + „Pobierz” (txt ref.); „Korekta” (status + link zamówienie + pobierz txt-info); usunięta osobna duża karta Klient.

- **Panel — nawigacja i układ:** `OrdersLayout` pod zamówieniami tylko „Nowe / Lista / Import” (bez zwrotów); `ComplaintsLayout` + `PageLayout`: „Reklamacje” + „Reklamacje — statusy” → ustawienia; `OrderList` i `ComplaintsPanelPage` pełna szerokość jak zwroty, wiersze 4-kolumnowe (`lg:gap-8`, `py-2.5`); `formatRelativeAgo` w `utils/`.

- **`OrdersLayout`:** `/orders/returns*` — „Zwroty” + 2 akcje zwrotów; reszta `/orders/*` — „Zamówienia” + Nowe/Lista/Import; `compactSpacing` na liście zwrotów i liście zamówień.

- **Panel zwrotów — lista:** pełna szerokość (bez `max-w-6xl`), wiersz `lg`: 4 sekcje (lewo: meta+select panelu, środek: produkty, środek-prawo: klient+źródło, prawo: kwota+czas+RMA+ikony), `py-2.5` `gap-8`, miniatury 7×7.

- **Panel zwrotów (`/orders/returns`):** układ 3 kolumn (lg, stack na mobile): lewo — data, RMZ, zamówienie, status RMZ, klient/źródło, select panelu; środek — produkty `Nx` + pełna nazwa (`break-words`); prawo — kwota PLN (`total_refund_amount` / legaci, min. 0,00) `text-lg font-semibold`, czas względny, badge typu zwrotu, ikony w rzędzie; `gap-8` między sekcjami.

- **Panel — sidebar podstatusów:** wiersze pod grupą NEW/IN_PROGRESS/DONE używają `substatus.color` (hex z DB) na tło (ok. 12–22% alpha), lewą obwódkę i badge; brak/walidacja → fallback RGB grupy (`panelSidebarHierarchy.ts`); `ReturnsListPanel`, `OrderList`, `ComplaintsPanelPage`.

- **Reklamacje — panel UI statusów:** tabele `complaint_ui_statuses` (tenant, bez warehouse) + `complaints` z `complaint_ui_status_id`; API `GET/POST /complaints/`, `GET /complaints/{id}/`, `office/complaint-ui/` (summary z `warehouse_id` tylko do liczników, CRUD podstatusów po `tenant_id`, `PATCH .../complaints/{id}/ui-status`); domyślne podstatusy (EN) przy pierwszym `summary`. UI: `/complaints`, szczegóły `/complaints/:id`, ustawienia `/settings/complaints/ui-statuses`.

- **Panel UI — kolory HEX:** `return_ui_statuses.color` i `order_ui_statuses.color` przechowują `#RRGGBB`; API waliduje format; migracja w `schema_upgrade` mapuje stare nazwy (green/blue/gray/…) na hex; frontend: picker + pole hex, chipy z `backgroundColor` + kontrast tekstu (luminancja WCAG).

- **Panel — statusy zamówień (osobno od `Order.status`):** model `OrderUiStatus` + `orders.order_ui_status_id`; API office `/api/office/order-ui/` (summary, CRUD podstatusów, `PATCH .../orders/{id}/ui-status`); lista `GET /orders/` z `panel_order_ui_*` i polem `order_ui_status`. UI: sidebar + filtry + dropdown na `OrderList`, select na `OrderDetailPage`, ustawienia `/settings/orders/ui-statuses` (nav: Zamówienia — statusy panelu); tenant/magazyn jak zwroty (`DAMAGE_TENANT_ID` + kontekst magazynu).

- **Panel — statusy zwrotów (osobno od WMS):** tabela `return_ui_statuses`, FK `wms_order_returns.ui_status_id`; API office `GET/POST/PATCH/DELETE /api/office/return-ui/...`, `GET /summary` (liczniki + `unassigned_count`), `PATCH /returns/{id}/ui-status`; lista `GET /api/wms/returns/` z opcjonalnymi `panel_ui_status_id` / `panel_ui_unassigned` i polem `ui_status` w odpowiedzi. UI: lewy panel filtrów na `/orders/returns`, przypisanie statusu na liście i w `/orders/returns/:id`, ustawienia `/settings/returns/ui-statuses` (Ustawienia → Zwroty — statusy panelu).

- **Panel — grupy główne zwrotów:** stałe trzy kubełki `main_group` = NEW / IN_PROGRESS / DONE (UI: Nowe zielone, W toku niebieskie, Zakończone szare); `return_ui_statuses` ma kolumnę `main_group`, unikalność nazwy per (magazyn, grupa); `GET /summary` zwraca `groups[]` z `total_count` i `sub_statuses`; filtrowanie listy RMZ przez `panel_ui_main_group` lub `panel_ui_status_id`; przypisanie `PATCH` z polem `sub_status_id` (alias `ui_status_id`); `ui_status` w read ma `main_group`.

- **Lista zwrotów (biuro):** `ReturnsListPanel` pod `/orders/returns` — wiersze Sellasist, `GET wms/returns/` + `lines_preview` w API; WMS `/wms/returns` z powrotem karty RMZ. „Zwroty” w nav → `/orders/returns`; `/returns` → redirect.

- **Lista zamówień (UI):** `OrderList.tsx` — układ wiersza jak panel Sellasist (meta / klient + podgląd max 3 pozycji z miniaturą / kwota + płatność + dostawa / status + ikony); klik wiersza lub `#` → `/orders/:id`; zaznaczenie → lewa obwódka; sortowanie jako chipy; API listy: `OrderListRead` + `items_preview`, `first_name`/`last_name`; sort `order_date` w backendzie.

- **WMS RMZ — zwrot kosztów dostawy:** reguła pełny vs niepełny zwrot (`sum(sztuki RMZ) === sum(sztuki zamówienia)` + zgodność każdej pozycji); pełny → checkbox domyślnie zaznaczony bez potwierdzenia; niepełny → domyślnie odznaczony, zaznaczenie tylko po modalu „Tak, zwróć”; przejście pełny→niepełny (np. zmiana danych) automatycznie odznacza; krótki tekst pomocniczy i licznik sztuk zwrot vs zamówienie.

- **WMS RMZ — typy uszkodzeń (B/C):** po wyborze klasy B lub C operator wybiera typy z chipów (wielokrotny wybór): osobne zestawy dla B i C, bez dropdownów; walidacja przed zapisem (grid, widok szczegółowy, modal). Zapis w `rmz_lines.damage_type` jako posortowane kody po przecinku (`b_*` / `c_*`); API `WmsReturnLineRead` zwraca `damage_type`; `DamageEntryCreate` przyjmuje dłuższy string dla tego samego snapshotu.

## 2026-03-27

- **WMS RMZ — powody odrzucenia zwrotu:** `WmsReturnsPage` — zastąpiono stare powody „stan produktu” listą operacyjną w `<optgroup>` (Zamówienie / Termin / Produkt / Regulamin / Operacyjne); etykieta „Dlaczego odrzucasz zwrot?”; przy „Inny powód” (`ops_other`) wymagane pole tekstowe (`rejectReasonOtherText`) w gridzie i widoku szczegółowym + walidacja zapisu.

- **WMS RMZ — druk etykiety pozycji:** `POST /api/labels/print/return` (`return_line_id`, `template_type: RETURN`) — PDF z szablonu typu RETURN; odpowiedź `WmsReturnLineRead.id` z `rmz_lines`. Grid: menu ⋮ → „Drukuj” — QZ gdy dostępne i zmapowana drukarka, w przeciwnym razie PDF w nowej karcie; brak szablonu → toast.

- **WMS RMZ — nagłówek:** `WmsReturnsPage` — **góra:** status wyśrodkowany (większy badge); **grid 3 kolumny (`lg`):** lewo dane klienta/zamówienia, środek karta „Zwrot kosztów dostawy”, prawo akcje (`items-end`: Korespondencja, ZAPISZ, Przyjmij wszystko); poniżej `lg` jedna kolumna.

- **WMS RMZ — Customer Insights:** Backend `GET /wms/returns/customer-insights` (tenant_id, warehouse_id, opcjonalnie `email` lub `external_id` zamówienia) — zlicza zamówienia z tym samym e-mailem w `addresses_json` oraz RMZ dla tych `order_id`; `return_rate` = zwroty/zamówienia; etykieta ryzyka (&lt;20% / 20–40% / &gt;40%). Frontend: klik w nazwę klienta w nagłówku RMZ → modal ze statystykami.

- **WMS RMZ — hierarchia akcji w nagłówku:** `WmsReturnsPage` — pierwszy wiersz: Korespondencja + blok „Zwrot kosztów:” (checkbox, przy wyłączeniu „nie zwracaj”, przy włączeniu kwota + „zł”); drugi wiersz: przycisk „ZAPISZ ZMIANY” (`h-12` `px-8` `shadow-lg`); trzeci: „Status:” + mniejszy badge.

- **WMS RMZ — modal korespondencji:** `WmsReturnsPage` — overlay `z-[128]`, trzy zakładki (ALLEGRO / EMAIL / NOTATKI); Allegro: „Brak wiadomości”; Email: `headerEmail` / `headerPhone` + komunikat braku historii; Notatki: `textarea` + zapis do `localStorage` (`wms.rmz.correspondenceNotes.{rid}`), read-only gdy `isFinished`.

- **WMS RMZ — modal szczegółów zamówienia:** `WmsReturnsPage` — klik w numer zamówienia otwiera modal (`max-w-[700px]`, scroll, ✕ / klik w tło). Dane z cache tej samej odpowiedzi `GET orders/:id/` (`orderDetailCached`). Backend `OrderRead` / `OrderItemRead`: dopisano `order_date`, `created_at`, `value`, `shipping_method`, `currency`, `addresses_json`, `unit_price`, `list_price` w istniejącym endpoincie szczegółów zamówienia. Modal: nagłówek (źródło, data, sposób dostawy), klient z `addresses_json` (shipping/billing) + fallback RMZ, lista pozycji (miniatura 12×12, nazwa, ilość, cena), podsumowanie (suma produktów, koszt dostawy z RMZ, wartość z `order.value` lub suma).

- **WMS RMZ — nagłówek operatora:** `WmsReturnsPage` — pierwsza linia: numer zamówienia (`text-xl font-bold`), potem `| RMZ | klient | źródło`; telefon jako przycisk 📞 → modal „Połączenia niedostępne. Skonfiguruj Sellasist Call.”; sekcja „Zwrot kosztów” w osobnej ramce z `ml-6/md:ml-8` i `gap` od przycisku „Zapisz”; status RMZ w osobnym wierszu po prawej; usunięto „Następny”; większe odstępy (`gap-4`, `gap-6`).

- **WMS RMZ — zakończony zwrot (read-only):** `WmsReturnsPage` — `isFinished` z `wmsReturnWorkflowTypeFromRead`: `status.type` lub `return_status.type`, zakończenie gdy `type.startsWith("done")`. Pełna blokada edycji: guardy przed API i mutacjami stanu (`applyLinePatch`, `applyGridDecision`, upload/kamera/sesja telefonu, split/refund, modal uszkodzenia); `cardLocked` zawiera `isFinished`; nagłówek: wyłączone Zwrot koszt dostawy + Zapisz + Następny; overlay na obszarze listy: „ZWROT ZAKOŃCZONY”. Przy przejściu w stan zakończony: zamknięcie modali/sesji i reset podglądu kamery.

- **WMS RMZ grid — pojedyncza decyzja + styl DAMAGED:** `WmsReturnsPage` — `homogeneousDecision` + `gridActiveDecision`: po jednolitej decyzji na wszystkich sztukach pokazywany jest tylko jeden duży przycisk (ACCEPTED/DAMAGED/REJECTED); mieszane decyzje → nadal trzy przyciski. `← ZMIEŃ DECYZJĘ` widoczny gdy nie ma `EDYTUJ` (zapisany overlay) i jest aktywna decyzja / flow uszkodzenia/odrzucenia. Pre-save: zielone/czerwone `bg-*-100` + obramowanie; uszkodzenie `border-orange-500 bg-orange-200` + badge `text-orange-900 font-bold`; panel edytora uszkodzenia `border-orange-500 bg-orange-100`.

- **WMS RMZ — warunki A/B/C wg przepływu:** `WmsReturnsPage` — przyjęcie bez wyboru klasy + `applyLinePatch` OK; zapis split z `condition: "A"` gdy tylko accepted; uszkodzenie tylko wybór **B/C** (bez A w UI); odrzucenie z wymaganym powodem (`WMS_REJECT_REASONS` + `lineOverrides.rejectReasonId`) i panelem w gridzie; modal uszkodzenia: tylko B/C. Backend: `process_rmz_line` ustawia `condition="A"` dla `OK`, `None` dla `REJECTED`; `split-process` ustawia `A` gdy `accepted_qty>0` i brak damaged, `None` gdy całość rejected.

- **WMS RMZ — zdjęcia (kamera / telefon / dysk):** `frontend/src/pages/damage/WmsReturnsPage.tsx` — przywrócono pełne podpięcie UI: trzy przyciski na gridzie „uszkodzenie”, w modalu uszkodzenia oraz w widoku szczegółowym (wiersz „Uszkodzony”); jeden wspólny ukryty `<canvas>` na poziomie strony; `gridCameraUnitIndex` dla kamery per sztuka w tabeli; sesja telefonu: `seenUrls` zawiera też zdjęcia z modala, polling dopina URL do `damageFiles` gdy modal jest otwarty dla tej linii; błąd HTTP 404 na `POST /wms/photo-upload/session` → komunikat „Upload przez telefon niedostępny (backend missing)”.

- **WMS warehouse UX:** `WarehouseProvider` fetches `GET /warehouses/` on mount; if exactly one warehouse exists, it calls `setWarehouse` automatically so WMS (no main-shell top bar) works without manual selection; multiple warehouses unchanged.

- **ORM mapper conflicts (startup 500):** `Basket.cart` no longer uses `back_populates="baskets"` against `Cart.baskets` (`CartBasket`); `Basket` ↔ `Cart` now uses `legacy_baskets` / `back_populates="legacy_baskets"`. `Cart.tenant` / `Cart.warehouse` gain `back_populates="carts"`. `OrderItem.product` gains `back_populates="order_items"` to match `Product.order_items`.

## 2026-03-26

- **SQLite `create_all` — rejestracja wszystkich modeli:** `backend/models/__init__.py` importuje pełny pakiet `warehouse` (layout: `WarehouseLayout`, `Rack`, `Aisle`, `Bin`, `StorageLocation` itd.), żeby tabele layoutu trafiały do `Base.metadata` przed `create_all`; `backend/main.py`: `from . import models` przed `from .database import …`, usunięto późniejszy duplikat importu `models`.

- **WMS zwroty — skrócony format etykiety zamówienia:** `frontend/src/pages/wms/WmsReturnsEntryPage.tsx` — w karcie nagłówkowej zamówienia i na kartach RMZ zastąpiono format `Zamówienie {number}` przez kompaktowe `#{number}` (bez prefiksu), aby szybciej skanować listę.
- **WMS RMZ process — refund i czytelność kart:** `frontend/src/pages/damage/WmsReturnsPage.tsx` — usunięto angielskie etykiety refundu z UI (zastąpione przez `Brak zwrotu / Pełny zwrot / Częściowy zwrot`), dropdown refundu zamieniono na stale widoczny 3-przyciskowy segment, logika refundu: `Pełny` automatycznie wylicza i wpisuje `refund_amount = Σ(unit_price * quantity)`, `Częściowy` zostawia pole kwoty edytowalne bez auto-podstawienia, `Brak` ustawia `0` i blokuje pole; dodatkowo karty pozycji przebudowano na układ flex (zdjęcie po lewej, nazwa+EAN+SKU, duże przyciski decyzji, wtórna linia kontekstu refundu).
- **WMS RMZ — podział ilości decyzji per pozycja:** backend + frontend rozszerzone o split qty: `backend/schemas/wms_return.py` dodaje `WmsReturnLineSplitProcess` oraz pola odczytu `accepted_qty/damaged_qty/rejected_qty`; `backend/models/wms_rmz_line.py` + `backend/db/schema_upgrade.py` + `backend/main.py` dodają i migrują kolumny split na `rmz_lines`; `backend/api/wms_returns.py` dodaje endpoint `POST /wms/returns/{return_id}/lines/{order_item_id}/split-process` z walidacją sumy (`accepted+damaged+rejected == quantity`) i mapowaniem statusu linii. Frontend `WmsReturnsPage.tsx` zamienia pojedynczą decyzję na trzy inputy ilości (`Przyjęte/Uszkodzone/Odrzucone`), domyślnie `accepted=total`, aktualizuje pozostałe ilości przy zmianie, pokazuje błąd sumy i blokuje zapis; payload zapisu jest w formacie `{ product_id, accepted_qty, damaged_qty, rejected_qty }` przez nowe API `processWmsReturnLineSplit`.
- **WMS RMZ split-process — dane uszkodzenia w payload:** `frontend/src/pages/damage/WmsReturnsPage.tsx` rozszerzony o sekcję uszkodzenia widoczną tylko przy `damaged_qty > 0` (stan `A/B/C`, typ uszkodzenia, upload zdjęć), walidację frontu (`condition` + min. 1 `photo_url` wymagane przed zapisem) i rozszerzony payload split-process o `condition`, `photo_urls`, `damage_type`, co eliminuje 400 z walidacji backendu dla uszkodzonych ilości.
- **WMS RMZ — blokada edycji dla zamkniętych zwrotów:** `frontend/src/pages/damage/WmsReturnsPage.tsx` wykrywa stan końcowy przez `return_status.type in {"done_success","done_rejected"}` (`isReturnFinished`), wygasza cały obszar edycji (`opacity-60 pointer-events-none`), dodaje badge `Zamknięty` na kartach, ukrywa dolny pasek akcji i dodaje guardy `return early` przed wszystkimi zapisami/API (`split save`, akcje masowe, refund, modal damage, upload split), aby nie wysyłać żadnych żądań dla zamkniętych RMZ.
- **WMS RMZ — model A/B/C/rejected + refund auto:** split model zmieniony z jednego `damaged_qty` na `accepted_qty`, `damaged_b_qty`, `damaged_c_qty`, `rejected_qty` (backend schema/model/API + frontend types/UI). Walidacja sumy: `A + B + C + rejected == quantity`. UI kart zwrotu ma teraz 4 inputy (`A`, `B`, `C`, `Odrzucone`) zamiast starego modelu uszkodzeń. Manualny wybór/edycja refundu został usunięty; dolny panel pokazuje tylko automatycznie liczony refund `A*100% + B*50% + C*0% (+ rejected*0)` i akcję zakończenia z automatycznym payloadem refundu.
- **WMS RMZ — uproszczenie split + klasy uszkodzeń zagnieżdżone:** logika i UI wróciły do prostego top-level split (`accepted_qty`, `damaged_qty`, `rejected_qty`) z klasami uszkodzeń tylko jako szczegół (`damaged_b_qty`, `damaged_c_qty`) widoczny przy `damaged_qty > 0`; walidacje: `accepted + damaged + rejected = total` oraz `damaged_b + damaged_c = damaged`. Refund uproszczony: selektor `Brak / Pełny / Częściowy` + auto-fill tylko dla `Pełny` (`accepted_qty * unit_price`), bez auto naliczania dla uszkodzonych.
- **WMS RMZ — redesign decision-first:** karta pozycji przełączona na przepływ „decyzja → ilość → szczegóły”: usunięto stały model wielu pól qty na starcie, dodano 3 przyciski decyzji (`Przyjęte / Uszkodzone / Odrzucone`) i pojedyncze pole `Ilość` po wyborze trybu. Dla `Uszkodzone` wróciły zdjęcia (upload wymagany) oraz selektor klasy (`B`/`C`). Dla `Przyjęte` pokazuje się sekcja refundu (`Pełny / Częściowy / Brak`) z auto-fill tylko dla `Pełny`; końcowy refund w stopce jest sumowany z decyzji linii. Payload split-process nadal zawiera główne qty + klasy uszkodzeń.
- **WMS RMZ — decyzja per sztuka (mixed cases):** UI kart pozycji zwrotu renderuje teraz `N` wierszy (`Sztuka 1..N`) dla ilości pozycji; każdy wiersz ma niezależną decyzję (`Przyjęty / Uszkodzony / Odrzucony`). Dla wiersza `Uszkodzony` dostępna jest klasa (`B/C`) oraz wymagany upload zdjęcia dla tej konkretnej sztuki. Zapis agreguje decyzje do payloadu linii (`accepted_qty`, `damaged_qty`, `rejected_qty`, `damaged_b_qty`, `damaged_c_qty`) i łączy zdjęcia uszkodzonych sztuk do wpisu uszkodzenia — bez ręcznego liczenia po stronie operatora.
- **WMS RMZ — workflow 2-kolumnowy (70/30) z panelem postępu:** `frontend/src/pages/damage/WmsReturnsPage.tsx` przebudowany na profesjonalny układ magazynowy: lewa kolumna (obszar pracy) pokazuje jedną aktywną pozycję z nagłówkiem produktu oraz zwartą tabelą sztuk (`lp / decyzja / klasa / zdjęcie`), ikoniczne małe przyciski decyzji i warunkowe pola uszkodzeń (B/C + upload); dodano akcje masowe per aktywna pozycja (`Wszystkie OK`, `Wszystkie uszkodzone`, `Wszystkie odrzucone`). Prawa kolumna zawiera listę wszystkich pozycji RMZ z postępem `checked/total` i podświetleniem aktualnie obrabianej pozycji dla szybkiego skanowania.
- **WMS RMZ — polish UX pod magazyn:** `frontend/src/pages/damage/WmsReturnsPage.tsx` dopracowano czytelność i bezpieczeństwo: tabela sztuk ma subtelne zebra-striping + hover + czerwone podbicie wierszy bez decyzji; przyciski decyzji zwiększono do większego hit-area (`h-10`) i większych ikon (`✓ ⚠ ✕`); przycisk zapisu linii jest zielony i sticky na dole sekcji. `Wszystkie OK` działa teraz bezpiecznie tylko na puste wiersze, dodano opcjonalne `Nadpisz wszystkie` z potwierdzeniem. Prawy panel ma sticky nagłówek, sticky pozycję i kolorowe kropki progresu (`zielony done / pomarańcz partial / szary pending`). Po udanym zapisie linii interfejs automatycznie przechodzi do następnej pozycji.
- **WMS RMZ — nowy Grid Mode (decision-first):** `frontend/src/pages/damage/WmsReturnsPage.tsx` lewy obszar roboczy domyślnie renderuje responsywną siatkę kart produktów (2–4 kolumny), każda karta ma duże zdjęcie, skrócone dane (nazwa/EAN/SKU) oraz trzy duże akcje (`✓ / ⚠ / ✕`) bez dodatkowych potwierdzeń. Po kliknięciu decyzji karta pokazuje szybki overlay (półtransparent + duża ikona). Dla `⚠` operator jest przenoszony do istniejącego widoku szczegółowego (detail/table) dla tej pozycji, z przyciskiem powrotu do gridu. Prawy panel postępu pozostaje aktywny.
- **WMS Returns — hard fix layout + workflow shell:** `frontend/src/layout/WmsLayout.tsx` i `frontend/src/pages/damage/WmsReturnsPage.tsx` ustawione pod pełny ekran operacyjny (`h-screen`, `flex-col`, `overflow-hidden`) z pojedynczym scrollem tylko w obszarze roboczym (`flex-1 overflow-auto`). Usunięto limitery szerokości/centrowanie (`max-w-*`, `mx-auto`, container-like wrappers) w głównym widoku, siatkę przełączono na pełną szerokość `repeat(auto-fill,minmax(320px,1fr))` z `gap-6`, a karty odblokowano z cięcia (bez fixed height i bez `overflow-hidden` na kontenerze karty; `overflow-hidden` zostaje wyłącznie na sekcji obrazka `h-[200px] object-cover`). Top bar pod nagłówkiem jest sticky i zawiera dane operacyjne (RMZ, zamówienie, klient, źródło, koszt dostawy), liczniki oraz akcje `Zapisz/Następny`; dolne/legacy sekcje akcji pozostają usunięte.
- **WMS RMZ — rozszerzenie modelu biznesowego (minimal safe):** backend: `backend/models/wms_order_return.py` dodano `return_type` (`RMA` default, `UNCLAIMED`), `backend/models/wms_rmz_line.py` dodano `final_disposition`; `backend/schemas/wms_return.py` rozszerzono create/read/split payload o `return_type`, `final_disposition`, `condition/photo_urls/damage_type` dla split-process. `backend/api/wms_returns.py`: create akceptuje i normalizuje `return_type`; split-process ma teraz walidację damaged evidence zgodną z legacy process, nie czyści już `condition/photo_urls/damage_type` gdy payload ich nie nadpisuje, automatycznie wylicza `final_disposition` z zapisanych ilości i dodaje placeholder `# TODO inventory routing`; `UNCLAIMED` blokuje decyzję/qty `REJECTED`. Refund endpoint dla `UNCLAIMED` omija standardową logikę refundu i wymusza zapis `NONE` + przejście do `success`.
- **Migracje DB dla nowych pól RMZ:** `backend/db/schema_upgrade.py` rozszerzone o dodanie `wms_order_returns.return_type` i `rmz_lines.final_disposition` na istniejących bazach; ścieżka rekonstrukcji tabeli `wms_order_returns__new` również zawiera nowe pole `return_type` z bezpiecznym defaultem `'RMA'`.
- **Frontend create RMZ — wybór typu zwrotu:** `frontend/src/pages/wms/WmsReturnsEntryPage.tsx` dodano minimalny selector `Typ zwrotu` (`RMA`/`UNCLAIMED`) i przekazywanie `return_type` w `createWmsReturn`; typy API rozszerzone w `frontend/src/types/wmsReturn.ts`. Dodatkowo split payload w `frontend/src/pages/damage/WmsReturnsPage.tsx` wysyła teraz `condition/photo_urls/damage_type`, żeby spełnić nową walidację split-process bez łamania flow.
- **WMS RMZ UX — wyłączony autosave, jawne `Zapisz`:** `frontend/src/pages/damage/WmsReturnsPage.tsx` — usunięto autozapis split-process (brak `processWmsReturnLineSplit` w efektach/klikach), decyzje aktualizują tylko lokalny stan + `dirtyLineIds`; przycisk górny `Zapisz` waliduje całość i zapisuje tylko zmienione linie (`processWmsReturnLineSplit`), po sukcesie robi reload RMZ i czyści stan zmian.
- **WMS RMZ — poprawiony flow „USZKODZONY” w gridzie:** `frontend/src/pages/damage/WmsReturnsPage.tsx` — klik „USZKODZONY” otwiera per-kartę panel uszkodzeń (`mode: idle/damaged`) bez ustawiania decyzji „DAMAGED”, panel pokazuje B/C, stepper ilości, upload + miniatury zdjęć, pole zwrotu i przycisk lokalny „Zapisz produkt” (bez API).
- **WMS RMZ — przywrócony upload zdjęć przez telefon (QR):** `frontend/src/pages/damage/WmsReturnsPage.tsx` — dodano trzecią akcję `📱 Telefon` obok `📷 Kamera` i `📁 Dysk`, tworzenie sesji `/wms/photo-upload/session`, modal QR do `/wms-upload/{session_id}`, polling co 2s `/wms/photo-upload/session/{session_id}` i automatyczne dopinanie nowych zdjęć do bieżącej uszkodzonej sztuki.
- **Backend WMS photo-upload (QR session) — brakujące endpointy dodane:** utworzono `backend/api/wms_photo_upload.py` z routerem `/wms/photo-upload` (`POST /session`, `GET /session/{session_id}`, `POST /` multipart z `session_id`), proste sesje in-memory (`photos[]`) i zapis plików do `backend/uploads` z odpowiedzią `/uploads/{filename}`; router zarejestrowany w `backend/main.py` pod prefiksem `/api`.

## 2026-03-25

- **WMS „Nowy zwrot” — wiersze produktów:** `WmsReturnsEntryPage` — formularz tworzenia RMZ: wiersz `flex items-center gap-3` z miniaturą `w-12 h-12 object-cover rounded`, nazwa **bold**, pod spodem EAN i SKU (`text-sm text-slate-500`), `resolveDamageMediaUrl` dla `image_url`, placeholder (ikonka) bez zdjęcia; `ProductInOrder` w API + `sku` (obok `symbol`, `ean`, `image_url`).

- **WMS RMZ — jednolity styl kart:** `WMS_CARD_BASE` (`rounded-xl`, `border border-slate-100`, `shadow-md`, `transition`, `hover:shadow-lg`) współdzielone przez kartę zamówienia (`bg-white p-6`) i karty RMZ (`w-52 p-4`); tint tła RMZ po `status.type`: `done_success` → `bg-green-50`, `in_progress` → `bg-blue-50`, `done_rejected` → `bg-red-50`; badge nadal z `status.color`.
- **WMS RMZ — karta zamówienia (hero):** `WmsReturnsEntryPage` — treść hero jak wcześniej; obudowa dołączona do wspólnego `WMS_CARD_BASE` + `bg-white p-6`, `mb-8` pod kartą.

- **RMZ statusy z bazy (`ReturnStatus`):** model + migracja `ensure_return_statuses_and_rmz`, `WmsOrderReturn.status_id` → logika po `type` / `transition_key` (nie po `name`); API `/wms/return-statuses` + `WmsReturnRead/ListItem.status` jako obiekt; frontend: karty/lista używają `status.name` i mapy kolorów dla `status.color`, refund UI po `transition_key`; admin **Zamówienia → Zwroty — statusy** (`/orders/returns/statuses`).

- **RMZ lista (karty):** API `WmsReturnListItem` — usunięto `external_id` / `sales_document_number`; dodano `customer_first_name`, `customer_last_name` (z `Order.addresses_json` Imię/Nazwisko), `order_source`; linie listy z `RMZLine` (decyzje) dla koloru statusu. UI: numer zamówienia, klient, źródło, badge „Zrealizowany” (zielony/czerwony gdy wszystkie REJECTED) / „W trakcie” (niebieski); bez RMZ i Zewn. na karcie.

- **Panel admin: Ustawienia WMS:** trasa `/settings/wms`, pozycja sidebara „Ustawienia WMS”, strona `WmsSettingsPage` — tryb zwrotów + checkboxy (zdjęcia / klasa stanu / refund), GET+POST `/api/wms/settings` (per tenant+magazyn); `WmsReturnsPage` ładuje config przez `getWmsSettings`.

- **WMS RMZ lista (`WmsReturnsEntryPage`):** usunięto przycisk „Zmień zamówienie”; karty RMZ wyśrodkowane (`flex-col items-center`, `text-center`), bez obramowania listy — tło i status: zakończony (`bg-red-50` + badge czerwony), oczekuje (`bg-blue-50` + niebieski), w toku / przyjęte / kontrola (`bg-yellow-50` + amber); spójna sekcja „Nowy zwrot” (`max-w-2xl`, nagłówki wyśrodkowane).

- **WMS RMZ 500 (POST / GET lista):** starsze pliki SQLite miały `wms_order_returns` bez kolumn z aktualnego modelu (`external_id`, czasem brak `status` / NULL → błąd SQLAlchemy przy `commit` lub walidacja Pydantic). Dodano `ensure_wms_order_returns_columns` w `db/schema_upgrade.py` (ALTER + backfill `status`, indeks + opcjonalny unique), wywołanie po `create_all` w `main.py`. Lista RMZ: join zwraca też `Order.sales_document_number` (wcześniej błędnie czytano z `WmsOrderReturn`); `status` w odpowiedzi z fallbackiem `"NEW"`.

- **Numeracja zamówień (import WMS / Sellasist):** `order.number` zawsze generowane jako `W{warehouse_id}-{seq}` (`next_internal_order_number`); CSV **„Numer dokumentu sprzedaży”** → `Order.sales_document_number`; **„Zewnętrzny identyfikator”** (lub legacy ID wiersza) → `external_id`; przy aktualizacji importu `number` **nie** jest zmieniane. Migracja startowa: `migrate_orders_sales_document_misassigned_number` — gdy `import_metadata_json` ma ten sam tekst co błędny `number` dla „Numer dokumentu sprzedaży”, przeniesienie do `sales_document_number` i nowy numer wewnętrzny. API: `OrderRead` / `OrderListRead` / `OrderLookupHit` + wyszukiwarka zamówień i WMS lookup po `external_id` i `sales_document_number`; UI zwrotów: podtytuł „Zewn.” / „Dok. sprz.”.
- **Numeracja zamówień (wersja uproszczona):** `next_internal_order_number` generuje teraz prosty numer sekwencyjny jako string (`"1"`, `"2"`, …) na podstawie maksimum `Order.number` (tylko wartości numeryczne) per `(tenant_id, warehouse_id)`. Import zamówień tworzy nowe rekordy wyłącznie z tym numerem wewnętrznym; dopasowanie UPSERT tylko po `external_id`; aktualizacja nigdy nie zmienia `order.number`. Przy kolizji unikalności (wyścig) import ponawia próbę wygenerowania numeru i opiera bezpieczeństwo o unikalny indeks DB `(tenant_id, warehouse_id, number)`.

- **Import produktów (CSV pełny Sellasist):** `import_products` — domyślny sniff CSV z fallbackiem `;`, nagłówki normalizowane (BOM/cudzysłowy, duplikaty `Name (i)`), pierwsza kolumna `Cena` → `sale_price`, kolejne `Cena*` i pozostałe nadmiarowe kolumny w `Product.metadata_json` wraz z `Zdjęcia_dodatkowe_url`, `dodatkowe_kody_ean`; `Zdjęcia` pierwszy URL → `image_url`; dopasowanie UPSERT: EAN → SKU/Symbol → `catalog_number` → nazwa; `Stan magazynowy` UPSERT `Inventory`/`InventoryUnit` (także przy aktualizacji); modele/kolumny `Product.catalog_number`, `Product.metadata_json` + `ensure_products_import_metadata_columns`; API `_product_to_dict` zwraca `catalog_number` i sparsowany `metadata_json`; zamówienia: dopasowanie po numerze katalogowym używa `Product.catalog_number`.

- **Import zamówień (Sellasist / CSV rzadki):** `import_orders` — forward-fill pól nagłówka, klucz `Zewnętrzny identyfikator`, `Order.external_id` + UPSERT `(tenant, warehouse, external_id)`, snapshot nagłówka w `import_metadata_json`, adresy 2× w `addresses_json`, linie z `list_price` / `vat_percent` / `metadata_json`, dopasowanie produktu: EAN → SKU wewn. → Symbol → katalog → nazwa; agregacja linii po kluczu; odpowiedź API: `orders_total`, `lines_imported`, `skipped_rows`, `unmapped_columns`. Migracja SQLite w `main._ensure_order_columns`.

- **WMS RMZ — skaner / klawiatura:** `/wms/returns` — auto-focus pola wyszukiwania, Enter = lookup, jedno trafienie = auto-wybór zamówienia, wielotrafienia = focus pierwszego wyniku, po wczytaniu zamówienia focus pierwszego RMZ lub pierwszego pola ilości; tworzenie RMZ jako `<form>` (Enter zapisuje). Process (`WmsReturnsPage`) — strzałki góra/dół między liniami przy zachowaniu kolumny akcji (OK / Uszkodzony / Odrzucony), focus pierwszej oczekującej po wejściu i po zamknięciu modala uszkodzenia; Enter w modalu zapisuje uszkodzenie (gdy są zdjęcia).
- **WMS RMZ — przepływ od zamówienia (bez globalnej listy):** `WmsReturnsEntryPage` nie woła już `GET /wms/returns/` przy mount; lista RMZ tylko z `GET /wms/returns/orders/{order_id}/returns` po wybraniu zamówienia; po `POST` tworzenia — ponowne wczytanie listy per order; pierwszy zwrot = pusty komunikat + formularz; gdy są zwroty — lista + przycisk „Dodaj kolejny zwrot”. Backend: `GET /wms/returns/orders/{order_id}/returns` z powrotem w `wms_returns.py`; frontend: `listWmsReturns` zastąpione przez `listWmsReturnsForOrder`.
- **API pod jednym prefiksem `/api`:** wszystkie routery w `main.py` z `prefix="/api"`; mount `StaticFiles` nadal `/uploads`. Usunięto `wms_photo_upload` i zdublowane mounty WMS. Frontend: `VITE_API_URL` z `/api`, axios `baseURL` + stripping wiodącego `/` na ścieżkach; upload wyłącznie `POST /api/uploads`; RMZ lista używa pola `lines` jak detail; walidacja `photo_urls` tylko `/uploads/…`.
- **API RMZ — lista GET:** `list_wms_returns` na `GET ''` i `GET '/'` (Ścieżki `/wms/returns` i `/wms/returns/`), drugi mount routera pod **`/api`** (jak `VITE_API_URL=…/api`); odpowiedź listy z **`items`** (linie z `lines_json`), sort **`created_at` DESC** (nulls last) + **`id`**; `POST` utworzenia też na `''` i `'/'`.
- **WMS Zwroty — jeden ekran (`/wms/returns`):** sekcje: **Zamówienie** (wyszukaj / wybierz) → **Lista RMZ** → **Nowy zwrot** (inline: produkty + ilości + **Dodaj zwrot**); po POST RMZ odświeżenie listy na miejscu, podświetlenie nowego, bez nawigacji na osobny widok. Usunięto `WmsReturnsOrderPage`; stara trasa `returns/order/:id` → redirect na `/wms/returns` ze `state.preselectOrderId`. Usunięto `WMS_ROUTES.returnsOrder`; z procesu zwrotu link „←” prowadzi do listy RMZ z preselekcją zamówienia.
- **WMS Zwroty — lista RMZ jak Sellasist:** `GET /wms/returns/` zwraca wszystkie RMZ magazynu (z `order_number`); `/wms/returns` pokazuje listę RMZ (status UI: OCZEKUJE dla `pending`/`new`) i wyszukiwanie zamówienia; klik RMZ → `/wms/returns/process/:id`.
- **WMS zwroty — tryb konfigurowalny (simple / two_step / advanced):** dodano modele `WmsSettings`, `RMZLine`, `WmsRefund` + API `/wms/settings/returns-mode`, oraz backend walidację wymagań wg trybu (`require_photos`, `require_condition`) na `POST /wms/returns/{id}/lines/{order_item_id}/process` i etap refundu na `POST /wms/returns/{id}/refund`; frontend `Setup` ma selektor trybu, a `WmsReturnsPage` warunkowo renderuje pole `condition (A/B/C)` i sekcję refundu zależnie od config + statusu RMZ.

## 2026-03-24

- **WMS Zwroty — uproszczenie UI (`WmsReturnsPage`):** jednoliniowy nagłówek `RMZ | Zamówienie | Klient` (strzałka wstecz), bez panelu metadanych i bez akcji w nagłówku; dolna belka stała: lewo `OK: / Uszkodzone: / Odrzucone:`, prawo masowe + `Zapisz i zakończ`; siatka bliżej nagłówka, mniejsze odstępy i karty; klient z opcjonalnych pól odpowiedzi zamówienia (`customer_name` / `billing_name` / `recipient_name`), w przeciwnym razie `—`.
- **WMS photo session — twarde blokowanie base64:** `_reject_base64_or_data_url_for_session` (HTTP 400) przed zapisem do sesji; GET wywołuje `_assert_response_photos_urls_only` po `_filter_session_photos` (wyłącznie `/uploads/{uuid}.{ext}`); brak `data:image` w odpowiedzi.
- **WMS photo session — tylko ścieżki `/uploads/`:** `backend/api/wms_photo_upload.py` — usunięto dekodowanie `data:image`; sesja przechowuje wyłącznie ścieżki zgodne z `save_damage_image_bytes` (`_filter_session_photos` odrzuca `data:`/base64); GET zapisuje oczyszczoną listę z powrotem; `print` + `logger.info` `SESSION PHOTOS`; POST waliduje URL przed `append`.
- **WMS zapis uszkodzenia:** przed `createDamageEntry` — `fetchWmsSessionPhotos` (GET `/wms/photo-upload/session/{id}`); `photo_urls` = złączenie `photos` z API + URL-i z miniatur (`/uploads/…`); puste → „Dodaj zdjęcie”; `parseWmsSessionPhotosResponse` + `encodeURIComponent(session_id)`.
- **DamageEntryCreate — bez ruchu magazynowego:** `location_uuid` opcjonalne (nullable w OpenAPI); przy podanym UUID tylko snapshot etykiety z `Location`/`Bin` — bez wymogu `storage_type=damaged` i bez kontroli stanu.
- **POST /damage-entries/ — tylko `photo_urls`:** usunięto `photo_url` z `DamageEntryCreate` i z payloadu frontendu; normalizacja odrzuca `data:`/`blob:` i wpisy spoza `/uploads/` oraz http(s); max 15. Duplikat `photo_url` w JSON nie był już ustawiany po stronie klienta.
- **POST /damage-entries/ bez lokalizacji:** `location_uuid` opcjonalne w API i payloadzie; brak weryfikacji stanu magazynu przy lokalizacji; gdy UUID podane — nadal wymagany bin `storage_type=damaged`. Wpis zapisuje `location_uuid=""` gdy pominięte. WMS zwroty i panel nie wysyłają UUID.
- **Debug POST /damage-entries/:** `createDamageEntry` — `console.log("PAYLOAD", body)` przed POST, `console.log("ERROR", response.data)` przy błędzie; `normalizeDamageEntryCreatePayload` — tylko stringi w `photo_urls`, wymuszenie poprawnego `damage_type` (literal), pierwsze zdjęcie jako `photo_url`; backend nie ma pola `reason` (tylko `damage_type`).
- **Zdjęcia uszkodzeń (upload plików):** `POST /api/uploads` → `{ "url": "/uploads/{uuid}.ext" }`; katalog `backend/uploads/` + mount `StaticFiles` `/uploads`; `wms/photo-upload` zapisuje na dysk zamiast data URL; WMS modal: upload przez API, `photo_urls` trzyma ścieżki; `resolveDamageMediaUrl` + wyświetlanie w Office; bez zmian w `product.image_url` / importach.
- **POST /damage-entries/ (400 WMS):** backend — jeśli **brak** wiersza `Inventory` dla (tenant, warehouse, product, damaged `location_uuid`), zapis dozwolony; gdy wiersz jest, nadal wymagane `quantity` ≤ stan. Frontend — `normalizeDamageEntryCreatePayload`, log `console.log` payload + `console.error` pełnego `response.data`; modal zwrotów pokazuje `detail` z API.
- **WMS uszkodzenia — wiele zdjęć:** modal zwrotów: lista `DamagePhotoItem` z `id`, limit 15 (jak API/Pydantic), miniatury w siatce z usunięciem po kliknięciu, kamera/upload/QR dopisują do listy (polling bez zamiany), sesja telefonu `MAX_PHOTOS_PER_SESSION=15` (`wms_photo_upload.py`); `DamageEntryCreate.photo_urls` max 15; `WmsPhoneUploadPage` pokazuje `detail` z API; panel WMS w Magazynie zapisuje `photo_urls: [url]`.
- **Frontend URL — tylko env:** `getApiBaseUrl()` / `frontend/src/config/apiBase.ts` (`VITE_API_URL`, bez domyślnego hosta w kodzie); `getPublicBaseUrl()` tylko `VITE_PUBLIC_URL`; axios + `OrderList` bulk delete + obrazki produktu (`DesignerRackPlacement`); `.env.example` z pustymi wartościami i przykładami IP.
- **WMS QR — adres publiczny:** link `${base}/wms-upload/{session}`; Vite `server.host: true`; bez bazy URL — komunikat o `.env`.
- **WMS zdjęcie z telefonu (QR):** backend sesje w pamięci `POST/GET/DELETE /wms/photo-upload/...`, `POST /wms/photo-upload` (multipart); strona `/wms-upload/:sessionId`; modal zwrotów: QR + polling 1,5 s; pełny ekran bez sidebara.
- **WMS modal uszkodzenia:** przyciski „Zrób zdjęcie” (`getUserMedia` + podgląd wideo, „Zapisz kadr”), „Upload zdjęcia”, „Zamknij kamerę”; `Zapisz` bez zdjęć wyłączone.
- **WMS Zwroty — zwrot bez lokalizacji:** siatka z linii RMZ + zamówienia (nie z magazynu uszkodzeń); `fetchFirstDamagedBinUuid` tylko przy zapisie „Uszkodzony”; usunięto `allocateDamageCandidatesForReturn` i synchronizację z `listDamageEntries`; nagłówek: przetworzone + OK / Uszkodzone / Odrzucone; komunikat „Ten zwrot nie został jeszcze obsłużony”.
- **WMS Zwroty — od zamówienia (RMZ):** backend `WmsOrderReturn` + `/wms/returns` (lookup zamówienia po numerze/kodzie/ID, lista zwrotów dla zamówienia, POST utworzenie RMZ, GET sesja); frontend: `/wms/returns` tylko wyszukiwanie, `/wms/returns/order/:id` szczegóły + istniejące RMZ + „Dodaj nowy zwrot”, `/wms/returns/process/:returnId` siatka (kandydaci z magazynu uszkodzeń przycięci do linii RMZ).
- **WMS pełna szerokość:** `WmsReturnsPage` bez `max-w`/`mx-auto`, `w-full px-4`; siatka `grid-cols-2` → `md:3` `lg:4` `xl:5` `2xl:6`, `gap-3`; nagłózek + akcje masowe w jednym rzędzie flex; `WmsLayout`/`MainLayout` gałąź WMS z `w-full`.
- **WMS Zwroty — widok siatki (wszystkie pozycje):** usunięto tryb pojedynczej karty; zawsze widoczna siatka kart z dużą etykietą stanu (OK / USZKODZONY / ODRZUCONY / NIEPRZETWORZONY), mocne obramowania i tła wg stanu, pierścieniowe wyróżnienie nieprzetworzonych; sortowanie: najpierw oczekujące; OK i Odrzucony natychmiast (bez modala); Uszkodzony → modal ze zdjęciem; postęp „przetworzonych / wszystkich”.
- **WMS Zwroty — nagłówek operacyjny:** usunięto dropdown magazynu, pole szukaj/sk oraz filtrowanie po zapytaniu; magazyn z `WarehouseContext` (wybór poza WMS); nagłówek statyczny „{nazwa} • Zwroty” + „i / n produktów”, pasek postępu w tym bloku (tryb pojedynczy).
- **WMS Zwroty — tryb pojedynczej pozycji:** domyślnie jedna duża karta (zdjęcie, nazwa, SKU, ilość), przyciski OK / Uszkodzony / Odrzucony (`min-h-[56px]`), pasek postępu + indeks `i / n`, auto-przejście do następnej pozycji po decyzji; przełącznik **Widok listy** (siatka jak wcześniej); modal uszkodzenia: wymagane zdjęcie (`Zapisz` wyłączone bez plików), opcjonalny typ uszkodzenia; `POST /damage-entries/` przyjmuje `damage_type`.
- **WMS shell:** `WmsLayout` + trasy zagnieżdżone `/wms`, `/wms/returns`, `/wms/picking`, `/wms/packing`, `/wms/receiving`; pełny ekran bez sidebara (`MainLayout`); strona wejścia z przyciskami trybów; zwroty bez `PageLayout` i etykiety RMA w belce; sidebar **WMS** → `/wms`.
- **WMS Zwroty — ekran operacyjny:** `WmsReturnsPage` — siatka kart (zdjęcie, SKU, ilość), akcje masowe (`Przyjmij wszystkie` / `Oznacz wszystkie jako uszkodzone` / `Odrzuć wszystkie`), odrzucenie z opcjonalnym modalem powodu; `DamageCandidate.imageUrl` z `fetchDamageCandidates`.
- **WMS Zwroty uproszczone:** (wcześniej) decyzje per produkt bez tekstów biurowych; usunięty `returnReasonsConfig.ts`.
- **RMA / zwroty — trzeci stan DAMAGED + wielościeżka zdjęć:** (wcześniej) backend `DamageEntry.photo_urls`, API create/read, wielokrotne obrazy w protokole z `entry_ids`.
- **Backend `DamageEntry`:** kolumna `photo_urls` (JSON), create akceptuje `photo_urls` lub legacy `photo_url`, odczyt zwraca `photo_urls`; generowanie protokołu z `entry_ids` zapisuje wiele `DamageReportImage` na wpis; migracja SQLite w `ensure_damage_report_columns`.

## 2026-03-23

- **Top Volume Products report upgraded for decision-making:** added new data builder `frontend/src/pdf/utils/buildTopVolumeReportData.ts` with per-product `totalWeight`, `totalValue`, `valueDensity` (zł/dm³), and distribution type (`CONCENTRATED`/`MEDIUM`/`SCATTERED`), plus global KPIs (`totalWeightAll`, `avgVolumePerProduct`, `top1SharePercent`, `top3SharePercent`) and top-3 problematic product detection (high volume + low density or >3 locations).
- **Top Volume PDF view refactor:** introduced `frontend/src/pdf/TopVolumeReportPDF.tsx` with expanded KPI cards, extended table columns (waga, wartość, wartość/dm³), Polish distribution labels, horizontal udział progress bars, highlighted #1 row, and a new "Problematyczne produkty" section; `frontend/src/pdf/generateTopVolumeReportPDF.tsx` now uses the new builder + component while keeping existing export flow.
- **Top Volume PDF typography/formatting hardening:** fixed Polish glyph rendering by forcing Roboto normal+bold registration and page-level `fontFamily: "Roboto"`, cleaned KPI/table labels to fully Polish diacritics, simplified table columns (moved value-density to small secondary line under value), improved row/column spacing and non-breaking numeric formatting, and reworked "Problematyczne produkty" into bullet-style lines with volume/value/location metrics.
- **Top Volume decision insights and visual indicators:** upgraded `buildTopVolumeReportData` + `TopVolumeReportPDF` with a new "Wnioski" section (TOP1/TOP3 share, zero-value detection, concentration signal), added `Najcięższy produkt (kg)` KPI, color-coded value density (red/orange/green thresholds), and enhanced "Problematyczne produkty" with explicit problem-type badges (`Niska efektywność przestrzeni`, `Duża objętość`, `Rozproszenie`).
- **Top Volume stale-render hardening:** added explicit debug marker text `DEBUG_VERSION_2` in the PDF document and render log `TOP_VOLUME_RENDER_V2` in generator; renamed component symbol `TopVolumeReportPDF -> TopVolumeReportPDF_v2` and updated direct import usage in `generateTopVolumeReportPDF.tsx` to force a fresh reference path.
- **Damage Report system (Protokół szkody):**
  - **Backend:** added new models `DamageReport`, `DamageReportItem`, `DamageReportImage` (`backend/models/damage_report.py`) with snapshot fields and value totals; exposed API `POST /damage-reports/`, `GET /damage-reports/`, `GET /damage-reports/{id}`, `POST /damage-reports/{id}/confirm` (`backend/api/damage_reports.py`), including auto-numbering `PS/{YYYY}/{MM}/{NNN}`, damaged-location validation (`warehouse_bins.storage_type = damaged`), quantity > 0 checks, and confirm lock behavior.
  - **Backend wiring:** registered models in `backend/models/__init__.py` and router in `backend/main.py`.
  - **Frontend:** added damage report types (`frontend/src/types/damageReport.ts`), API client (`frontend/src/api/damageReportsApi.ts`), React-PDF export (`frontend/src/pdf/generateDamageReportPDF.tsx`), and new Magazyn modal panel (`frontend/src/components/warehouse/magazyn/DamageReportsPanel.tsx`) with list/create/details flows and `Pobierz PDF`.
  - **Warehouse integration:** added `Szkody` entry button in `MagazynDashboardPanel`, integrated panel in `WarehouseDesigner`, and added context action in `MagazynProductsSidebar` (`Utwórz protokół szkody`) when a product is shown in a damaged location (prefills create form).
- **Damage handling separation (WMS + Office + Reports):**
  - Added operational source model `DamageEntry` (`backend/models/damage_report.py`) with required `photo_url`, review fields (`damage_type`, `description`, `decision`, `reviewed_by`, `reviewed_at`), and lifecycle status `NEW | REVIEWED | INCLUDED_IN_REPORT`.
  - API split:
    - **WMS:** `POST /damage-entries/` (create NEW entry, damaged-location + photo required)
    - **Office:** `GET /damage-entries/` (filter by status), `POST /damage-entries/{id}/review` (set decision and mark REVIEWED)
    - **Reports:** `POST /damage-reports/` accepts `entry_ids` (REVIEWED only), snapshots into report items, then marks entries `INCLUDED_IN_REPORT`.
  - Report item schema/model now carries decision snapshot and optional source link (`damage_entry_id`) for traceability.
  - Frontend `DamageReportsPanel.tsx` refactored to explicit tabs: **WMS / Office / Reports**, with Office edit/review actions and Reports selection limited to REVIEWED entries.
  - PDF output (`generateDamageReportPDF.tsx`) now includes decision in details.
- **Dedicated 3-layer UI pages for damage flow:**
  - Added routes/pages:
    - `/wms/returns` (`frontend/src/pages/damage/WmsReturnsPage.tsx`) — returns table from damaged-location candidates; modal requires `photo_url` and quantity before creating `DamageEntry` (no decision fields).
    - `/office/damages` (`frontend/src/pages/damage/OfficeDamagesPage.tsx`) — Office review table (`NEW`/`REVIEWED`) with side drawer showing large image + product details and editable `damage_type`, `description`, `decision` (radio), plus REVIEWED action.
    - `/office/damage-reports` (`frontend/src/pages/damage/OfficeDamageReportsPage.tsx`) — batch checkbox selection of `REVIEWED` entries only and action `Generuj protokół szkody`.
  - Added shared data helper `frontend/src/pages/damage/damageShared.ts` for warehouse + damaged candidates loading.
  - Added sidebar navigation entries in `MainLayout.tsx` for WMS/Office/Reports damage screens.
- **Warehouse structure report layout system:** `frontend/src/components/warehouse/structureReport/WarehouseStructureReportView.tsx` — introduced a consistent max-width centered card/grid layout, converted KPI block to aligned 2x2 cards with number+unit together, wrapped storage distribution into one card with a thicker horizontal progress bar and labels below, and replaced template plain-text slot rows with bordered boxed slot grids per level (while keeping report data unchanged).
- **Warehouse structure PDF map page rendering:** `backend/templates/warehouse_structure_report.html.j2` — map is now rendered on its own portrait A4 page with the heading "Mapa magazynu", centered large SVG map area sized to fill most of page height while keeping proportions (`viewBox` + `preserveAspectRatio="xMidYMid meet"`), and removed framed map container styling (no map border/background/shadow).
- **Warehouse structure report high-level insights:** added dynamic, minimal SaaS-style context in UI and HTML PDF (`WarehouseStructureReportView.tsx`, `warehouse_structure_report.html.j2`): snapshot line (density + dominant storage + shop-zone presence), locations-per-m² metric, compact structure block (racks / total levels / templates), dominant-storage interpretation sentence, and most-common-template share.
- **Warehouse Executive Report (new PDF):** added a dedicated management report pipeline for `executive` export using react-pdf (`frontend/src/pdf/utils/executiveReportDataBuilder.ts`, `frontend/src/pdf/WarehouseExecutiveReportPDF.tsx`, `frontend/src/pdf/generateWarehouseExecutivePDF.tsx`) and wired it in `WarehouseDesigner.tsx`. Report now has 4 business pages: Executive Summary (KPI + auto insights), Value (top 5 by value + share), Utilization (occupancy + storage usage), and Issues (decision-focused risks/recommendations), with no map/UUID/technical detail noise.
- **Exports vs Reports UI separation:** reports were removed from Designer export dropdown (`RackSidebar` now exposes only location map CSV, CSV, JSON). In Magazyn view, `MagazynDashboardPanel` now includes a dedicated `Raporty` button opening `WarehouseReportsPanel` with three report entries (structure, occupancy, executive), each with one-line description and explicit `Pobierz PDF` action; report generation logic/endpoints remain unchanged and are called via existing `handleExportWarehouseReport`.
- **Product Location Report (new PDF):** added `product_locations` report variant and wired it into Magazyn `Raporty` panel. New builder + PDF pipeline (`frontend/src/pdf/utils/productLocationReportDataBuilder.ts`, `frontend/src/pdf/ProductLocationReportPDF.tsx`, `frontend/src/pdf/generateProductLocationReportPDF.tsx`) uses inventory as source of truth, groups by `locationUUID` with per-product/location deduped quantities, sorts products by location-count desc, and renders management-friendly blocks (name, SKU, location count, total qty, location label + qty + storage type) plus summary/filter counters.
- **Warehouse structure HTML->PDF via frontend route (Puppeteer URL mode):** refactored backend reports endpoint contract to `POST /reports/warehouse-structure/pdf` with body `{ warehouse_id, layout_id, tenant_id? }` (`backend/api/reports.py`, `backend/schemas/structure_report_pdf.py`), replaced payload-to-Jinja rendering path with URL-based Puppeteer render (`backend/services/structure_report_pdf_service.py`, `backend/scripts/structure_report_pdf/render_from_url.mjs`), and added frontend standalone report route/page (`frontend/src/reports/WarehouseStructureReportPage.tsx`, route in `App.tsx`, chrome bypass in `MainLayout.tsx`). Frontend download flow now sends IDs only (`structureReportPdfApi.ts`, `WarehouseDesigner.tsx`).
- **Product Location Report migrated to HTML+Puppeteer:** removed product report react-pdf files (`frontend/src/pdf/generateProductLocationReportPDF.tsx`, `frontend/src/pdf/ProductLocationReportPDF.tsx`), added HTML report view/page (`frontend/src/reports/ProductLocationReportView.tsx`, `frontend/src/reports/ProductLocationReportPage.tsx`, route `/report/product-locations`), added backend endpoint `POST /reports/product-locations/pdf` (`backend/api/reports.py`) and service URL render helper (`generate_product_location_report_pdf_bytes`), and replaced frontend download flow with ID-based backend call (`downloadProductLocationReportPdf` in `structureReportPdfApi.ts`, wired in `WarehouseDesigner.tsx`).
- **Inventory UUID compatibility layer:** added nullable `location_uuid` support to active stock/inventory persistence and API without removing `location_id`. Changes include schema upgrade/backfill + index (`ensure_inventory_location_uuid_columns` in `backend/db/schema_upgrade.py`), startup migration hook (`backend/main.py`), model fields (`backend/models/stock.py`, `backend/models/inventory.py`), and response schema/API population (`backend/schemas/inventory.py`, `backend/api/inventory_api.py`) so inventory now returns both `location_id` and `location_uuid`.
- **Inventory write-path UUID refactor:** `_sync_inventory_from_assigned_locations` in `backend/api/product.py` now requires non-empty `locationUUID` in each assignment entry, resolves locations strictly by `Location.location_uuid` (removed name/label/address resolution), rejects missing/invalid UUIDs with 400, and writes `Inventory.location_uuid` alongside `location_id` for compatibility.
- **Legacy fallback removal (UUID-only joins):** removed frontend `product.location_id` / label-based fallback matching from core Magazyn/report paths (`frontend/src/pages/WarehouseDesigner.tsx`, `frontend/src/pages/WarehouseDesigner/useDesignerMagazynState.ts`, `frontend/src/components/warehouse/magazyn/MagazynProductsSidebar.tsx`) so assignment/inventory joins are UUID-driven only; `frontend/src/pdf/utils/productLocationReportDataBuilder.ts` also removed `location_id` fallback and now keys location grouping strictly by UUID fields.
- **Single source of physical truth in reports:** enforced reporting semantics where physical stock comes only from inventory (`SUM(inventory.quantity)` by `location_uuid`) and `product.quantity` is treated as expected/system stock. `frontend/src/pdf/utils/executiveReportDataBuilder.ts` now computes quantity reconciliation (expected vs actual), mismatch share, and top deltas; `frontend/src/pdf/WarehouseExecutiveReportPDF.tsx` renders these differences in KPI + decision page; `frontend/src/pdf/utils/productLocationReportDataBuilder.ts` keeps inventory-only logic and removed temporary debug logging; `frontend/src/types/warehouse.ts` comment clarifies `quantity` as expected/system value.
- **Inventory as single physical source in backend flows:** migrated stock read paths to inventory and removed stock writes from operational utilities. `backend/api/inventory_api.py` now uses `Inventory` for list/create/get; `backend/api/picks.py` and `backend/services/wave_service.py` now allocate/decrement from `Inventory`; `backend/services/distribute_import_stock_service.py`, `backend/services/generate_test_stock_service.py`, and `backend/services/import_service.py` now write inventory rows only. `backend/services/sync_inventory_to_stock_service.py` is kept as backward-compatible no-op with deprecation warning. `backend/models/stock.py` now emits a one-time warning when Stock ORM is read or written: `"Stock table is deprecated - using inventory instead"`.
- **Frontend active-bin filtering:** added `is_active?: boolean` to `BinState` and new helpers `isBinActive` / `activeBinsForRack` in `warehouseUtils`. Updated key layout-consumer paths to filter inactive bins: map rendering (`RackLayer`), inventory mapping (`inventoryMaps`), capacity/report builders (`computeCapacity`, `buildWarehouseStructureReportData`, `productLocationReportDataBuilder`), main designer calculations (`WarehouseDesigner.tsx`), magazyn sidebars (`MagazynProductsSidebar`, `ProductLocatorSidebar`), and location exports/import mapping (`DesignerExport`, `LocationMappingExportImport`).
- **Orphaned inventory detection:** `backend/api/inventory_api.py` now detects rows where `inventory.location_uuid` exists but no active `Bin` exists for that UUID, logs warning lines with `location_uuid`, `product_id`, and `quantity`, and marks response items with `orphaned_inventory=true`. `backend/schemas/inventory.py` extends `InventoryReadWithNames` with `orphaned_inventory: bool`.
- **Product Location Report source refactor:** report pipeline now uses `products.assignedLocations` as the only source (no `/inventory` dependency for this report). `ProductLocationReportPage.tsx` removed inventory fetch and normalizes `assigned_locations` -> `assignedLocations` from products API response; `productLocationReportDataBuilder.ts` now builds per-product totals and location lists from assigned locations, resolves labels/storage via layout bins, keeps max-50 default, and logs grouped product counts.

## 2026-03-22

- **Warehouse structure PDF (premium dashboard):** `WarehouseStructureReportView.tsx` — header „Projekt magazynu”, KPI 2×2, typy magazynowania z %, sekcja jakości danych (`jakoscDanych`, liczby tylko przy problemach), mapa SVG + legenda kolorów szablonów, karty szablonów (nazwa + wymiary + mini-siatka). Frontend pobiera PDF z `POST /reports/warehouse-structure/pdf` (stary `POST /warehouse/structure-report-pdf` nadal działa). Szablon Jinja `warehouse_structure_report.html.j2` zsynchronizowany z widokiem.

- **Warehouse structure report (produkcja):** Etykiety typów PL (Podstawowe / Zapasowe / Uszkodzone / Sklepowe), szablony: nazwa + KPI (regały, poziomy, lok. na regał) + jedna **przykładowa adresacja** z pierwszego regału grupy (`przykladowaAdresacja`), bez pełnej listy regałów/lokalizacji. PDF: trzy strony (przegląd | mapa | szablony), zwarte marginesy, `scale` 0.94 w Puppeteer.

- **Warehouse structure report data:** `frontend/src/reports/buildWarehouseStructureReportData.ts` — `buildWarehouseStructureReportData` returns Polish-keyed payload: unique `location_uuid` counts, `binVolumeDm3` only (no rack formulas), storage PRIMARY/RESERVE/DAMAGED/SHOP, building `building_*` only, template groups with rack-instance dimensions + catalog name or `"Preset / Custom"`, plus `jakoscDanych` (skipped bins, duplicate UUIDs, racks without template).

- **Frontend reports module (structure only):** `frontend/src/reports/` — static `REPORT_DEFINITIONS`, `runReport` + `flattenSnapshotMetrics` (transform-only, no metric recomputation); `LocationMetricGranule` for UUID-keyed grouping/filters; `ReportsPage` not implemented yet.

- **Frontend warehouse metrics engine:** `frontend/src/metrics/` — pure TypeScript (no React/API): `computeCapacity`, `computeOccupancy` (inventory load volume), `computeInventoryValue`, `computeSpaceUtilization`, aggregator `computeWarehouseMetrics`; joins on normalized `locationUUID`; capacity deduped per UUID. UI/PDF not wired yet.

- **Frontend logging:** `src/utils/logger.ts` — `log` / `warn` only when `import.meta.env.DEV`; `error` always uses `console.error`. `DEBUG` flag for optional verbose traces. Replaced `console.log` / `console.warn` across the app; removed noisy designer/canvas/path debug blocks. Production builds stay quiet.

- **Warehouse PDF fonts:** Roboto static TTF (`Roboto-Regular.ttf`, `Roboto-Bold.ttf`) in `frontend/src/pdf/fonts/`, imported in `generateWarehousePDF.tsx`; `pdfConfig.ts` toggles `PDF_EMBED_ROBOTO` (false → Helvetica, skip `Font.register`). `safeText.ts` (`pdfStr`, `pdfInt`, `pdfNumFixed`) hardens dynamic `<Text>`. Inter removed from PDF path.

- **Warehouse PDF (react-pdf):** Designer export — wartość/zajętość: `buildPdfReportData` + `generateWarehousePDF`. **Struktura magazynu:** HTML (Jinja2) + Puppeteer (`POST /warehouse/structure-report-pdf`), nie react-pdf. Legacy jsPDF w `components/warehouse/reports/` nieużywane w tym przepływie.

- **Warehouse PDF reports (legacy note):** Earlier iteration used jsPDF variants (`WarehouseReportExecutive` / `Operations` / `Technical`) with shared metrics and vector floor plan; superseded by react-pdf stack above.

- **Dashboard occupancy from DB:** `GET /warehouse/occupancy-metrics` aggregates `inventory` × `products.volume` (no `bin.current_load_dm3`); buckets from layout `Bin.storage_type` via `Location.location_uuid`, else `Location.type`. Magazyn dashboard uses API when available, falls back to bin `usedVolumeAtBin`.

- **Dashboard storage-type volume (PRIMARY / RESERVE / DAMAGED):** One pass over distinct slot keys (`locationUUID` or `rackId-level-segment`); `usedVolumeAtBin` split by `normalizeStorageType` — damaged no longer folded into primary; `MagazynDashboardPanel` shows Uszkodzone dm³; `WarehouseLegend` stats optional `damagedUsedDm3`.

- **Warehouse dashboard product qty/volume:** `quantityAssignedInLayoutBins` now sums **once per `location_uuid`** (inventory wins, else `assigned_locations`); legacy `location_id` + `quantity` adds **at most once per label** (fixes full `p.quantity` × N bins). `validLayoutLocationUUIDs` / `uuidToBin` / `uuidToRackId` use `binLocationUuidFromBin` (camel + snake).

- **Magazyn rack side view (weight + packing):** `fetchProductsForMap` now maps `weight_kg` / `weight` and resolves legacy `location_id` via `assignedLocationEntryUuid` (API `location_uuid`). `useDesignerMagazynState` uses `binLocationUuid(bin)` for inventory/assignment joins. `RackSideViewGrid` hover UUID accepts snake_case.

- **Magazyn dashboard stats (occupied only):** `WarehouseDesigner` — `globalLocationStatsForLegend` counts primary/reserve/damaged bins only when `usedVolumeAtBin(bin) > 0`; `productsAssignedVolumeDm3` is Σ `usedVolumeAtBin` over all bins (aligned with per-bin load, not product-row sum or max capacity).

- **Row draw (cursor-based):** `rowDrawRackPositionsAlongCursor(startAlong, cursorAlong, step)` — mousedown → cursor with `dir = ±1`, no span/count. Preview (`WarehouseCanvas`), `countEmptyRowSlotsInDraw`, `appendEmptyRowToLayoutState`, catalog stamp use raw `start`/`end` on the row axis; positions are grid-filtered (no `maxSlots`/`maxCount` slice). Preview length: `max(along) − min(along) + footprint`. Dev `console.log` in preview when `import.meta.env.DEV`.

- **Designer mouse → grid (zoom/pan):** `designerMouseUtils` — `clientToSvgUserPoint` uses `createSVGPoint()` + `matrixTransform(getScreenCTM().inverse())` on `#warehouse-canvas`; `getCellFromWarehouseLayoutSvg` maps SVG user coords with viewBox size to grid cells (fallback: rect only if CTM missing). Wall hit / drag along wall use the same CTM path. DEV: one-time `console.log` of CTM + warn if null.

- **Template row horizontal:** `appendRowWithTemplateToLayoutState` now branches: **horizontal** → `appendHorizontalRowWithTemplateFromCursor` (same cursor + `step = cellW + layoutCmToCellsX(rowGapCm)` as preview/empty row); **vertical** unchanged (span + greedy). `countPlaceRowWithTemplateRacks` matches: horizontal uses cursor count; vertical greedy unchanged. `rowGapCm` added to append + count APIs; `WarehouseDesigner` modal counts pass `rowGapCm`.

- **Row draw geometry unified:** `rowDrawSegmentExtents` in `warehouseUtils` (min/max per axis, `extentX`/`extentY`, `isHorizontal = extentX >= extentY`). Gap uses `layoutCmToCellsX/Y(layout, rowGapCm)` in preview, `appendEmptyRowToLayoutState`, `countEmptyRowSlotsInDraw`; template counts use `layoutCmToCellsX/Y` in `countPlaceRowWithTemplateRacks`. Minor axis locked to **mousedown**: horizontal `lineY`/`startY` = `start.y`, vertical `lineX`/`start.x` = `start.x`. `countEmptyRowSlotsInDraw` / `countPlaceRowWithTemplateRacks` now take `layout` as first arg.

- **Row draw: cell picking under pan/zoom:** `getCellFromWarehouseLayoutSvg` maps `clientX/Y` with linear `getCellFromClientPosition` using the **parent** of `#warehouse-canvas` (the `warehouse-map-canvas` transform layer), not SVG `getScreenCTM()` (CTM could disagree with the layer that receives pointer events). Canvas SVG has `id="warehouse-canvas"`. Dev logs in `useDesignerMouseHandlers` `handleCanvasMouseMove` when row tool active: `CELL FROM MOUSE: cell.x` and `{ startX, endX, rawSpan, grid_cols }`.

- **Row draw preview (designer):** `WarehouseCanvas` — `computeRowDrawGhostPreview` caps slot/rack count by warehouse grid only: `maxSlots = floor((availableAlongAxis − rackFootprint) / step) + 1` (horizontal: from `x0` to `grid_cols`; vertical: from `y0` to `grid_rows`), then `count = min(desiredFromDragSpan, maxSlots)`. Row length uses full extent in cells including gaps: `(count − 1) * step + orientedW|H`, converted to meters via `layoutCellsToMetersX` / `layoutCellsToMetersY`. `RowPreviewOverlay` and toolbar show **`X.X m`** instead of **`kom.`**.

- **Rack name UNIQUE (layout save 500):** `warehouse_layout_service._save_layout_racks_by_identity` deactivates racks whose UUID is not in the incoming payload **before** upserting payload racks (avoids two active rows with the same name in one transaction). `schema_upgrade.ensure_warehouse_layout_rack_name_unique_index` recreates `uq_warehouse_layout_racks_layout_name` as a partial unique index on `(layout_id, name)` **where `is_active = 1`** (inactive rows may keep old names; startup drops old index then creates new). Payload duplicate names still rejected by `_validate_unique_rack_names_in_payload` (400).

- **Layout save: row slot `rackId` + dimensions:** `layoutSavePayload.ts` — after `stripUndefinedDeep`, `alignLayoutDimensionsFromBuilding` sets `width_m` / `length_m` from finite `building_width_m` / `building_depth_m` when present; `normalizeRowContainerRackIdsToUuids` maps slot `rackId` to each rack’s `uuid` (by uuid, `id`, `rack_index`, or slot bbox vs rack grid rect), drops orphan refs. **Designer / generator:** new row slots store `rackId: rack.uuid` (`layoutGenerator`, `useDesignerRackPlacement`, `useDesignerRowOperations` place-row / fill-row / stamp-into-slot). **`rackMatchesSlotRackId`** in `warehouseUtils` centralizes slot↔rack matching (uuid or legacy id/index). **`canPlaceGroup` / `rackInGroup`**, **`useRackInteraction`**, **`RowLayer`**, **`WarehouseDesigner` delete**, **`DesignerRackPlacement` snap** updated for uuid-based selection and slot refs.

- **Layout save debugging:** `layoutSavePayload.ts` — `validateAndSanitizeLayoutPayload` strips `undefined`, coerces numeric fields, requires `racks`/`row_containers` arrays; optional `layout_id` must be numeric. `WarehouseDesigner` `saveLayout` logs `SAVE PAYLOAD`, blocks invalid payloads with snackbar, logs `ERROR RESPONSE:` from axios. Snackbars for 400/422/500 with `detail` when present. **`api/axios`:** removed request interceptor that appended trailing slashes to all paths (broke `PUT /warehouse/:id/layout` with redirects / wrong route).

- **Designer zoom persistence:** `useDesignerCanvas(layout.layout_id)` stores zoom under `warehouse_zoom_${layoutId}` (localStorage), clamped `0.2…2`, skips persist when `layout_id` is null. Hydrates on `layout_id` change; avoids double-write after load via `skipPersistAfterHydrateRef`. `WarehouseCanvas` document reset no longer forces `setZoom(1)` so stored zoom survives load; wheel/button zoom limits match `MIN_ZOOM`/`MAX_ZOOM` `0.2` / `2`.

- **Rack labels vs zoom:** `RackLayer` takes `zoom` (from canvas CSS scale). Text/badges use SVG `translate(cx,cy) scale(1/zoom) translate(-cx,-cy)` at each label’s center so screen size stays readable. Hidden when `zoom < 0.4` (IDs, quantity/type badges, route step markers, row-overview labels).

- **Warehouse canvas zoom + pan:** Scrollable `.warehouse-map-viewport` (`overflow-auto`, fills panel); inner `.warehouse-map-canvas` uses `transform: translate(pan) scale(zoom)` with `transformOrigin: 0 0`; wrapper size `width×height×zoom` for scrollbars. Ctrl/Cmd + wheel (non-passive) zoom with clamp `0.3…3` (`z - deltaY * 0.001`). Toolbar: +/−/percent; Reset sets `zoom` to 1, clears pan, scrolls top-left; layout switch also resets zoom to 1.

- **Warehouse map: pure grid rendering:** `WAREHOUSE_CANVAS_CELL_PX = 14` in `renderUtils.ts` (shared with `BASE_PX_PER_CELL`). Racks: `width×height×cellPx` only (`rackMapVisual.ts` — no cm factors, no zoom-based min size). Canvas: no CSS `scale()` on the map layer (pan `translate` only); scroll area = layout px; major/strong grid lines use fixed cell counts (10 / 50), not meters. Removed zoom +/- UI and Ctrl+wheel zoom; row preview shows length in grid cells. `RackLayer`: fixed label LOD (full). `VisualLayer` cart icon: no `scale()` transform.

- **Rack map rendering (UX grid, not real scale):** `rackMapVisual.ts` draws rack footprints from `rack.width`/`rack.height` × `cellPx` with optional perception factors `width_cm/120` and `length_cm/80` (axes swap when `rotationDegrees === 90`). Removed meter-based `getRackFootprintGridSpan` / `getRackFootprintMeters` from `warehouseGridMetrics.ts`. `clampRackRectLayout` / `getRackFootprintPixelBounds` no longer take layout; `RouteStopLayer` updated accordingly.

- **Canvas (fixed cell + scroll):** `CANVAS_CELL_SIZE_PX = 24`; layout `width`/`height` = grid × 24; scroll viewport `overflow-auto` fills panel; inner wrapper `width/height = layout × zoom` to size scrollbars; canvas `transform: translate(pan) scale(zoom)`, `transform-origin: 0 0`. No viewport auto-fit. `layoutDisplayScale` for rack LOD = `zoom × (24 / BASE)`. Wall helpers use `getSvgLayoutSizePx(svg, …)`.

- **Rack map min size + visual LOD:** `rackMapVisual.ts` — min screen footprint 20×10 px via layout-space clamp using `layoutDisplayScale`. Visual LOD: scale below 0.3 = stroke line on long axis; below 0.7 = filled rects only (no rack/type/qty/bin highlights); else full. `RackLayer` gets `layoutDisplayScale` (cell px ratio vs BASE); row-prefix overview labels only in full visual LOD.

- **Canvas zoom / pan:** `MIN_ZOOM=0.5`, `MAX_ZOOM=5` applies CSS `scale(zoom)` on the map; cell size stays 24px. View reset when `selectedWarehouseId` or `layout.layout_id` changes. “Dopasuj” resets zoom to 1, pan to 0, scroll top-left. `WarehouseMainView` canvas wrapper `overflow-hidden` so only the canvas viewport scrolls.

- **Rack label LOD (map):** `rackLabelLod.ts` — LOW (userZoom below 0.5): one row-prefix label per `normalizeRowPrefixLetters` group, centered on combined rack bounds; MEDIUM (0.5–1.2): every 5th rack per prefix (Y/X sort); HIGH: all rack ID labels. Opacity scales with `rackLabelOpacityForZoom`. `RackLayer` takes optional `labelZoom` (default 1); `WarehouseCanvas` passes `zoom`. Route/quantity/type badges unchanged.

- **Layout generator column count (auto-fill):** `columns` / `columnsFit` use `floor((usableWidth + gap) / (rackWidth + gap))` so spacing is not reserved after the last rack in a row (`layoutGenerator.generateFullWarehouseLayout`, `GenerateWarehouseLayoutModal` preview). Placement loop unchanged: boundary check is `x + rackWidth` only; gap applied after each placed rack.

- **Grid metrics (designer):** `WarehouseCanvas` row-draw ghosts, row length preview, catalog drag ghost, and 1 m / 5 m grid overlays use `warehouseGridMetrics` (`metersPerCellX/Y` from building ÷ grid). `pairedAisleOffsetCells` takes optional layout + perpendicular axis (`x` for vertical rows, `y` for horizontal). `useDesignerRackPlacement` uses `layoutCmToCellsX/Y` (including vertical row slot `reqW`/`reqD`). Dev `console.log` in `WarehouseDesigner` for `building_width_m`, `grid_cols`, `metersPerCellX` (and depth/Y).

- **layoutGenerator bounds:** `warehouseWidth`/`warehouseHeight` = `maxCols`/`maxRows` (grid cells from building). During horizontal placement: hard stop when `x + rackW > warehouseWidth` or `y + rackH > warehouseHeight` (plus non-negative checks when bounds enforced). `filterLayoutToWarehouseBounds` on horizontal result and again after vertical transform. Vertical transform no longer filters internally.

- **layoutGenerator vertical mode:** Vertical layout is no longer a separate placement loop. Exported `generateHorizontalLayout` (unchanged horizontal logic); `generateWarehouseLayout` calls it then `applyVerticalTransformFromHorizontal` (swap x↔y, width↔height on racks and row slots, `orientation: "vertical"` on racks/containers). Optional post-transform bounds filter vs `maxCols`/`maxRows` drops out-of-bounds racks and prunes slots; `console.log` BEFORE/AFTER racks. LTR/RTL on row containers unchanged by transform.

- **Generate warehouse layout modal:** User templates only (no presets); „Brak szablonów” when empty. Global „Kierunek numeracji regałów” (LTR/RTL) on all generated `RowContainer`s; „Kierunek numeracji lokalizacji (pierwszy rząd)” with automatic opposite bin direction for back-to-back pair rows (`binDirectionForGeneratorRow` in `layoutGenerator.ts`). Real-time fit vs `building_width_m` / depth when set (orientation-aware width/depth need); Polish OK/error copy; optional preview: total racks, estimated dm³, aisle count between rows. Generate disabled when fit fails or no templates.

- **Empty row + template UX:** `RowPrefixModal` always shows „Szablon regału” + „Wypełnij automatycznie” for empty-row and template draws (`templateOptions` = tenant `customTemplates` only). `appendEmptyRowToLayoutState` / `placeEmptyRow` accept optional `templateId`; `placePairedRowPair` (`PairedRowPlacementSpec`) mixes per-row auto-fill vs deferred fill. `effectiveRowAutoFill` + `resolveRowCatalogItemForRowModal` in `WarehouseDesigner`; `allowAutoFillWithoutTemplateSelection` keeps OK enabled when drawing from catalog preset not listed in dropdown.

- **Paired rows (dual aisle draw):** `RowPrefixModal` — toggle „Utwórz podwójny rząd”, two prefix/rack/bin sections, defaults (row2 prefix = `nextRowPrefixLetters`, bin = opposite, rack = same), name preview. `appendRowWithTemplateToLayoutState` / `appendEmptyRowToLayoutState` + `placePairedRowsWithTemplate` / `placePairedEmptyRows`; second row offset via `pairedAisleOffsetCells` + `shiftRowDrawForPairedRow` (template: `depth_cm` + `rowGapCm`; empty: `cellsToCm(DEFAULT_ROW_SLOT_H)` + gap). Single `setLayout` applies both rows or neither. Validation merges `generateRackNames` for both rows (`countEmptyRowSlotsInDraw` / `countPlaceRowWithTemplateRacks`).
- **Paired rows (per-row template):** `RowPrefixModal` — when paired + `rowWithTemplate`, dropdowns per row (`templateOptions` = `CATALOG_PRESETS` + `customTemplates`); keys `preset:<id>` / `custom:<uuid>` via `catalogItemTemplateKey` / `catalogItemFromTemplateKey`. `placePairedRowsWithTemplate` takes two `CatalogItem`s; aisle offset from row 1 `depth_cm`. `RowContainer.templateId` on template-filled rows (`rowContainerTemplateIdFromCatalogItem`). Single-row draw unchanged (no template in modal).

- **Row prefix modal (generated names):** `normalizeRowPrefixLetters` (letters only, max 4); `generateRackNames` + `validateGeneratedRackNames` vs `layout.racks` (case-insensitive, `effectiveRackDisplayName`); `countPlaceRowWithTemplateRacks` matches `placeRowWithTemplate` geometry. OK blocked with `Regały już istnieją: A1, A2, …` when any planned name exists. `RowPrefixModal` input strips non-letters. `getNextIndexInRow` compares normalized `rowPrefix`. `getProposedFirstRackLabelForStampFromCatalog` + stamp/row ops use normalized prefix. Save still uses `validateAllRackNamesInLayout`.

- **Rack names unique per layout:** `warehouseUtils` — `effectiveRackDisplayName`, `isRackNameUnique`, `validateLayoutRackNamesUnique`, `nextUniqueRackName`, `assignUniqueRackNamesToNewRacks`. Placement, row ops, append layout generation, paste/duplicate use `nextUniqueRackName` / `assignUniqueRackNamesToNewRacks`. `RackPropertiesSidebar` name field + inline Polish duplicate message; layout always stores draft so save stays blocked when invalid. `DesignerToolbar` disables save when `validateLayoutRackNamesUnique` fails; API 400 surfaces via snackbar. **Backend:** `_validate_unique_rack_names_in_payload` (400, same message) before rack save; `ensure_warehouse_layout_rack_name_unique_index` partial UNIQUE index best-effort on SQLite.

- **Frontend (display location labels):** `getDisplayLocationLabel(rack, bin, layout)` is the single UI string for full addresses (uses `getRackDisplayId` + `getBinDisplayLabel`). `getAllPositionsFromRacks` / `getPositionsFromLayoutRacks` set `locationAddress` to that; pickers, Products page, CSV import/export, Magazyn sidebars, elevation panel, rack properties, and side view use it. Sorting “other locations” and locator uses `compareLocationUuidsByLayoutOrder` (layout slot order + level + segment), not parsed display strings. UUIDs unchanged.

- **Frontend (row direction LTR/RTL):** `row_containers[].direction` drives rack display IDs (`getRackDisplayId`) and bin display labels (`getBinDisplayLabel`) without moving geometry. Wired through map (`RackLayer`), side view, sidebars, modals, elevation panel, location picker positions, and Magazyn product sidebar UUID→label maps. New/generated rows default `direction: "LTR"`.
- **RowPrefixModal:** Two fields below prefix — **Kierunek numeracji regałów** and **Kierunek numeracji lokalizacji** (LTR/RTL each); OK passes `{ rowPrefix, rack_direction, bin_direction }` into row creation. Legacy layouts may still have single `direction`; `resolveRowContainerRackDirection` / `resolveRowContainerBinDirection` fall back so both default from `direction` when new fields are absent. **RackSidebar** / **WarehouseCanvas** toolbar edit `rack_direction` and `bin_direction` independently. `getRackDisplayId` uses rack direction only; `getBinDisplayLabel` uses bin direction only.
- **RackSideViewGrid:** When `bin_direction` is RTL (via `resolveRowContainerBinDirection`), beam badges and bin tiles use visual index `vis` left→right while logical `segment_index` stays `bin = locs - 1 - vis`; `onBinClick(lev, bin)` and keys `${lev}-${bin}` unchanged. Labels still `getDisplayLocationLabel`.
- **InternalLayoutModal / ElevationPanel:** Shared helpers `isBinDirectionRtl` + `segmentIndexForVisualSlot` in `warehouseUtils` (same semantics as `getBinDirectionForRack` + RackSideView vis/bin mapping). **InternalLayoutModal** uses `Array.from({ length: locs }, (_, vis) => { const binIndex = segmentIndexForVisualSlot(...) })` — **no** `flex-row-reverse`; **ElevationPanel** uses `isBinDirectionRtl` for display reversal. **InternalLayoutModal** modal panel + bin row use `dir="ltr"` so a document-level RTL layout does not reverse the flex row (vis 0 stays visually left); tile `order: vis`; keys `binKey(levIdx, binIndex)`; bin lookup uses `rackFromLayout`.

## 2026-03-21

- **Frontend (layout designer rack click / URL):** `magazynMapInteractions = mainView === "magazyn" && searchParams.get("view") !== "layout"` gates Magazyn map rack/aisle pointer logic in `usePlacementInteraction` / `useRackInteraction` so `?view=layout` never gets elevation/side-view side effects from the shared hook. Fixed `mainView` init + `searchParams` sync (previously always resolved to layout). Clear elevation + `selectedRackIdForSideView` when switching Magazyn → Layout. Dev: `console.log("RACK CLICK", { selectedRackId, mainView, magazynMapInteractions })`.

- **Frontend (layout designer rack click):** `useRackInteraction` Magazyn-only branch now gates on `mainView === "magazyn"` (was `!== "layout"`), and no longer calls `setMainView("magazyn")`. Prevents accidental switch to Magazyn UI / side-view state when clicking a rack in layout mode. `usePlacementInteraction` uses `mainView === "magazyn"` instead of `isLiveView` for rack elevation. Removed unused `setMainView` from `useDesignerMouseHandlers` setters.

- **Frontend (`WarehouseCanvas` wheel + hit target):** Removed manual scroll, capture-phase wheel, and non-zoom `preventDefault`. Zoom: non-passive `wheel` on `viewportRef` only when Ctrl/⌘. SVG root `pointer-events: none`; interactive pieces `auto` (racks, aisles, row slots, walls, visuals, specials); building outline + ghosts `none`. Canvas mouse handlers moved from `<svg>` to the transformed HTML wrapper so empty map hit-testing targets a div → native wheel scrolls `overflow-auto` viewport. Dev: `console.log("wheel")` on viewport `onWheel` (non-modifier).

- **Frontend (`WarehouseCanvas` responsive fit):** `ResizeObserver` viewport size drives `baseFitScale = min(containerW/gridW, containerH/gridH)` for logical grid px (`grid_cols|rows × BASE_PX_PER_CELL`). Display scale = `baseFitScale * userZoom`; scroll wrapper sized to scaled footprint so overflow matches zoom. `fitToContent` divides by `baseFit` so bbox zoom stays in user-zoom space. Dev-only `console.log` `[WarehouseCanvas][dimensions]`. Magazyn and layout share the same component/path.

- **Frontend (designer vertical flex chain):** `WarehouseMainView` canvas scroll wrapper is `flex flex-1 min-h-0 flex-col overflow-auto` so `<main>` `flex-1` resolves; Magazyn map/rack scroll divs use `flex flex-1 flex-col overflow-auto`. Workspace row / white card / `DesignerGrid` / left rail rely on `flex-1` + `self-stretch` instead of `h-full` where it broke percentage height. Removed duplicate `h-full` on workspace row.

- **Frontend (designer flex min-width chain):** `MainLayout` content shell (`p-10`) and `PageContent` (`fillHeight` + default) use `min-w-0` so large canvas intrinsic width does not expand the main column. `WarehouseDesigner` workspace row: removed `-mx-6 -mb-6` negative margins.

- **Frontend (warehouse designer horizontal overflow):** Center columns use `flex-1 basis-0 min-w-0 max-w-full` so flex shrink isn’t blocked by wide grid content; scroll wrappers get `min-w-0 max-w-full`. `WarehouseCanvas` viewport dropped inline `overflow: hidden` (it overrode `overflow-auto`); toolbar row `min-w-0 overflow-x-auto`. Side rails `w-[260px|320px] flex-none`. `PageLayout` compact header: actions `flex-1 min-w-0 overflow-x-auto` so toolbar doesn’t force page width.

- **Frontend (warehouse designer vertical height chain):** `MainLayout` uses `overflow-hidden` (not `overflow-y-auto`) on the main column for `/designer` and `/warehouse-designer` so the page does not scroll. `PageLayout` `fillHeight`: `h-full max-h-full flex-1 min-h-0 flex-col overflow-hidden`, compact `PageHeader`, `PageContent` `flex-1 min-h-0 flex-col gap-0`. `WarehouseDesigner` workspace row + Magazyn white card + left rail use `h-full min-h-0`; map/rack scroll shells `h-full w-full overflow-auto`; layout path `DesignerGrid` / `WarehouseMainView` / canvas wrapper same. Side rails (`RackSidebar`, Magazyn sidebars, `RackPropertiesSidebar`, layout asides) get `h-full min-h-0`.

- **Frontend (warehouse designer flex parity):** `WarehouseDesigner` workspace row uses `flex flex-1 min-h-0 overflow-hidden min-w-0`. Magazyn map + layout designer share the same center pattern: outer `flex-1 min-w-0 min-h-0 overflow-hidden flex-col`, inner scroll `w-full h-full min-h-0 overflow-auto` around `WarehouseLayoutRenderer` / `RackSideViewGrid` / `WarehouseCanvas`. Removed `shrink-0` from Magazyn main stack; rack detail wrapped in `overflow-hidden` + scroll shell like map mode.

- **Frontend (Magazyn + designer horizontal flex / sidebars):** `WarehouseDesigner` main row stays `flex flex-1 min-h-0 min-w-0 overflow-hidden`; Magazyn left dashboard `w-[260px] shrink-0`. Magazyn map row is explicit `flex-row items-stretch overflow-hidden`; map column `flex-1 min-w-0 min-h-0 overflow-auto`. Magazyn right asides `w-[320px] shrink-0` (`ProductLocatorSidebar`, `MagazynProductsSidebar`; `TopProductsSidebar` already 320). Layout: `RackSidebar` fixed `w-[260px] shrink-0`; `WarehouseMainView` center column `overflow-auto` + `min-w-0`; visual/aisle/`RackPropertiesSidebar` panels `w-[320px] shrink-0`.

- **Frontend (Magazyn + designer vertical scroll):** `MainLayout` main column and padded content area use `min-h-0`; content wrapper is `flex flex-col` so `PageLayout` `fillHeight` can `flex-1` without forcing `min-h-screen`. `PageLayout` `fillHeight`: `flex-1 min-h-0 min-w-0 overflow-hidden` instead of `min-h-screen`; `PageContent` adds `overflow-hidden` when `fillHeight`. Magazyn map + sidebars row: `overflow-hidden`. `TopProductsSidebar`: dropped `h-full` for `self-stretch min-h-0` + inner `flex-1 min-h-0 overflow-hidden`. `ProductLocatorSidebar`: removed `h-fit` / `max-h-[calc(100vh-200px)]`; `self-stretch min-h-0` + scroll in `flex-1 min-h-0 overflow-y-auto` body.

## 2026-03-21

- **Frontend (warehouse canvas alignment):** Initial `pan` in `useDesignerCanvas` `{80,80}` → `{0,0}`. `WarehouseCanvas` viewport: removed `margin: 16px`, added `p-0 m-0`; `<main>` `items-stretch justify-start p-0 m-0`. `fitToContent` pan uses top-left of rack bbox (`-minXpx*zoom`, `-minYpx*zoom`) instead of horizontal center + `offsetY: 60`. Empty/loading placeholders: `items-start justify-start` instead of centered. `WarehouseMainView` canvas column: `p-0 m-0 items-stretch justify-start`.

- **Frontend (`WarehouseCanvas` wheel / page scroll):** Native `wheel` on white map `viewportRef` with `{ passive: false }`. Magazyn (`isLiveView`): wheel still zooms + `preventDefault`. Layout designer: plain wheel only `preventDefault` (no page scroll); **Ctrl / ⌘ + wheel** zooms. React `onWheel` removed (passive). Tooltip: „Ctrl lub ⌘ + kółko myszy: zoom” in layout mode.

- **Frontend (layout designer canvas height):** Removed `overflow-y-auto` from `WarehouseCanvas` wrapper in `WarehouseMainView` (replaced with `overflow-hidden` + `flex flex-col min-h-0`); dropped wheel hook on that wrapper so flex assigns full height to the canvas. `DesignerGrid` wraps `WarehouseMainView` in `flex-1 min-h-0 overflow-hidden`. `WarehouseDesigner` main row always `min-w-0 overflow-hidden`. `WarehouseCanvas` `<main>` + layout sidebars: `min-h-0 self-stretch` where needed for flex chain.

- **Frontend (layout designer wheel / scroll chaining):** `useWheelScrollBoundaryContain` on scrollable **side** asides only (`RackSidebar`, `RackPropertiesSidebar`, visual/aisle panels in `WarehouseMainView`). Canvas column uses `overflow-hidden`; interaction/zoom stays on `WarehouseCanvas` viewport.

- **Frontend (Magazyn wheel / scroll chaining):** `magazynMapScrollRef` + `magazynRackSideScrollRef` on Magazyn map and rack side scroll wrappers; `addEventListener("wheel", …, { passive: false })` prevents default at scroll top/bottom only so wheel does not propagate to the page.

- **Frontend (Magazyn scroll):** `WarehouseDesigner` sets `document.body.style.overflow = "hidden"` while `mainView === "magazyn"` (restore on leave/unmount). Magazyn main row: `overflow-hidden` + `overscroll-behavior: contain`; left dashboard `overflow-y-auto` + `overscroll-y-contain`; map column wrapper `overflow-y-auto` + contain; rack detail area scrollable with contain. `WarehouseCanvas`: `overscroll-behavior: contain` on viewport (and on `<main>` when `isLiveView`). `MagazynProductsSidebar`: `self-stretch min-h-0` + `overscroll-y-contain` (drops fixed `max-h-[calc(100vh-200px)]`). White card uses `flex-1 min-h-0` instead of `calc(100vh-200px)` height.

- **Frontend (Magazyn toolbar):** „Dopasuj” (`fitToContent`) hidden when `isLiveView` (Magazyn); still shown in layout designer (`WarehouseCanvas` zoom row).

- **Frontend („Pokaż etykiety”):** Single `showLabels` / `setShowLabels` state in `WarehouseDesigner` (renamed from `showRackLabels`). Magazyn map no longer forces labels on (`WarehouseLayoutRenderer` always receives the toggle). `WarehouseCanvas` / `RackLayer` / `VisualLayer` use `showLabels`. `RackSideViewGrid` takes `showLabels` and hides beam slot address badges when off (weight badges unchanged).

- **Frontend (Magazyn):** `hoveredLocationUUID` state in `WarehouseDesigner`; `MagazynProductsSidebar` `onHoverLocationUUIDChange` on location rows + „Aktualna lokalizacja” banner (UUID). `WarehouseCanvas` / `RackLayer` draw cyan bin overlay (`drop-shadow`) for sidebar hover; independent of selection / `highlightedBinUUIDs`. Cleared on empty-map click. **`RackSideViewGrid`:** optional `hoveredLocationUUID`; matching bin gets cyan tint/stroke overlay (`pointerEvents="none"`) under labels/bars, same UUID trim as map.

- **Frontend (Magazyn map / `WarehouseCanvas`):** Read mode supports `onReadModeCanvasBackgroundClick`: SVG `click` clears selection only when target is not inside `data-rack-interactive`, `data-special-location`, wall-elements layer, `data-row-empty-slot`, `data-visual-elements`, or `data-aisle-cell`. `WarehouseDesigner` wires it to clear map rack, layout `selectedRackId(s)`, bin focus, and product/map highlight state. Rack groups use `data-rack-interactive` and `mousedown` `stopPropagation` when rack click handlers exist.

- **Frontend (Pulpit magazynu / `WarehouseDesigner`):** „Całkowita zajętość” **used dm³** = Σ (`quantityAssignedInLayoutBins` × `product.volume_dm3`) = sum of `sortedProductsByVolume[].volumeAssignedDm3` (same as sidebar / top products; only piece qty × product volume, never bin capacity). `quantityAssignedInLayoutBins` now walks every layout bin with the same per-bin merge as `usedVolumeAtBin` (Stock wins, then assignment for UUID, then legacy `location_id === bin.label`). **totalCapacity** unchanged: Σ rack `total_capacity_dm3 ?? Σ binVolumeDm3`. Primary vs reserve used dm³ still from per-bin `usedVolumeAtBin` + `storage_type`.

- **Frontend (Magazyn „PRODUKTY W MAGAZYNIE”):** `sortedProductsByVolume` lists only products with stock or `assigned_locations` on current layout bin UUIDs; merged qty per bin like rack map (inventory overrides assignment per UUID); no bare `product.quantity`. `TopProductItem`: `quantityAssigned` / `volumeAssignedDm3`; UI „W lokalizacjach: X szt.” / „Objętość: … dm³”.

- **Frontend (`MagazynProductsSidebar`):** Live search via `useMemo`: filter name/SKU/EAN (case-insensitive `includes`), hide non-matches; sort by `productSearchRank` (prefix & field priority) then Polish name; empty query restores full `baseList`; map top-5 only when query empty.

- **Frontend (Magazyn):** Removed map toolbar „Szukaj produktu (mapa)”. Rack list search lives in `MagazynProductsSidebar` under „PRODUKTY W REGALE”, above summary; same `productSearchQuery` / filtering. Global catalog search + locator dropdown moved under „PRODUKTY W MAGAZYNIE” in `TopProductsSidebar` (same state, dark styling).

- **Frontend (Magazyn map):** `selectedProductId` state toggled from `MagazynProductsSidebar` (click product; Ctrl/Cmd+click opens product). `highlightedBinUUIDsForSidebarProduct` from inventory + `assigned_locations` (layout UUIDs). `RackLayer` draws per-bin overlays; racks without a match dim. `canvasHighlightedRackIds` skips rack-level product highlight while sidebar bin mode is active.

- **Frontend (`MagazynProductsSidebar`):** Under "PRODUKTY W REGALE", rack summary line: unique product count and total szt. (reuses `baseList` when rack-wide; map mode sums `totalQuantity` from `rackProductsForMap`; inventory matches per-row rack-total logic). "Brak produktów" when zero.

- **Frontend (Magazyn clear rack):** "Opróżnij regał" is hidden when the rack has no `assigned_locations` entries on its bins (same UUID scope as clear). `MagazynRackDetailHeader` uses `hideEmptyRackButton` from `clearRackConfirmPreview.assignmentCount`; `MagazynProductsSidebar` only renders the button when `hasAssignedProductsOnRack`.

- **Frontend (confirm modals, copy only):** Clear-rack confirmation in `WarehouseDesigner` shows rack display id (`getRackDisplayId`) and a count of assignment rows that would be removed (same bin UUID set as the clear action), with Polish pluralization. Single-location remove in `MagazynProductsSidebar` keeps the location line and optionally shows product name.

- **Frontend (inventory UUID-only):** Removed `byLocationName`, `normalizeInventoryLocationKey`, and label-based rack grouping from `buildInventoryMaps`; stock ↔ bin joins use `location_uuid` only. `useDesignerMagazynState` / `MagazynProductsSidebar` / `WarehouseDesigner` / `ProductLocatorSidebar` updated accordingly; legacy `product.location_id` (no `assigned_locations`) still matched to bins by display label for scope only. `uuidToStorageType` / `uuidToBin` for primary vs reserve splits.

- **Frontend (MagazynProductsSidebar):** With `inventoryMaps` + single-bin filter, `quantityAtLocation` now falls back to summed `assigned_locations` quantities when `assignedLocationEntryUuid` matches `selectedBinUUID` and inventory qty for that bin is 0 (inventory wins when &gt; 0). `currentLocation` banner uses assignment qty in that fallback. `volumeAtLocation` unchanged formula, now non-zero when assignment-only.
- **Frontend (inventory ↔ assignments by UUID):** `buildInventoryMaps` adds `byLocationUuid`, `normalizeInventoryLocationUuid`, layout `locationUuidToRackKey`, and prefers UUID for `byRackId` grouping when present. `useDesignerMagazynState` adds `getInventoryRowsForBin` (UUID first, then `getInventoryRowsForBinLabel`). `MagazynProductsSidebar` uses `invRowMatchesBin`, canonical loc keys (`inventoryRowCanonicalLocKey` + `nkToPrimaryUuid`), assignment merges keyed by slot UUID; labels for display only.

- **Frontend (inventory location keys):** `normalizeInventoryLocationKey` in `inventoryMaps.ts` (trim + collapse whitespace) used for `byLocationName` / rack grouping, `useDesignerMagazynState` bin lookups and `selectedRackBinLabels`. `MagazynProductsSidebar` maps UUID → same key via layout bins only (no `locationAddress` merge), DEV warnings for UUID without layout label or unknown assignment UUID; `otherLocations` rows use inventory display string or layout label, not raw map keys.

- **Frontend (MagazynProductsSidebar):** Rack-wide (`!filterToSingleBin`): `quantityAtLocation` sums per-bin on selected rack (inventory qty if &gt;0 else assigned); `otherLocations` includes on-rack bins except `excludeBinNK` when a bin is focused; assignment merge no longer skips all `selectedRackBinUUIDs` / `selectedRackBinLabels`.

- **Frontend (MagazynProductsSidebar):** In inventory mode, `otherLocations` merges assignment-only bins: UUID → label via `uuidToLabel` (fallback `locationAddress`), skip selected bin / current rack, skip labels already in inventory `qtyByLoc`; inventory quantities unchanged; `assignAdd` map sums per label then merges to avoid duplicates.

- **Backend (layout save → assigned_locations cleanup):** After `_upsert_layout_aisles`, `WarehouseLayoutService.save_layout` runs `_cleanup_product_assigned_locations_after_layout_save`: loads active bin UUIDs for the layout, all layout bin UUIDs (incl. inactive), and tenant-wide active bin UUIDs; one query for all tenant products with `assigned_locations`; filters entries in Python; `bulk_update_mappings` when rows change. Preserves assignments still active in other warehouses; drops inactive/removed bins for this layout. Logs removed reference count and product count. No inventory changes.

- **Backend (product assigned_locations):** `POST/PUT` products validate `assigned_locations` as before (active bin/rack, tenant layout, single warehouse). On failure **400** body is `detail: { detail: "Invalid location assignment", invalid_uuids, optional not_found / inactive / wrong_warehouse }` (FastAPI wraps in outer `detail`). A second query with `include_inactive=True` runs only on failure to classify missing UUIDs.

- **Frontend (routing core replaced):** Replaced grid-step path generation with an aisle-based routing model in `aisleGraphRoute.ts`. Aisles are auto-derived from horizontal free gaps between rack rows, stops/start/packing are snapped to nearest aisle lines, and routes are built only from orthogonal aisle legs (`horizontal` on aisle + `vertical` connectors) with rack-collision checks. Output is a minimal turn-based polyline (no micro-segments).

- **Frontend (Route Overview Mode):** Added `routeViewMode: "live" | "overview"` in `WarehouseDesigner` (auto-defaults to `overview` when a new route appears). In overview, canvas uses full route polyline (`routePathFull` fallback to single-leg path), shows all stops, hides live step focus (`currentStopIndex`, `highlightedStopIndex`, `routeStepBadges`), and shows START/PACK markers. Added sidebar toggle: **"Widok trasy: LIVE / CAŁA TRASA"**; overview hides "Następny krok", "Krok i/j", and leg-only metrics.

- **Frontend (route visualization UX):** `PathLayer` route mode now uses a strong opaque blue line (`3-4px`), repeated arrow glyphs aligned to polyline direction, `1..N` step circles projected onto the path itself, and a subtle animated flow overlay (`stroke-dashoffset`). `RouteStopLayer` now draws short connector lines from each stop to its rack edge; START marker styling is aligned with route blue palette.

- **Frontend (grid route pathfinding):** Added **`gridRoutePathfinding.ts`**: **`buildWalkabilityGrid`** (cells not under rack footprints), **`nearestWalkableCell`**, **`findPathAStarGrid`** (4-neighbors, Manhattan heuristic), **`gridPathToCellCenterPoints`**. **`buildAisleGraphRoutePath` / Segment / PickStartToRack** now chain A\* legs instead of aisle-centerline graph + straight Manhattan. **`getRackRouteWaypoint`** uses nearest walkable cell to pick point.

- **Frontend (route geometry):** `useDesignerRowState` + **`aisleHalfWidthCellsFromCm(aisleWidthCm)`** run before **`routeStops`**; stops use **`getRackRouteWaypoint`** (snapped pick) or **`getRackPickPointCell`**; aisle-graph **`build*`** helpers receive **`aisleRouteOpts`**. **`RackLayer`** takes **`routeStops`** and draws step badges at pick-cell centers with a 6px aisle nudge.

- **Frontend (route step navigation):** **`routeStepIndex`** drives a single leg: **`buildAisleGraphRoutePathSegment`** (rack→rack) or **`buildAisleGraphRoutePathPickStartToRack`** (one rack). Canvas shows **`routeSegmentPath`** only; **`routeStopsForCanvas`** limits PathLayer fallback. **`RackLayer`** uses **`routeStepBadges`** only (large current / small next stop numbers); all-rack order pills removed. **`showRouteEndpointMarkers`** false when a route exists (no START/PACK on map). Sidebar: full list with **current row highlight**, **Krok i/j**, **Odcinek** (leg m), **Całość** (total m), **Następny krok**; **`clearRoute`** resets step index.

- **Frontend (route visualization):** Path renders **under** rack tiles (after aisles, before `RackLayer`) so the line reads as corridor-only; **`PathLayer`** uses thin stroke (`2`), **`#3b82f6`**, opacity **0.6**, no end arrow; graph / visual routes run through **`mergeCollinearRoutePointsForDisplay`** after merge. **`RouteStopLayer`** shows only **START** and **PACK** (no per-stop path circles). Visit order is **`[label] (n)`** badges top-right on racks via **`routeOrderByRackId`** (no longer hidden when `showRoute`). Mid-path **`pathMarkers`** suppressed when a computed route with `routeStops` is active.

- **Backend:** `save_layout` — after racks/bins persist, **`flush()`** then **`_ensure_missing_locations_for_layout_bins`** creates any missing `Location` rows for bin `location_uuid` (same geometry as bin sync), then **`_sync_storage_locations_from_bins`**. Batched `Location.location_uuid IN (...)` queries (`_LOCATION_UUID_IN_BATCH=500`). If any bin UUID still has no `Location`, logs **`[CRITICAL] missing Locations for bins`**.

- **Backend:** `WarehouseGraphService.build_graph` — `LocationNode` deletes by `node_id IN (...)` are **batched** (500 ids) to avoid SQLite “too many SQL variables”. `WarehouseEdge` / `WarehouseNode` deletes use a single `warehouse_id` filter (unchanged). Log: `[Graph] deleting LocationNode rows in batches: total=…`.

- Aisle routing: **`findPath`** (BFS on aisle graph), **`mergeCollinearOverlappingSegments`** so parallel aisle rects on one corridor connect, **rounded node coords** for stable graph ids, **`computeManhattanPathLengthCells`** + sidebar **route length** from **`routeGraphPolyline`** (not straight-line between waypoints).

- Picking route **geometry** is **strict aisle-only**: graph is built **only** from user-drawn `layout.aisles` centerlines (no grid, no perimeter). Nodes = intersections + segment endpoints + projected START/packing/rack access; edges only along those segments. Each rack gets **one** nearest aisle and **access on the aisle line** (`getRackAisleAccessPoint`). No Manhattan / free-space fallback — `buildAisleGraphRoutePath` returns `null` on failure; `PathLayer` treats explicit `null` as **no** route line (no `buildVisualRoutePath` fallback). Validation: path must lie on aisle segments; duplicate undirected edges rejected.

- Picking **visit order** is no longer implicit click order / TSP: `computePickingRouteOrder` (`aisleRouteOrder.ts`) clusters racks into horizontal or vertical **aisles** (union-find on center Y or X within tolerance), sorts aisles along the cross axis, orders racks **along** each aisle, applies **serpentine** (alternate direction) with first aisle direction chosen from **pick start** vs access points; `routeStops` / sidebar / badges use this order; **Optymalizacja** reapplies the same ordering to `routeRackIds`.

- Picking route visualization simplified: `PathLayer` in route mode draws a **single** thick rounded polyline with **one** end arrow (no segment halos, repeated arrows, or debug logging); path vertices collinear-deduped; `RouteStopLayer` shows only START / numbered stops / PACK with markers nudged into the aisle (no leader lines); duplicate order badges removed from `RackLayer` when the route overlay is active.

- Picking route rendering now uses rack **access points** (in front of racks: horizontal → below footprint, vertical → to the right) via `rackAccessPoint.ts`, not rack centers; visual path (`buildVisualRoutePath`) no longer uses lane-merged stop positions, appends optional **packing** end node, and `PathLayer` hides polyline marker labels when `routeMode`; `RouteStopLayer` drops A1/A2 rack-label pills and adds an orange **PACK** marker at the chosen packing cell; backend route segments extended with a last leg to packing; `routeEndCell` / `routePackingCell` wired through canvas.

- Route planning UX in layout designer: toolbar button shows active state (“Zakończ trasę” / outline vs filled), canvas uses `isRoutePlanningMode` for pointer cursor and stronger rack hover, sidebar shows contextual hints, `routeMode` for mouse handlers now follows `isRouteActive` (was incorrectly tied to non-empty route), removed `onRackClickPassthrough` during route mode to avoid double-toggle with mousedown, `addRackToRoute` logs `ROUTE CLICK`, and auto single-rack path fetch skips while route planning is active.

## 2026-03-16

- Added a project rule in `.cursor/rules/memory-bank.mdc` that requires use of the workspace `memory/` directory for persistent context.
- Initialized the memory bank with canonical files: `project-context.md`, `current-context.md`, and `change-log.md`.
- Recorded the current route-visualization direction: stop-first markers on racks, demoted path styling, and frontend-only scope.
- Updated `RouteStopLayer` to make stop markers dominant: larger solid blue markers with white numbers, stronger current-stop highlight, and a visually distinct green START marker.
- Updated `PathLayer` to further demote the route path with thinner, lighter dashed strokes while keeping highlighted path-to-stop guidance secondary.
- Updated `RouteStopLayer` again for step-by-step picking UX: only a small window around the active step is shown, completed stops are reduced and faded, the next stop stays visible, and the current stop is the dominant focal point.
- Reverted the designer route layer from step-by-step picker UX back to full-route planning UX: all stops visible again, uniform blue numbered markers, no checkmarks, and rack labels kept readable with lighter label pills below the markers.
- Added a `showRoute` toggle in the Warehouse Layout Designer UI to show or hide route visualization layers (`PathLayer` and `RouteStopLayer`) without touching route generation or stored route data.
- Updated planning-view route markers so each stop shows both its number and rack label directly on the map, and changed the route path to an even subtler start-to-end gradient cue for direction.
- Updated route preview flow styling so the path reads as one continuous route: stronger solid path, visible end arrow, darker start-to-end gradient, and connector lines from approach points into stop markers.
- Added a render-time path simplification pass in `PathLayer` to reduce micro-zigzags and near-collinear node noise so the route reads like aisle movement rather than raw graph output.
- Added a stronger visibility mode for route preview rendering: thick solid blue path with white halo, repeated directional arrow glyphs, and runtime console logs in `WarehouseCanvas` / `PathLayer` to verify `pathPoints`, `showRoute`, and path-layer rendering.
- Fixed route visibility ordering in `WarehouseCanvas` by moving `PathLayer` above `RackLayer` while keeping `RouteStopLayer` on top.
- Replaced route preview path construction in `PathLayer` so route mode now draws clean orthogonal segments from `START` to each `routeStop.position` with at most one turn per segment, instead of drawing raw backend node geometry.
- Hardened `buildVisualRoutePath` so every leg now emits explicit `start/turn/end` geometry and logs the generated point count plus the first segment points for debugging broken route rendering.
- Aligned Magazyn route rendering with layout by reusing the same shared `PathLayer`; in read mode, stop badges are now positioned relative to the route line via `RouteStopLayer` instead of being anchored to rack centers.
- Strengthened Magazyn product-based rack highlighting in `RackLayer`: matching racks now get a stronger blue outline/glow and non-matching racks dim to the background while preserving selection and dragging priority.
- Extended the Magazyn product locator with a frontend-only per-rack quantity map in `WarehouseDesigner`, passed it through `WarehouseCanvas`, and rendered quantity pills on highlighted racks in `RackLayer`.
- Implemented layout identity step 1: added persisted `uuid` on backend racks and `location_uuid` on backend bins, exposed them through layout GET/PUT, backfilled missing values for existing layouts, preserved incoming rack/bin primary keys during full-replace saves, and stored rack UUIDs plus bin location UUIDs in frontend layout state.
- Implemented layout refactor step 2A in `warehouse_layout_service`: extracted the old destructive rack save into a legacy helper, added a `uuid`-based rack upsert path for in-place rack updates, kept existing bins for matched racks untouched, still creates default bins only for brand-new racks so new racks remain usable, and deletes removed racks via rack-row delete without adding new explicit bin-delete logic.
- Implemented layout refactor step 2B across backend models/schema and `warehouse_layout_service`: added `location_uuid` to `Location`, added `is_active` flags to racks/bins, matched bins by `location_uuid` for in-place updates, created new bins only when no UUID match exists, marked removed bins inactive instead of deleting them, updated `Location` rows by UUID so label renames no longer recreate them, and filtered inactive racks/bins out of `GET /warehouse/layout`.
- Hardened soft deletes globally: added `Location.is_active`, added activity indexes in schema upgrades, introduced a SQLAlchemy session-level active-only filter for `Rack`/`Bin`/`Location`, updated layout save helpers to opt into `include_inactive=True` only where reconciliation/reactivation is required, and now deactivate linked `Location` rows when bins become inactive so inactive layout elements no longer leak into routing, product mapping, or other operational reads.
- Implemented layout refactor step 2C part 1 for aisles in `warehouse_layout_service`: replaced delete-all aisle persistence with ID-based upsert, kept frontend aisle IDs round-tripped as the stable identity source, updated existing aisles in place, created new aisles when needed, and deleted only aisles omitted from the payload.
- Implemented layout refactor step 2C part 2 for `visual_elements`: added `visual_elements_json` to `WarehouseLayout`, extended schema upgrade to create the column on existing DBs, added `VisualElementSchema` plus `visual_elements` to `WarehouseLayoutPayload`, and updated `save_layout` / `get_layout` so frontend visual elements persist as JSON without relational modeling.
- Implemented layout API step 3: added a unified `GET /warehouse/layout` response shape `{ layout, special_locations }`, factored special-location payload building into a shared helper while keeping the legacy `/warehouse/{warehouse_id}/special-locations` endpoint intact, updated `WarehouseDesigner` to load special locations from the unified response instead of a second initial fetch, and patched other frontend layout consumers to read `data.layout ?? data` for compatibility.
- Hardened rack persistence in `warehouse_layout_service`: added pre-save integer coercion/validation for rack footprint fields, skip invalid new racks before DB insert with a clear warning log, and preserve existing rack `x`/`y`/`width`/`height` during partial updates when payload values are `None`, preventing `NOT NULL` crashes on `warehouse_layout_racks.x/y`.
- Implemented `StorageLocation` sync from bins: added `location_id` and `is_active` to `storage_locations`, extended SQLite schema upgrades and indexes, introduced `_sync_storage_locations_from_bins()` in `warehouse_layout_service`, call it from `save_layout()` after rack/bin/location sync, ensure active bins have exactly one active storage-location record linked through `location_uuid -> Location`, and stop deleting storage-location rows when bins are deactivated.
- Completed frontend rack-type persistence wiring: `RackState.rack_type` is now required, template creator supports `rack_type`, manual rack placement supports WH/STORE selection, all placement/generator flows use template/default rack_type instead of hardcoded `"warehouse"`, layout save always sends rack_type, and racks render a small WH/STORE badge in `RackLayer`.
- Enhanced rack side-view readability for storage/location types: added per-slot top-right storage-type icons in `RackSideViewGrid` (matching editor icons), added tooltip labels and click-to-highlight same-type slots, and extended `WarehouseLegend` rack-detail mode with a new "Typy lokalizacji" section that shows only storage types used by the currently selected rack.
- Refined rack-type UX and behavior in Warehouse Designer: active rack type now filters sidebar custom templates (`used` and `available`) and magazyn dashboard template usage, switching type clears template-dependent selections, and generation modal now filters custom templates by active type while applying the active type to preset-generated racks.
- Updated rack type labels in UI controls/badges from `WH/STORE` to `Magazyn/Sklep` (store badge label now `Sklep`).
- Moved `Zapisz układ` from sidebar bottom into the top designer toolbar (shown in layout mode), removing duplicate save placement and making save action globally visible.
- Improved rack template preview realism in `TemplateCreator`: removed preview mode tabs, switched to a single physical-proportion preview, rendered level heights proportionally, added low-opacity orange travers separators between levels, showed `SZ/GŁ/WYS` labels instead of depth scaling, and added responsive compact rendering that hides detail lines for narrow slots.
- Added physically correct travers model to rack templates in `TemplateCreator`: beam inputs are now rendered only between levels (`levels - 1`), no beam input for top level, single-level templates show no beam input, and saved `levelConfig` now carries optional `beamBelowCm` on non-top levels for accurate rack semantics.
- Removed redundant visual-mode toggle from `InternalLayoutModal` (rack layout editor): deleted `Wizualny/Kompaktowy` switch, kept compact layout as the only mode, and removed related conditional rendering/state branches for a simpler UI and cleaner code path.
- Enforced derived slot width consistency in rack internal layout flow: width editing is now removed in `InternalLayoutModal`, slot width is computed as `rack.width_cm / locationsPerLevel` and shown read-only (`SZ ... cm`), internal-structure widths are normalized on load and internal-layout save in `WarehouseDesigner`, and bin `width_cm` is recalculated to prevent width drift, overflow inconsistencies, and capacity mismatches.
- Fixed template/layout dimension drift by prioritizing template dimensions for template-based racks: added template-driven normalization in `WarehouseDesigner` load flow/effect to override mismatched slot `width_cm`/`depth_cm`/`height_cm` (and corresponding `internal_structure` location dimensions) using template level geometry; removed the previous rack-width auto-recalculation override path so template values are preserved 1:1.
- Refactored picking-route UX in `WarehouseDesigner` to remove floating route panel and route-mode toggles: route selection now uses ordered `routeRackIds` as the single source, rack clicks always toggle route membership, and route visibility is automatic when at least 2 stops exist.
- Added route planning UI to the existing right properties sidebar (`RackPropertiesSidebar`) as "Trasa kompletacji" with ordered stop list, distance readout, "Wyczyść trasę", and "Optymalizuj kolejność" (stub).
- Added visual map feedback for route order directly in `RackLayer` via numbered badges on selected racks; wiring was passed through `WarehouseCanvas`/`WarehouseMainView`.
- Complaint detail/WMS defects refactor: removed global "Wady" section from `ComplaintDetailPage`; added per-line defects display in `ComplaintLinesDecisionsPanel` right column above warehouse photos, with pill tags and empty state text.
- Complaint line data model extended with `defect_ids_json` (`backend/models/complaint_line.py` + schema upgrade), exposed as `defect_ids` in `ComplaintLineRead` and frontend `ComplaintLineDetail`, so defects are bound to `complaint_item_id` instead of complaint-global state.
- Complaint defects UI moved again in `ComplaintLinesDecisionsPanel`: removed "Wady" from right operations/photos column and placed defects directly in product header (under SKU/EAN), with quantity badge aligned to product name and subtle empty state text "Brak wad".
- Fixed complaint create/fetch mapping for defects and photos:
  - backend `ComplaintCreateFromOrderLine` now accepts per-line defect aliases (`defect_ids`, `defects`, `reasons`, `complaint_reasons`);
  - create flow persists per-line defects into `complaint_lines.defect_ids_json` (with fallback from top-level `defect_ids`);
  - complaint read splits line photos into `customer_photos` vs `warehouse_photos` using top-level warehouse photo set;
  - frontend `complaintsApi` normalizes line defect fields from alternate response names to `defect_ids`, and logs create payload + fetched lines in DEV.
- Complaint defects moved from complaint-level state to item-level end-to-end:
  - `NewComplaintWizard` now selects defects per selected order line and sends them in each line payload (`defect_ids` + alias `defects`);
  - backend create no longer persists top-level `complaints.defects_json` for new complaints (line-level only), while read keeps migration fallback by applying legacy complaint defects to lines that have no own defects;
  - backend `ComplaintLineRead` now includes `defects: [{ id, name }]` alongside `defect_ids`;
  - WMS complaint detail tags now read `line.defect_ids` per product (removed fallback to global `data.defect_ids`).
- Photo upload mapping hardening: `uploadComplaintPanelPhotos` now resolves `photo_kind` explicitly from `isWmsView` (`warehouse` in WMS, otherwise `customer`) and logs the resolved kind in DEV; WMS upload callsites now pass explicit `isWmsView=true`.
- Standardized page-size selector UX for paginated tables with a new shared component `frontend/src/components/table/DataTablePageSizeSelect.tsx` (`Pokaż na stronie`, options `10/25/50/100/250`, default `25`).
- Wired persistent per-module page size (`localStorage`) and reset-to-page-1 behavior on page-size change in:
  - `PurchasingReplenishmentPage` (`purchase_generator.pageSize`, server `page_size` respected),
  - `PurchasingPoPage` (`purchase_orders.pageSize`, server `page_size` respected),
  - `ProductWarehouseMovementsPanel` (`product_movements.pageSize`, server `limit` respected),
  - `DocumentsWarehousePage` (`warehouse_docs.pageSize`, added client-side pagination + controls).
- Added central cost/margin service `backend/services/product_cost_service.py` with:
  - `get_product_current_cost`, `refresh_product_cost_from_pz`, `calculate_product_margin`,
  - source priority: latest posted PZ weighted average -> latest supplier price -> manual `product.purchase_price`,
  - landed margin formula: `landed_cost_net = purchase_net + extra_cost_net`, `margin_value = sale_net - landed_cost_net`, `margin_percent = margin_value / sale_net * 100`.
- Added per-product extra cost columns (SQLite migration + model): `extra_cost_packaging_net`, `extra_cost_commission_percent`, `extra_cost_other_net`; wired in product create/update API and product serialization.
- Product API now returns `current_cost` snapshot (trusted backend calculation) for product read/create/update paths; Product edit UI now shows "OSTATNI KOSZT" with margin color thresholds and editable extra-cost inputs.
- Purchasing replenishment margin now uses landed cost from central service (not raw buy price only), and new filters were added: `show_loss_products`, `low_margin_lt`.

## 2026-05-06

- **Orders list (priority flame in row number):** `OrderListDenseTable` now renders `OrderPriorityFlameIcon` before `#orderNumber` in the `order_core` cell, using the same backend-driven `priority_color` field as order details. The list API already returns `priority_color`, so no serializer/DTO changes were needed.
- **WMS settings layout polish (sticky nav + save bar shell):**
  - `WmsSettingsStickyAside` switched to a stable two-column grid (`260px + content`) with sticky nav (`top-24`) so subsection navigation stays visible while scrolling.
  - `WmsSettingsPage` now keeps tabs and active tab content inside one white card container (same border/background), removing the visual split where settings content looked outside the card.
  - `StickySaveBar` restyled to align with page gutters (`px-4 md:px-6`) and a rounded bordered inner shell, making the save bar feel attached to settings content instead of full-viewport detached chrome.
- **WMS settings layout follow-up (container-attached sticky save + sticky nav at md):**
  - `StickySaveBar` is no longer viewport-fixed; it now renders as `sticky bottom-0` inside the white WMS settings card and aligns to card edges (`-mx-5`), so it does not overlap the global app sidebar area.
  - `WmsSettingsPage` now renders the save bar inside the same settings card, directly after tab content.
  - `WmsSettingsStickyAside` sticky behavior now starts on `md` breakpoint (not only `lg`), so subsection navigation remains sticky during regular settings-page scrolling.

## 2026-05-25

- **Analiza → Centrum operacyjne:** added a live WMS control-center module at `/analytics/warehouse-operations` (alias `/analytics/live-warehouse`) with sticky KPI strip, compact operator cards, operational queues, alert panel, activity stream, operator detail modal, local idle threshold configuration, and CSV/XLSX export filters.
- **Centrum operacyjne navigation separation:** moved `/analytics/warehouse-operations` out of `AnalyticsLayout` so the legacy analytics tab shell does not render inside the WMS cockpit. `Analiza` sidebar flyout now treats it as a sibling of Dashboard / Analityka / Symulacje / Optymalizacja / Mapy, and the page has its own operational tabs (Live, Operatorzy, Kolejki, Kompletacja, Pakowanie, Operacje magazynowe, Alerty, Historia).
- **Warehouse operations aggregation:** added `warehouse_operations_service` + schemas + API under `/api/wms/warehouse-operations/*`. It reuses existing event/session/activity data instead of new workflow tables: `wms_order_events` for picking/packing/timeline, `user_activity_logs` for receiving/putaway/MM/carriers, `warehouse_inventory_movements` for durable operation counts, picks/order items for progress, stock documents for receiving/putaway queues, and WMS shortage/operational tasks for problem queues.
- **Idle analytics:** inactivity counts only gaps above the configured short threshold; a 6-minute gap counts as the full 6 minutes. Status colors use green `<= short`, gray `> short`, red `> long`, with defaults 5/10 minutes.
