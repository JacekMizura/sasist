# current-context

## Active

**Layout + Routing UX rev. 3.1** — C1–C4 accepted. Pre-push: PassageInspector wired (uncommitted).

### Commits (local main, ahead 5)
1. `ed5dba18` C1 Passage defaults
2. `d325862b` C2 TemplateCreator overlay
3. `ee6c7cef` C3 Routing Edit
4. `fe156c43` C4 Command layer
5. `a5065e86` C3 gap-fix endpoint drag + inspectors

### Working tree
- PassageInspector mounted; Otwórz szablon → TemplateCreator; awaiting user OK before push

### Constraints
- Bez push / deploy / PROD / Etapu 3; OMS tests out of scope
