# current-context

## Active

**Print dialog UX:** operator chooses template + place (workstation with printer name / online status) + PDF/browser alternatives. Never shows „Sasist Agent”. Prefs in `localStorage` per document type. `template_version_id` flows through queue for stock/sale/production cards. List API exposes `default_printer_name`.
