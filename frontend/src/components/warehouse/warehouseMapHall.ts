/**
 * Shared map hall chrome (white surface, soft shadow, scroll) — Magazyn visual SSOT.
 * Class defined in index.css as `.warehouse-magazyn-hall` (kept for now; rename later if needed).
 */
export const warehouseMapHallClassName =
  "warehouse-magazyn-hall flex min-h-0 min-w-0 max-w-full w-full flex-1 flex-col overflow-auto overscroll-y-contain";
