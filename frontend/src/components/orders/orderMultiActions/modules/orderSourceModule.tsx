import { Globe } from "lucide-react";

import { createStubOrderModule } from "./stubModule";

export const orderSourceModule = createStubOrderModule(
  "order_source",
  "Źródło zamówienia",
  "Klient",
  Globe,
  "Źródło zamówienia — w przygotowaniu.",
);
