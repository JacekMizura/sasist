/**
 * Alias for the shared “visible fields” modal — same UX across modules.
 * Prefer importing `FilterVisibilityModal` directly when you need custom copy.
 */
export { FilterVisibilityModal as VisibleFieldsManager } from "./FilterVisibilityModal";
export type { FilterFieldCatalogItem } from "./FilterVisibilityModal";
