import WmsInventoryDocumentList from "@/modules/inventoryCount/ui/wms/WmsInventoryDocumentList";

/** Start screen — document queue (no sidebar, no ERP actions). */
export default function WmsInventoryCountLandingPage() {
  return <WmsInventoryDocumentList />;
}
