/** Canonical `<section>` anchor styling (scroll-margin). */
export const WMS_SETTINGS_SECTION_ANCHOR_CLASS = "scroll-mt-32";

/** Sticky line inside scroll container (px from visible top). Scroll + scrollspy use the same value. */
export const WMS_SETTINGS_STICKY_LINE_PX = 140;

export const WMS_SETTINGS_ACTIVATION_TOLERANCE_PX = 40;
