/** Nested overlays: drawers, confirm dialogs, floating menus. */
export const Z_WAREHOUSE_DOC_OVERLAY = 280;

/** Global toasts above page content. */
export const Z_WAREHOUSE_DOC_TOAST = 400;

/** Destructive confirm above everything else in documents module. */
export const Z_WAREHOUSE_DOC_CONFIRM = 500;
