/**
 * Product photos must never sit on a gray surface — always white behind the image.
 * Use for gallery cards, thumbnails, edit previews, and header product images.
 */
export const PRODUCT_IMAGE_SURFACE_CLASS = "bg-white";
