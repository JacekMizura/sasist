{
  "private": true,
  "description": "Legacy root deps for PDF tooling — NOT the Railway backend app. API deploy uses nixpacks.toml providers=[python].",
  "dependencies": {
    "@react-pdf/renderer": "^4.3.2",
    "bcryptjs": "^3.0.3"
  }
}
